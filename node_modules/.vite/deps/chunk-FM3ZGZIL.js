import {
  l
} from "./chunk-JA7GW34Q.js";
import {
  D
} from "./chunk-62AMW2EQ.js";
import {
  u
} from "./chunk-VWVJEZKN.js";

// node_modules/@arcgis/core/renderers/support/renderingInfoUtils.js
function i(e, t) {
  if (null != e.symbol) return e.symbol;
  const o = t?.renderer;
  return null != o && "dot-density" !== o.type ? o.getSymbol(e, t) : null;
}
function r(e, r2) {
  const l3 = i(e, r2);
  if (null == l3) return null;
  const n2 = r2?.renderer, a2 = new l(n2, l3);
  if (null == n2 || !("visualVariables" in n2) || !n2.visualVariables) return a2;
  const s2 = D(n2, e, r2) ?? [], u2 = ["proportional", "proportional", "proportional"];
  for (const { variable: t, value: o } of s2) if (null != o || "size" === t.type && t.useSymbolValue) switch (t.type) {
    case "color":
      a2.color = o?.toRgba();
      break;
    case "size":
      if ("outline" === t.target) a2.outlineSize = o;
      else {
        const e2 = t.axis, i2 = t.useSymbolValue ? "symbol-value" : o ?? "proportional";
        switch (e2) {
          case "width":
            u2[0] = i2;
            break;
          case "depth":
            u2[1] = i2;
            break;
          case "height":
            u2[2] = i2;
            break;
          case "width-and-depth":
            u2[0] = u2[1] = i2;
            break;
          default:
            u2[0] = u2[1] = u2[2] = i2;
        }
      }
      break;
    case "opacity":
      a2.opacity = o;
      break;
    case "rotation":
      switch (t.axis) {
        case "tilt":
          a2.tilt = o;
          break;
        case "roll":
          a2.roll = o;
          break;
        default:
          a2.heading = o;
      }
  }
  return "proportional" === u2[0] && "proportional" === u2[1] && "proportional" === u2[2] || (a2.size = u2), a2;
}
async function l2(e, t) {
  return null != e.symbol ? e.symbol : t?.renderer?.getSymbolAsync(e, t) ?? null;
}
async function n(i2, r2) {
  const n2 = await l2(i2, r2);
  if (!n2) return null;
  const a2 = r2?.renderer, s2 = new l(a2, n2);
  if (!a2 || !("visualVariables" in a2) || !a2.visualVariables) return s2;
  const u2 = D(a2, i2, r2) ?? [], p = ["proportional", "proportional", "proportional"];
  for (const { variable: t, value: o } of u2) if ("color" === t.type) s2.color = u.toUnitRGBA(o);
  else if ("size" === t.type) if ("outline" === t.target) s2.outlineSize = o;
  else {
    const e = t.axis, i3 = t.useSymbolValue ? "symbol-value" : o;
    "width" === e ? p[0] = i3 : "depth" === e ? p[1] = i3 : "height" === e ? p[2] = i3 : p[0] = p[1] = "width-and-depth" === e ? i3 : p[2] = i3;
  }
  else "opacity" === t.type ? s2.opacity = o : "rotation" === t.type && "tilt" === t.axis ? s2.tilt = o : "rotation" === t.type && "roll" === t.axis ? s2.roll = o : "rotation" === t.type && (s2.heading = o);
  return (isFinite(p[0]) || isFinite(p[1]) || isFinite(p[2])) && (s2.size = p), s2;
}
function a(e, t = 0) {
  const o = e[t];
  return "number" == typeof o && isFinite(o) ? o : null;
}
function s(e) {
  for (let t = 0; t < 3; t++) {
    const o = e[t];
    if ("number" == typeof o) return isFinite(o) ? o : 0;
  }
  return 0;
}

export {
  r,
  n,
  a,
  s
};
//# sourceMappingURL=chunk-FM3ZGZIL.js.map
