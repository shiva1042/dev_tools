import {
  J,
  T,
  f,
  g as g2,
  h,
  m,
  o,
  p,
  y
} from "./chunk-FU4YCUH5.js";
import {
  s
} from "./chunk-77HFCFIB.js";
import {
  c,
  g,
  r
} from "./chunk-5ZRDULGW.js";
import {
  G,
  H
} from "./chunk-YHUFOIJJ.js";
import {
  n
} from "./chunk-SWU3RIKJ.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/materials/internal/bufferWriterUtils.js
function b(e, t, f2, o2 = 1) {
  const { data: r2, indices: i } = e, s2 = t.typedBuffer, n2 = t.typedBufferStride, c2 = i.length;
  if (f2 *= n2, 1 === o2) for (let l = 0; l < c2; ++l) s2[f2] = r2[i[l]], f2 += n2;
  else for (let l = 0; l < c2; ++l) {
    const e2 = r2[i[l]];
    for (let t2 = 0; t2 < o2; t2++) s2[f2] = e2, f2 += n2;
  }
}
function m2(e, t, f2) {
  const { data: o2, indices: r2 } = e, i = t.typedBuffer, s2 = t.typedBufferStride, n2 = r2.length;
  f2 *= s2;
  for (let c2 = 0; c2 < n2; ++c2) {
    const e2 = 2 * r2[c2];
    i[f2] = o2[e2], i[f2 + 1] = o2[e2 + 1], f2 += s2;
  }
}
function B(e, t, f2, o2 = 1) {
  const { data: r2, indices: i } = e, s2 = t.typedBuffer, n2 = t.typedBufferStride, c2 = i.length;
  if (f2 *= n2, 1 === o2) for (let l = 0; l < c2; ++l) {
    const e2 = 3 * i[l];
    s2[f2] = r2[e2], s2[f2 + 1] = r2[e2 + 1], s2[f2 + 2] = r2[e2 + 2], f2 += n2;
  }
  else for (let l = 0; l < c2; ++l) {
    const e2 = 3 * i[l];
    for (let t2 = 0; t2 < o2; ++t2) s2[f2] = r2[e2], s2[f2 + 1] = r2[e2 + 1], s2[f2 + 2] = r2[e2 + 2], f2 += n2;
  }
}
function h2(e, t, f2, o2 = 1) {
  const { data: r2, indices: i } = e, s2 = t.typedBuffer, n2 = t.typedBufferStride, c2 = i.length;
  if (f2 *= n2, 1 === o2) for (let l = 0; l < c2; ++l) {
    const e2 = 4 * i[l];
    s2[f2] = r2[e2], s2[f2 + 1] = r2[e2 + 1], s2[f2 + 2] = r2[e2 + 2], s2[f2 + 3] = r2[e2 + 3], f2 += n2;
  }
  else for (let l = 0; l < c2; ++l) {
    const e2 = 4 * i[l];
    for (let t2 = 0; t2 < o2; ++t2) s2[f2] = r2[e2], s2[f2 + 1] = r2[e2 + 1], s2[f2 + 2] = r2[e2 + 2], s2[f2 + 3] = r2[e2 + 3], f2 += n2;
  }
}
function F(e, t, f2) {
  const o2 = e.typedBuffer, r2 = e.typedBufferStride;
  t *= r2;
  for (let i = 0; i < f2; ++i) o2[t] = 0, o2[t + 1] = 0, o2[t + 2] = 0, o2[t + 3] = 0, t += r2;
}
function z(e, t, f2, o2, i = 1) {
  if (!t) return void B(e, f2, o2, i);
  const { data: s2, indices: n2 } = e, c2 = f2.typedBuffer, l = f2.typedBufferStride, d = n2.length, u = t[0], a = t[1], p2 = t[2], g3 = t[4], y2 = t[5], b2 = t[6], m3 = t[8], h3 = t[9], F2 = t[10], v = t[12], w = t[13], z2 = t[14];
  o2 *= l;
  let S2 = 0, k2 = 0, N2 = 0;
  const $2 = H(t) ? (e2) => {
    S2 = s2[e2] + v, k2 = s2[e2 + 1] + w, N2 = s2[e2 + 2] + z2;
  } : (e2) => {
    const t2 = s2[e2], f3 = s2[e2 + 1], o3 = s2[e2 + 2];
    S2 = u * t2 + g3 * f3 + m3 * o3 + v, k2 = a * t2 + y2 * f3 + h3 * o3 + w, N2 = p2 * t2 + b2 * f3 + F2 * o3 + z2;
  };
  if (1 === i) for (let r2 = 0; r2 < d; ++r2) $2(3 * n2[r2]), c2[o2] = S2, c2[o2 + 1] = k2, c2[o2 + 2] = N2, o2 += l;
  else for (let r2 = 0; r2 < d; ++r2) {
    $2(3 * n2[r2]);
    for (let e2 = 0; e2 < i; ++e2) c2[o2] = S2, c2[o2 + 1] = k2, c2[o2 + 2] = N2, o2 += l;
  }
}
function S(e, t, f2, o2, s2 = 1) {
  if (!t) return void B(e, f2, o2, s2);
  const { data: n2, indices: c2 } = e, l = t, d = f2.typedBuffer, u = f2.typedBufferStride, a = c2.length, p2 = l[0], g3 = l[1], y2 = l[2], b2 = l[4], m3 = l[5], h3 = l[6], F2 = l[8], v = l[9], w = l[10], z2 = !G(l), S2 = 1e-6, k2 = 1 - S2;
  o2 *= u;
  let N2 = 0, $2 = 0, x2 = 0;
  const A2 = H(l) ? (e2) => {
    N2 = n2[e2], $2 = n2[e2 + 1], x2 = n2[e2 + 2];
  } : (e2) => {
    const t2 = n2[e2], f3 = n2[e2 + 1], o3 = n2[e2 + 2];
    N2 = p2 * t2 + b2 * f3 + F2 * o3, $2 = g3 * t2 + m3 * f3 + v * o3, x2 = y2 * t2 + h3 * f3 + w * o3;
  };
  if (1 === s2) if (z2) for (let r2 = 0; r2 < a; ++r2) {
    A2(3 * c2[r2]);
    const e2 = N2 * N2 + $2 * $2 + x2 * x2;
    if (e2 < k2 && e2 > S2) {
      const t2 = 1 / Math.sqrt(e2);
      d[o2] = N2 * t2, d[o2 + 1] = $2 * t2, d[o2 + 2] = x2 * t2;
    } else d[o2] = N2, d[o2 + 1] = $2, d[o2 + 2] = x2;
    o2 += u;
  }
  else for (let r2 = 0; r2 < a; ++r2) A2(3 * c2[r2]), d[o2] = N2, d[o2 + 1] = $2, d[o2 + 2] = x2, o2 += u;
  else for (let r2 = 0; r2 < a; ++r2) {
    if (A2(3 * c2[r2]), z2) {
      const e2 = N2 * N2 + $2 * $2 + x2 * x2;
      if (e2 < k2 && e2 > S2) {
        const t2 = 1 / Math.sqrt(e2);
        N2 *= t2, $2 *= t2, x2 *= t2;
      }
    }
    for (let e2 = 0; e2 < s2; ++e2) d[o2] = N2, d[o2 + 1] = $2, d[o2 + 2] = x2, o2 += u;
  }
}
function k(e, t, f2, o2, r2 = 1) {
  if (!t) return void h2(e, f2, o2, r2);
  const { data: s2, indices: n2 } = e, c2 = t, l = f2.typedBuffer, d = f2.typedBufferStride, u = n2.length, a = c2[0], p2 = c2[1], g3 = c2[2], y2 = c2[4], b2 = c2[5], m3 = c2[6], B2 = c2[8], F2 = c2[9], v = c2[10], w = !G(c2), z2 = 1e-6, S2 = 1 - z2;
  if (o2 *= d, 1 === r2) for (let i = 0; i < u; ++i) {
    const e2 = 4 * n2[i], t2 = s2[e2], f3 = s2[e2 + 1], r3 = s2[e2 + 2], c3 = s2[e2 + 3];
    let u2 = a * t2 + y2 * f3 + B2 * r3, h3 = p2 * t2 + b2 * f3 + F2 * r3, k2 = g3 * t2 + m3 * f3 + v * r3;
    if (w) {
      const e3 = u2 * u2 + h3 * h3 + k2 * k2;
      if (e3 < S2 && e3 > z2) {
        const t3 = 1 / Math.sqrt(e3);
        u2 *= t3, h3 *= t3, k2 *= t3;
      }
    }
    l[o2] = u2, l[o2 + 1] = h3, l[o2 + 2] = k2, l[o2 + 3] = c3, o2 += d;
  }
  else for (let i = 0; i < u; ++i) {
    const e2 = 4 * n2[i], t2 = s2[e2], f3 = s2[e2 + 1], c3 = s2[e2 + 2], u2 = s2[e2 + 3];
    let h3 = a * t2 + y2 * f3 + B2 * c3, k2 = p2 * t2 + b2 * f3 + F2 * c3, N2 = g3 * t2 + m3 * f3 + v * c3;
    if (w) {
      const e3 = h3 * h3 + k2 * k2 + N2 * N2;
      if (e3 < S2 && e3 > z2) {
        const t3 = 1 / Math.sqrt(e3);
        h3 *= t3, k2 *= t3, N2 *= t3;
      }
    }
    for (let i2 = 0; i2 < r2; ++i2) l[o2] = h3, l[o2 + 1] = k2, l[o2 + 2] = N2, l[o2 + 3] = u2, o2 += d;
  }
}
function N(e, t, f2, o2, r2 = 1) {
  const { data: i, indices: s2 } = e, n2 = f2.typedBuffer, c2 = f2.typedBufferStride, l = s2.length;
  if (o2 *= c2, t === i.length && 4 === t) {
    n2[o2] = i[0], n2[o2 + 1] = i[1], n2[o2 + 2] = i[2], n2[o2 + 3] = i[3];
    const e2 = new Uint32Array(f2.typedBuffer.buffer, f2.start), t2 = c2 / 4, s3 = e2[o2 /= 4];
    o2 += t2;
    const d = l * r2;
    for (let f3 = 1; f3 < d; ++f3) e2[o2] = s3, o2 += t2;
    return;
  }
  if (1 !== r2) if (4 !== t) for (let d = 0; d < l; ++d) {
    const e2 = 3 * s2[d];
    for (let t2 = 0; t2 < r2; ++t2) n2[o2] = i[e2], n2[o2 + 1] = i[e2 + 1], n2[o2 + 2] = i[e2 + 2], n2[o2 + 3] = 255, o2 += c2;
  }
  else for (let d = 0; d < l; ++d) {
    const e2 = 4 * s2[d];
    for (let t2 = 0; t2 < r2; ++t2) n2[o2] = i[e2], n2[o2 + 1] = i[e2 + 1], n2[o2 + 2] = i[e2 + 2], n2[o2 + 3] = i[e2 + 3], o2 += c2;
  }
  else {
    if (4 === t) {
      for (let e2 = 0; e2 < l; ++e2) {
        const t2 = 4 * s2[e2];
        n2[o2] = i[t2], n2[o2 + 1] = i[t2 + 1], n2[o2 + 2] = i[t2 + 2], n2[o2 + 3] = i[t2 + 3], o2 += c2;
      }
      return;
    }
    for (let e2 = 0; e2 < l; ++e2) {
      const t2 = 3 * s2[e2];
      n2[o2] = i[t2], n2[o2 + 1] = i[t2 + 1], n2[o2 + 2] = i[t2 + 2], n2[o2 + 3] = 255, o2 += c2;
    }
  }
}
function $(e, t, f2) {
  const { data: o2, indices: r2 } = e, i = t.typedBuffer, s2 = t.typedBufferStride, n2 = r2.length, c2 = o2[0];
  f2 *= s2;
  for (let l = 0; l < n2; ++l) i[f2] = c2, f2 += s2;
}
function x(o2, r2, i, s2) {
  c(A, o2, r2);
  const n2 = Math.max(Math.sqrt(r(A)), 1e-4);
  g(A, A, 1 / n2), i[s2++] = A[0], i[s2++] = A[1], i[s2++] = A[2], i[s2++] = n2;
}
var A = n();
function M(e, t, f2, o2, r2 = 1) {
  const i = t.typedBuffer, s2 = t.typedBufferStride;
  if (o2 *= s2, 1 === r2) for (let n2 = 0; n2 < f2; ++n2) i[o2] = e[0], i[o2 + 1] = e[1], i[o2 + 2] = e[2], i[o2 + 3] = e[3], o2 += s2;
  else for (let n2 = 0; n2 < f2; ++n2) for (let t2 = 0; t2 < r2; ++t2) i[o2] = e[0], i[o2 + 1] = e[1], i[o2 + 2] = e[2], i[o2 + 3] = e[3], o2 += s2;
}
function j(e, t, f2, o2, r2, i, n2) {
  let c2 = { numItems: 0, numVerticesPerItem: 0 };
  for (const l of f2.fields.keys()) {
    const f3 = e.get(l), d = f3?.indices;
    if (f3 && d) "position" === l && (c2 = { numItems: 1, numVerticesPerItem: d.length }), q(l, f3, o2, r2, i, n2);
    else if ("olidColor" === l && null != t) {
      const f4 = e.get("position")?.indices;
      if (f4) {
        const e2 = f4.length;
        M(t, i.getField(l, g2), e2, n2);
      }
    }
  }
  return c2;
}
function q(e, t, f2, o2, r2, i) {
  switch (e) {
    case "position": {
      s(3 === t.size);
      const o3 = r2.getField(e, o);
      s(!!o3, `No buffer view for ${e}`), z(t, f2, o3, i);
      break;
    }
    case "normal": {
      s(3 === t.size);
      const f3 = r2.getField(e, o);
      s(!!f3, `No buffer view for ${e}`), S(t, o2, f3, i);
      break;
    }
    case "normalCompressed":
    case "profileRight":
    case "profileUp": {
      s(2 === t.size);
      const f3 = r2.getField(e, J);
      s(!!f3, `No buffer view for ${e}`), m2(t, f3, i);
      break;
    }
    case "uv0": {
      s(2 === t.size);
      const f3 = r2.getField(e, p) ?? r2.getField(e, y);
      s(!!f3, `No buffer view for ${e}`), m2(t, f3, i);
      break;
    }
    case "uvi": {
      s(2 === t.size);
      const f3 = r2.getField(e, J);
      s(!!f3, `No buffer view for ${e}`), m2(t, f3, i);
      break;
    }
    case "color":
    case "symbolColor": {
      const f3 = r2.getField(e, g2);
      s(!!f3, `No buffer view for ${e}`), s(3 === t.size || 4 === t.size), N(t, t.size, f3, i);
      break;
    }
    case "colorFeatureAttribute":
    case "opacityFeatureAttribute":
    case "sizeFeatureAttribute": {
      const f3 = r2.getField(e, f) ?? r2.getField(e, f);
      s(!!f3, `No buffer view for ${e}`), s(1 === t.size), $(t, f3, i);
      break;
    }
    case "tangent": {
      s(4 === t.size);
      const o3 = r2.getField(e, T);
      s(!!o3, `No buffer view for ${e}`), k(t, f2, o3, i);
      break;
    }
    case "profileVertexAndNormal": {
      s(4 === t.size);
      const f3 = r2.getField(e, m) ?? r2.getField(e, T);
      s(!!f3, `No buffer view for ${e}`), h2(t, f3, i);
      break;
    }
    case "profileAuxData": {
      s(3 === t.size);
      const f3 = r2.getField(e, h) ?? r2.getField(e, o);
      s(!!f3, `No buffer view for ${e}`), B(t, f3, i);
      break;
    }
  }
}

export {
  b,
  m2 as m,
  h2 as h,
  F,
  z,
  S,
  N,
  x,
  M,
  j,
  q
};
//# sourceMappingURL=chunk-B65PBZPY.js.map
