import {
  t as t2
} from "./chunk-65ZDE4MC.js";
import {
  c
} from "./chunk-VZKS734B.js";
import {
  e
} from "./chunk-IT62DCG6.js";
import {
  e as e3
} from "./chunk-SZ5DGQ75.js";
import {
  r
} from "./chunk-YOYO2XMY.js";
import {
  e as e2
} from "./chunk-SZTZNZQW.js";
import {
  n,
  t
} from "./chunk-CFXCM2NO.js";
import {
  l
} from "./chunk-7SS3OQIH.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/terrain/TileComposite.glsl.js
var a = class extends c {
  constructor() {
    super(...arguments), this.scale = 1, this.offset = l;
  }
};
function i(s) {
  s.attributes.add("position", "vec2"), s.attributes.add("uv0", "vec2"), s.varyings.add("uv", "vec2"), s.varyings.add("vuv", "vec2"), s.vertex.uniforms.add(new r("scale", (s2) => s2.scale), new e3("offset", (s2) => s2.offset)).main.add(t`gl_Position = vec4(position, 0.0, 1.0);
uv = uv0 * scale + offset;
vuv = uv0;`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/util/BlendModes.glsl.js
function o(o2, l2) {
  const b2 = l2.blendMode;
  0 !== b2 && (30 === b2 && o2.code.add(t`float reflectBlend(in float cb, in float cl) {
return (cl == 1.0) ? cl : min(cb * cb / (1.0 - cl), 1.0);
}`), 6 !== b2 && 13 !== b2 || o2.code.add(t`float colorDodge(in float cb, in float cl) {
return (cb == 0.0) ? 0.0 : (cl == 1.0) ? 1.0 : min(1.0, cb / (1.0 - cl));
}`), 9 !== b2 && 13 !== b2 || o2.code.add(t`float colorBurn(in float cb, in float cl) {
return (cb == 1.0) ? 1.0 : (cl == 0.0) ? 0.0 : 1.0 - min(1.0, (1.0 - cb) / cl);
}`), 10 === b2 && o2.code.add(t`float overlay(in float cb, in float cl) {
return (1.0 - step(0.5, cl)) * (1.0 - 2.0 * (1.0 - cl ) * (1.0 - cb)) + step(0.5, cl) * (2.0 * cl * cb);
}`), 12 === b2 && o2.code.add(t`float hardLight(in float cb, in float cl) {
return (1.0 - step(0.5, cl)) * (2.0 * cl * cb) + step(0.5, cl) * (1.0 - 2.0 * (1.0 - cl) * (1.0 - cb));
}`), 11 === b2 && o2.code.add(t`float softLight(in float cb, in float cl) {
if (cl <= 0.5) {
return cb - (1.0 - 2.0 * cl) * cb * (1.0 - cb);
}
if (cb <= 0.25) {
return cb + (2.0 * cl - 1.0) * cb * ((16.0 * cb - 12.0) * cb + 3.0);
}
return cb + (2.0 * cl - 1.0) * (sqrt(cb) - cb);
}`), 13 === b2 && o2.code.add(t`float vividLight(in float cb, in float cl) {
return (1.0 - step(0.5, cl)) * colorBurn(cb, 2.0 * cl) + step(0.5, cl) * colorDodge(cb, (2.0 * (cl - 0.5)));
}`), 14 !== b2 && 15 !== b2 && 17 !== b2 && 16 !== b2 || (o2.code.add(t`float minv3(in vec3 c) {
return min(min(c.r, c.g), c.b);
}
float maxv3(in vec3 c) {
return max(max(c.r, c.g), c.b);
}
float lumv3(in vec3 c) {
return dot(c, vec3(0.3, 0.59, 0.11));
}
vec3 clipColor(vec3 color) {
float lum = lumv3(color);
float mincol = minv3(color);
float maxcol = maxv3(color);
if (mincol < 0.0) {
color = lum + ((color - lum) * lum) / (lum - mincol);
}
if (maxcol > 1.0) {
color = lum + ((color - lum) * (1.0 - lum)) / (maxcol - lum);
}
return color;
}
vec3 setLum(vec3 cbase, vec3 clum) {
return clipColor(cbase + vec3(lumv3(clum) - lumv3(cbase)));
}`), 14 !== b2 && 15 !== b2 || o2.code.add(t`float satv3(vec3 c) {
return maxv3(c) - minv3(c);
}
vec3 setLumSat(vec3 cbase, vec3 csat, vec3 clum)
{
float minbase = minv3(cbase);
float sbase = satv3(cbase);
float ssat = satv3(csat);
return setLum(sbase > 0.0 ? (cbase - minbase) * ssat / sbase : vec3(0.0), clum);
}`)), o2.code.add(t`
    vec4 applyBlendMode(vec3 cl, float ol, vec3 cb, float ob) {
      ${8 === b2 ? t`return vec4(cl * ol * cb * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 1 === b2 ? t`return vec4((cb + cl) * 0.5 * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 2 === b2 ? t`return vec4(max(cb, cl) * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 7 === b2 ? t`return vec4(min(cl, cb) * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 3 === b2 ? t`return vec4(cl * ol + cb * ob, ol + ob);` : 4 === b2 ? t`return clamp(vec4(cl.rgb + cb.rgb, ol + ob), 0.0, 1.0);` : 28 === b2 ? t`return vec4(clamp(vec3(cb.rgb - cl.rgb), 0.0, 1.0), ob * ol);` : 5 === b2 ? t`return vec4((cl + cb - cl * cb) * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 26 === b2 ? t`return vec4(abs(cb - cl) * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 29 === b2 ? t`return vec4((1.0 - cb) * ol * ob + cb * ob * (1.0 - ol), ob);` : 18 === b2 ? t`return vec4(cl * ol * (1.0 - ob) + cb * ob, ol + ob - ol * ob);` : 19 === b2 ? t`return vec4(cl * ol * (1.0 - ob) + cb * ob * ol, ol);` : 21 === b2 ? t`return vec4(cb * ob * (1.0 - ol), ob * (1.0 - ol));` : 22 === b2 ? t`return vec4(cl * ol * ob + cb * ob * (1.0 - ol), ob);` : 24 === b2 ? t`return vec4(cl * ol * (1.0 - ob), ol * (1.0 - ob));` : 25 === b2 ? t`return vec4(cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), ol * (1.0 - ob) + ob * (1.0 - ol));` : 20 === b2 ? t`return vec4(cb * ob * ol, ol * ob);` : 23 === b2 ? t`return vec4(cl * ol * ob, ol * ob);` : 14 === b2 ? t`
          vec3 f = setLumSat(cl, cb, cb);
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 15 === b2 ? t`
          vec3 f = setLumSat(cb, cl, cb);
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 17 === b2 ? t`
          vec3 f = setLum(cl, cb);
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 16 === b2 ? t`
          vec3 f = setLum(cb, cl);
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 27 === b2 ? t`
          vec3 f = cl + cb - 2.0 * cl * cb;
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 30 === b2 ? t`
          vec3 f = vec3(reflectBlend(cb.r, cl.r), reflectBlend(cb.g, cl.g), reflectBlend(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 6 === b2 ? t`
          vec3 f = vec3(colorDodge(cb.r, cl.r), colorDodge(cb.g, cl.g), colorDodge(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 9 === b2 ? t`
          vec3 f = vec3(colorBurn(cb.r, cl.r), colorBurn(cb.g, cl.g), colorBurn(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 10 === b2 ? t`
          vec3 f = vec3(overlay(cb.r, cl.r), overlay(cb.g, cl.g), overlay(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 11 === b2 ? t`
          vec3 f = vec3(softLight(cb.r, cl.r), softLight(cb.g, cl.g), softLight(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 12 === b2 ? t`
          vec3 f = vec3(hardLight(cb.r, cl.r), hardLight(cb.g, cl.g), hardLight(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : 13 === b2 ? t`
          vec3 f = vec3(vividLight(cb.r, cl.r), vividLight(cb.g, cl.g), vividLight(cb.b, cl.b));
          return vec4(f * ol * ob + cl * ol * (1.0 - ob) + cb * ob * (1.0 - ol), mix(ob, 1.0, ol));` : t``}
    }
  `));
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/terrain/TileBackground.glsl.js
function b(o2, i2) {
  const { output: d, blendMode: b2, baseOpacityMode: g, premultipliedSource: n2 } = i2, u = o2.fragment, p = 1 === g;
  p && u.uniforms.add(new r("baseOpacity", (o3) => o3.baseOpacity));
  const m = 0 !== b2, f = !m && !(1 === n2) && (1 === d && !p || 4 === d);
  u.include(o, i2);
  let y = "";
  switch (d) {
    case 4:
    case 0:
      y = t`vec4(0.0)`;
      break;
    case 2:
      u.uniforms.add(new e("backgroundColor", (o3) => o3.backgroundColor)), y = t`vec4(backgroundColor, 1.0)`;
      break;
    case 3:
      u.include(t2), y = t`vec4(gridColor(uv), 1.0)`;
      break;
    case 1:
      u.uniforms.add(new e2("fboColor", (o3) => o3.fboTexture)), y = t`texelFetch(fboColor, ivec2(gl_FragCoord.xy), 0)`;
  }
  u.code.add(t`
    vec4 getBackground(vec2 uv) {
      return ${n(p, t`baseOpacity *`)} ${y};
    }

    vec4 blendLayers(vec2 bgUV, vec4 colorLayer, float opacity) {
      ${m ? t`
          vec3 cl = colorLayer.a == 0.0 ? colorLayer.rgb : colorLayer.rgb / colorLayer.a;
          vec4 bgColor = getBackground(bgUV);
          vec3 cb = bgColor.a == 0.0 ? bgColor.rgb : bgColor.rgb / bgColor.a;
          return applyBlendMode(clamp(cl, vec3(0.0), vec3(1.0)), colorLayer.a * opacity, cb, bgColor.a);` : t`
          float composeAlpha = colorLayer.a * opacity;
          ${f ? t`return colorLayer * opacity;` : t`
            vec4 bgColor = getBackground(bgUV);
            return bgColor * (1.0 - composeAlpha) + colorLayer * opacity;`}`}
    }`);
}

export {
  a,
  i,
  b
};
//# sourceMappingURL=chunk-XZM7XEQ4.js.map
