import {
  s
} from "./chunk-SHCBT5YZ.js";
import {
  a,
  i3 as i
} from "./chunk-45CZB3H2.js";
import {
  F,
  e2 as e,
  p2 as p,
  u,
  y
} from "./chunk-XL3L5YCX.js";

// node_modules/@arcgis/core/geometry/support/UCharArray.js
function t(t4) {
  return Array.isArray(t4) ? t4.length < y ? t4 : new Uint8Array(t4) : t4.length < y ? Array.from(t4) : t4;
}

// node_modules/@arcgis/core/geometry/support/UShortArray.js
function t2(t4) {
  return Array.isArray(t4) ? t4.length < y ? t4 : new Uint16Array(t4) : t4.length < y ? Array.from(t4) : t4;
}

// node_modules/@arcgis/core/views/3d/layers/i3s/LEPCC.js
var t3 = true;
var o = { identifierOffset: 0, identifierLength: 10, versionOffset: 10, checksumOffset: 12, byteCount: 16 };
function r(e2, r2, n2) {
  return { identifier: String.fromCharCode.apply(null, new Uint8Array(e2, n2 + o.identifierOffset, o.identifierLength)), version: r2.getUint16(n2 + o.versionOffset, t3), checksum: r2.getUint32(n2 + o.checksumOffset, t3) };
}
var n = { sizeLo: 0, sizeHi: 4, minX: 8, minY: 16, minZ: 24, maxX: 32, maxY: 40, maxZ: 48, errorX: 56, errorY: 64, errorZ: 72, count: 80, reserved: 84, byteCount: 88 };
function i2(e2, o2) {
  return { sizeLo: e2.getUint32(o2 + n.sizeLo, t3), sizeHi: e2.getUint32(o2 + n.sizeHi, t3), minX: e2.getFloat64(o2 + n.minX, t3), minY: e2.getFloat64(o2 + n.minY, t3), minZ: e2.getFloat64(o2 + n.minZ, t3), maxX: e2.getFloat64(o2 + n.maxX, t3), maxY: e2.getFloat64(o2 + n.maxY, t3), maxZ: e2.getFloat64(o2 + n.maxZ, t3), errorX: e2.getFloat64(o2 + n.errorX, t3), errorY: e2.getFloat64(o2 + n.errorY, t3), errorZ: e2.getFloat64(o2 + n.errorZ, t3), count: e2.getUint32(o2 + n.count, t3), reserved: e2.getUint32(o2 + n.reserved, t3) };
}
function c(t4) {
  const c2 = new DataView(t4, 0);
  let d3 = 0;
  const { identifier: l2, version: a3 } = r(t4, c2, d3);
  if (d3 += o.byteCount, "LEPCC     " !== l2) throw new s("lepcc-decode-error", "Bad identifier");
  if (a3 > 1) throw new s("lepcc-decode-error", "Unknown version");
  const u3 = i2(c2, d3);
  d3 += n.byteCount;
  if (u3.sizeHi * 2 ** 32 + u3.sizeLo !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
  const f2 = new Float64Array(3 * u3.count), h3 = [], w3 = [], g2 = [], p3 = [];
  if (d3 = s2(t4, d3, h3), d3 = s2(t4, d3, w3), d3 = s2(t4, d3, g2), d3 = s2(t4, d3, p3), d3 !== t4.byteLength) throw new s("lepcc-decode-error", "Bad length");
  let m2 = 0, U2 = 0;
  for (let e2 = 0; e2 < h3.length; e2++) {
    U2 += h3[e2];
    let t5 = 0;
    for (let o2 = 0; o2 < w3[e2]; o2++) {
      t5 += g2[m2];
      const e3 = p3[m2];
      f2[3 * m2] = Math.min(u3.maxX, u3.minX + 2 * u3.errorX * t5), f2[3 * m2 + 1] = Math.min(u3.maxY, u3.minY + 2 * u3.errorY * U2), f2[3 * m2 + 2] = Math.min(u3.maxZ, u3.minZ + 2 * u3.errorZ * e3), m2++;
    }
  }
  return { errorX: u3.errorX, errorY: u3.errorY, errorZ: u3.errorZ, result: f2 };
}
function s2(e2, t4, o2) {
  const r2 = [];
  t4 = d(e2, t4, r2);
  const n2 = [];
  for (let i3 = 0; i3 < r2.length; i3++) {
    n2.length = 0, t4 = d(e2, t4, n2);
    for (let e3 = 0; e3 < n2.length; e3++) o2.push(n2[e3] + r2[i3]);
  }
  return t4;
}
function d(o2, r2, n2) {
  const i3 = new DataView(o2, r2), c2 = i3.getUint8(0), s3 = 31 & c2, d3 = !!(32 & c2), l2 = (192 & c2) >> 6;
  let a3 = 0;
  if (0 === l2) a3 = i3.getUint32(1, t3), r2 += 5;
  else if (1 === l2) a3 = i3.getUint16(1, t3), r2 += 3;
  else {
    if (2 !== l2) throw new s("lepcc-decode-error", "Bad count type");
    a3 = i3.getUint8(1), r2 += 2;
  }
  if (d3) throw new s("lepcc-decode-error", "LUT not implemented");
  const u3 = Math.ceil(a3 * s3 / 8), f2 = new Uint8Array(o2, r2, u3);
  let h3 = 0, w3 = 0, g2 = 0;
  const p3 = -1 >>> 32 - s3;
  for (let e2 = 0; e2 < a3; e2++) {
    for (; w3 < s3; ) h3 |= f2[g2] << w3, w3 += 8, g2 += 1;
    n2[e2] = h3 & p3, h3 >>>= s3, w3 -= s3, w3 + s3 > 32 && (h3 |= f2[g2 - 1] >> 8 - w3);
  }
  return r2 + g2;
}
var l = { sizeLo: 0, sizeHi: 4, count: 8, colorMapCount: 12, lookupMethod: 14, compressionMethod: 15, byteCount: 16 };
function a2(e2, o2) {
  return { sizeLo: e2.getUint32(o2 + l.sizeLo, t3), sizeHi: e2.getUint32(o2 + l.sizeHi, t3), count: e2.getUint32(o2 + l.count, t3), colorMapCount: e2.getUint16(o2 + l.colorMapCount, t3), lookupMethod: e2.getUint8(o2 + l.lookupMethod), compressionMethod: e2.getUint8(o2 + l.compressionMethod) };
}
function u2(t4) {
  const n2 = new DataView(t4, 0);
  let i3 = 0;
  const { identifier: c2, version: s3 } = r(t4, n2, i3);
  if (i3 += o.byteCount, "ClusterRGB" !== c2) throw new s("lepcc-decode-error", "Bad identifier");
  if (s3 > 1) throw new s("lepcc-decode-error", "Unknown version");
  const d3 = a2(n2, i3);
  i3 += l.byteCount;
  if (d3.sizeHi * 2 ** 32 + d3.sizeLo !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
  if ((2 === d3.lookupMethod || 1 === d3.lookupMethod) && 0 === d3.compressionMethod) {
    if (3 * d3.colorMapCount + d3.count + i3 !== t4.byteLength || d3.colorMapCount > 256) throw new s("lepcc-decode-error", "Bad count");
    const o2 = new Uint8Array(t4, i3, 3 * d3.colorMapCount), r2 = new Uint8Array(t4, i3 + 3 * d3.colorMapCount, d3.count), n3 = new Uint8Array(3 * d3.count);
    for (let e2 = 0; e2 < d3.count; e2++) {
      const t5 = r2[e2];
      n3[3 * e2] = o2[3 * t5], n3[3 * e2 + 1] = o2[3 * t5 + 1], n3[3 * e2 + 2] = o2[3 * t5 + 2];
    }
    return n3;
  }
  if (0 === d3.lookupMethod && 0 === d3.compressionMethod) {
    if (3 * d3.count + i3 !== t4.byteLength || 0 !== d3.colorMapCount) throw new s("lepcc-decode-error", "Bad count");
    return new Uint8Array(t4, i3).slice();
  }
  if (d3.lookupMethod <= 2 && 1 === d3.compressionMethod) {
    if (i3 + 3 !== t4.byteLength || 1 !== d3.colorMapCount) throw new s("lepcc-decode-error", "Bad count");
    const o2 = n2.getUint8(i3), r2 = n2.getUint8(i3 + 1), c3 = n2.getUint8(i3 + 2), s4 = new Uint8Array(3 * d3.count);
    for (let e2 = 0; e2 < d3.count; e2++) s4[3 * e2] = o2, s4[3 * e2 + 1] = r2, s4[3 * e2 + 2] = c3;
    return s4;
  }
  throw new s("lepcc-decode-error", "Bad method " + d3.lookupMethod + "," + d3.compressionMethod);
}
var f = { sizeLo: 0, sizeHi: 4, count: 8, scaleFactor: 12, bitsPerPoint: 14, reserved: 15, byteCount: 16 };
function h(e2, o2) {
  return { sizeLo: e2.getUint32(o2 + f.sizeLo, t3), sizeHi: e2.getUint32(o2 + f.sizeHi, t3), count: e2.getUint32(o2 + f.count, t3), scaleFactor: e2.getUint16(o2 + f.scaleFactor, t3), bitsPerPoint: e2.getUint8(o2 + f.bitsPerPoint), reserved: e2.getUint8(o2 + f.reserved) };
}
function w(t4) {
  const n2 = new DataView(t4, 0);
  let i3 = 0;
  const { identifier: c2, version: s3 } = r(t4, n2, i3);
  if (i3 += o.byteCount, "Intensity " !== c2) throw new s("lepcc-decode-error", "Bad identifier");
  if (s3 > 1) throw new s("lepcc-decode-error", "Unknown version");
  const l2 = h(n2, i3);
  i3 += f.byteCount;
  if (l2.sizeHi * 2 ** 32 + l2.sizeLo !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
  const a3 = new Uint16Array(l2.count);
  if (8 === l2.bitsPerPoint) {
    if (l2.count + i3 !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
    const o2 = new Uint8Array(t4, i3, l2.count);
    for (let e2 = 0; e2 < l2.count; e2++) a3[e2] = o2[e2] * l2.scaleFactor;
  } else if (16 === l2.bitsPerPoint) {
    if (2 * l2.count + i3 !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
    const o2 = new Uint16Array(t4, i3, l2.count);
    for (let e2 = 0; e2 < l2.count; e2++) a3[e2] = o2[e2] * l2.scaleFactor;
  } else {
    const o2 = [];
    if (d(t4, i3, o2) !== t4.byteLength) throw new s("lepcc-decode-error", "Bad size");
    for (let e2 = 0; e2 < l2.count; e2++) a3[e2] = o2[e2] * l2.scaleFactor;
  }
  return a3;
}

// node_modules/@arcgis/core/views/3d/layers/i3s/I3SBinaryReader.js
var d2 = () => i.getLogger("esri.views.3d.layers.i3s.I3SBinaryReader");
function b(t4, n2, r2) {
  let o2 = "", i3 = 0;
  for (; i3 < r2; ) {
    const u3 = t4[n2 + i3];
    if (u3 < 128) o2 += String.fromCharCode(u3), i3++;
    else if (u3 >= 192 && u3 < 224) {
      if (i3 + 1 >= r2) throw new s("utf8-decode-error", "UTF-8 Decode failed. Two byte character was truncated.");
      const a3 = (31 & u3) << 6 | 63 & t4[n2 + i3 + 1];
      o2 += String.fromCharCode(a3), i3 += 2;
    } else if (u3 >= 224 && u3 < 240) {
      if (i3 + 2 >= r2) throw new s("utf8-decode-error", "UTF-8 Decode failed. Multi byte character was truncated.");
      const a3 = (15 & u3) << 12 | (63 & t4[n2 + i3 + 1]) << 6 | 63 & t4[n2 + i3 + 2];
      o2 += String.fromCharCode(a3), i3 += 3;
    } else {
      if (!(u3 >= 240 && u3 < 248)) throw new s("utf8-decode-error", "UTF-8 Decode failed. Invalid multi byte sequence.");
      {
        if (i3 + 3 >= r2) throw new s("utf8-decode-error", "UTF-8 Decode failed. Multi byte character was truncated.");
        const a3 = (7 & u3) << 18 | (63 & t4[n2 + i3 + 1]) << 12 | (63 & t4[n2 + i3 + 2]) << 6 | 63 & t4[n2 + i3 + 3];
        if (a3 >= 65536) {
          const e2 = 55296 + (a3 - 65536 >> 10), t5 = 56320 + (1023 & a3);
          o2 += String.fromCharCode(e2, t5);
        } else o2 += String.fromCharCode(a3);
        i3 += 4;
      }
    }
  }
  return o2;
}
function y2(e2, t4) {
  const n2 = { byteOffset: 0, byteCount: 0, fields: /* @__PURE__ */ Object.create(null) };
  let r2 = 0;
  for (let o2 = 0; o2 < t4.length; o2++) {
    const i3 = t4[o2], u3 = i3.valueType || i3.type, a3 = j[u3];
    n2.fields[i3.property] = a3(e2, r2), r2 += x[u3].BYTES_PER_ELEMENT;
  }
  return n2.byteCount = r2, n2;
}
function g(e2, t4, n2) {
  return p2(e2, t4, n2).map((e3) => {
    const t5 = e3 ? Date.parse(e3) : null;
    return null == t5 || Number.isNaN(t5) ? null : t5;
  });
}
function p2(t4, n2, r2) {
  const o2 = [];
  let i3, u3, a3 = 0;
  for (u3 = 0; u3 < t4; u3 += 1) {
    if (i3 = n2[u3], i3 > 0) {
      if (o2.push(b(r2, a3, i3 - 1)), 0 !== r2[a3 + i3 - 1]) throw new s("string-array-error", "Invalid string array: missing null termination.");
    } else o2.push(null);
    a3 += i3;
  }
  return o2;
}
function m(e2, t4) {
  return new (0, x[t4.valueType])(e2, t4.byteOffset, t4.count * t4.valuesPerElement);
}
function w2(e2, t4) {
  const n2 = m(e2, t4);
  if (n2.length >= y) return n2;
  const r2 = new Array();
  return n2.forEach((e3, t5) => r2.push(v(n2, t5))), r2;
}
function v(e2, t4) {
  if (!e2) return null;
  const n2 = e2[t4];
  if (u(e2)) return n2 === p ? null : n2;
  if (e(e2)) return n2 === F ? null : n2;
  return n2 != n2 ? null : n2;
}
function C(e2, t4) {
  return new Uint8Array(e2, t4.byteOffset, t4.byteCount);
}
function h2(n2, r2, o2) {
  const i3 = null != r2.header ? y2(n2, r2.header) : { byteOffset: 0, byteCount: 0, fields: { count: o2 } }, u3 = { header: i3, byteOffset: i3.byteCount, byteCount: 0, entries: /* @__PURE__ */ Object.create(null) };
  let a3 = i3.byteCount;
  for (let s3 = 0; s3 < r2.ordering.length; s3++) {
    const n3 = r2.ordering[s3], o3 = a(r2[n3]);
    if (o3.count = i3.fields.count ?? 0, "String" === o3.valueType) {
      if (o3.byteOffset = a3, o3.byteCount = i3.fields[n3 + "ByteCount"], "UTF-8" !== o3.encoding) throw new s("unsupported-encoding", "Unsupported String encoding.", { encoding: o3.encoding });
      if (o3.timeEncoding && "ECMA_ISO8601" !== o3.timeEncoding) throw new s("unsupported-time-encoding", "Unsupported time encoding.", { timeEncoding: o3.timeEncoding });
    } else {
      if (!F2(o3.valueType)) throw new s("unsupported-value-type", "Unsupported binary valueType", { valueType: o3.valueType });
      {
        const e2 = B(o3.valueType);
        a3 += a3 % e2 !== 0 ? e2 - a3 % e2 : 0, o3.byteOffset = a3, o3.byteCount = e2 * o3.valuesPerElement * o3.count;
      }
    }
    a3 += o3.byteCount ?? 0, u3.entries[n3] = o3;
  }
  return u3.byteCount = a3 - u3.byteOffset, u3;
}
function I(t4, n2, r2) {
  if (n2 !== t4 && d2().error(`Invalid ${r2} buffer size
 expected: ${t4}, actual: ${n2})`), n2 < t4) throw new s("buffer-too-small", "Binary buffer is too small", { expectedSize: t4, actualSize: n2 });
}
function A(e2) {
  return { isDraco: false, isLegacy: false, color: null != e2.color, normal: null != e2.normal, uv0: null != e2.uv0, uvRegion: null != e2.uvRegion, featureIndex: null != e2.faceRange && null != e2.featureId };
}
function U(e2, t4) {
  const n2 = y2(e2, t4 && t4.header);
  let r2 = n2.byteCount;
  const o2 = { isDraco: false, header: n2, byteOffset: n2.byteCount, byteCount: 0, vertexAttributes: {} }, i3 = n2.fields, u3 = null != i3.vertexCount ? i3.vertexCount : i3.count;
  for (const c2 of t4.ordering) {
    if (!t4.vertexAttributes[c2]) continue;
    const e3 = { ...t4.vertexAttributes[c2], byteOffset: r2, count: u3 }, n3 = D[c2] || "_" + c2;
    o2.vertexAttributes[n3] = e3, r2 += B(e3.valueType) * e3.valuesPerElement * u3;
  }
  const a3 = i3.faceCount;
  if (t4.faces && a3) {
    o2.faces = {};
    for (const e3 of t4.ordering) {
      if (!t4.faces[e3]) continue;
      const n3 = { ...t4.faces[e3], byteOffset: r2, count: a3 };
      o2.faces[e3] = n3, r2 += B(n3.valueType) * n3.valuesPerElement * a3;
    }
  }
  const s3 = i3.featureCount;
  if (t4.featureAttributes && t4.featureAttributeOrder && s3) {
    o2.featureAttributes = {};
    for (const e3 of t4.featureAttributeOrder) {
      if (!t4.featureAttributes[e3]) continue;
      const n3 = { ...t4.featureAttributes[e3], byteOffset: r2, count: s3 };
      o2.featureAttributes[e3] = n3;
      r2 += ("UInt64" === n3.valueType ? 8 : B(n3.valueType)) * n3.valuesPerElement * s3;
    }
  }
  return I(r2, e2.byteLength, "geometry"), o2.byteCount = r2 - o2.byteOffset, o2;
}
function E(e2, t4) {
  return !(!e2 || !e2.compressedAttributes || "draco" !== e2.compressedAttributes.encoding) ? O(e2.compressedAttributes.attributes) : e2 ? A(e2) : T(t4);
}
function T(e2) {
  const t4 = { isDraco: false, isLegacy: true, color: false, normal: false, uv0: false, uvRegion: false, featureIndex: false };
  for (const n2 of e2.ordering) if (e2.vertexAttributes[n2]) switch (n2) {
    case "position":
      break;
    case "normal":
      t4.normal = true;
      break;
    case "color":
      t4.color = true;
      break;
    case "uv0":
      t4.uv0 = true;
      break;
    case "region":
      t4.uvRegion = true;
  }
  return e2.featureAttributes && e2.featureAttributeOrder && (t4.featureIndex = true), t4;
}
function O(e2) {
  const t4 = { isDraco: true, isLegacy: false, color: false, normal: false, uv0: false, uvRegion: false, featureIndex: false };
  for (const n2 of e2) switch (n2) {
    case "position":
      break;
    case "normal":
      t4.normal = true;
      break;
    case "uv0":
      t4.uv0 = true;
      break;
    case "color":
      t4.color = true;
      break;
    case "uv-region":
      t4.uvRegion = true;
      break;
    case "feature-index":
      t4.featureIndex = true;
  }
  return t4;
}
var D = { position: "position", normal: "normal", color: "color", uv0: "uv0", region: "uvRegion" };
function S(t4, n2, r2, o2 = false) {
  if ("lepcc-rgb" === t4.encoding) return o2 ? u2(n2) : t(u2(n2));
  if ("lepcc-intensity" === t4.encoding) return o2 ? w(n2) : t2(w(n2));
  if (null != t4.encoding && "" !== t4.encoding) throw new s("unknown-attribute-storage-info-encoding", "Unknown Attribute Storage Info Encoding");
  t4["attributeByteCounts "] && !t4.attributeByteCounts && (d2().warn("Warning: Trailing space in 'attributeByteCounts '."), t4.attributeByteCounts = t4["attributeByteCounts "]), "ObjectIds" === t4.ordering[0] && t4.hasOwnProperty("objectIds") && (d2().warn("Warning: Case error in objectIds"), t4.ordering[0] = "objectIds");
  const i3 = h2(n2, t4, r2);
  I(i3.byteOffset + i3.byteCount, n2.byteLength, "attribute");
  const u3 = i3.entries.attributeValues || i3.entries.objectIds;
  if (u3) {
    if ("String" === u3.valueType) {
      const e2 = i3.entries.attributeByteCounts, t5 = m(n2, e2), r3 = C(n2, u3);
      return u3.timeEncoding ? g(e2.count, t5, r3) : p2(e2.count, t5, r3);
    }
    return o2 ? m(n2, u3) : w2(n2, u3);
  }
  throw new s("bad-attribute-storage-info", "Bad attributeStorageInfo specification.");
}
var x = { Float32: Float32Array, Float64: Float64Array, UInt8: Uint8Array, Int8: Int8Array, UInt16: Uint16Array, Int16: Int16Array, UInt32: Uint32Array, Int32: Int32Array };
var j = { Float32: (e2, t4) => new DataView(e2, 0).getFloat32(t4, true), Float64: (e2, t4) => new DataView(e2, 0).getFloat64(t4, true), UInt8: (e2, t4) => new DataView(e2, 0).getUint8(t4), Int8: (e2, t4) => new DataView(e2, 0).getInt8(t4), UInt16: (e2, t4) => new DataView(e2, 0).getUint16(t4, true), Int16: (e2, t4) => new DataView(e2, 0).getInt16(t4, true), UInt32: (e2, t4) => new DataView(e2, 0).getUint32(t4, true), Int32: (e2, t4) => new DataView(e2, 0).getInt32(t4, true) };
function F2(e2) {
  return x.hasOwnProperty(e2);
}
function B(e2) {
  return F2(e2) ? x[e2].BYTES_PER_ELEMENT : 0;
}

export {
  c,
  m,
  v,
  U,
  E,
  S
};
//# sourceMappingURL=chunk-MHBIAIJ3.js.map
