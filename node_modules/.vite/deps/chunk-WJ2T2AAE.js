import {
  html,
  safeClassMap
} from "./chunk-467JXPUC.js";

// node_modules/@esri/calcite-components/dist/chunks/text.js
var CSS = {
  textMatch: "text-match"
};
function capitalizeWord(word) {
  return word.charAt(0).toUpperCase() + word.slice(1);
}
function highlightText({ text, pattern }) {
  if (!pattern || !text) {
    return text;
  }
  const parts = text.split(pattern);
  if (parts.length > 1) {
    parts[1] = html`<mark class=${safeClassMap(CSS.textMatch)}>${parts[1]}</mark>`;
  }
  return parts;
}

export {
  capitalizeWord,
  highlightText
};
//# sourceMappingURL=chunk-WJ2T2AAE.js.map
