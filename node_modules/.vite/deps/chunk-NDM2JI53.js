import {
  a2 as a,
  f
} from "./chunk-7W7MTAUJ.js";
import {
  u
} from "./chunk-CSMCPN64.js";

// node_modules/@arcgis/core/views/2d/engine/flow/utils.js
function i(t) {
  const n = o(r(t)), i2 = n, s2 = true, a3 = Math.max(n / 2, 5), l2 = Math.round(u(t.maxPathLength) / a3) + 1, u3 = 10, { density: c2 } = t;
  return { smoothing: u(t.smoothing), interpolate: true, velocityScale: "flow-from" === t.flowRepresentation ? 1 : -1, verticesPerLine: l2, minSpeedThreshold: 1e-3, segmentLength: a3, maxTurnAngle: 1, collisions: s2, lineCollisionWidth: i2, lineSpacing: u3, density: c2, onlyForwardTracing: true, wrapAround: false };
}
function o(e) {
  return "constant" === e.kind ? e.value[0] : e.values[e.values.length - 1];
}
function s(e) {
  const t = e.toRgba();
  return [t[0] / 255, t[1] / 255, t[2] / 255, t[3]];
}
function a2(e) {
  return { kind: "constant", value: [0.1, 0.1, 0.1, 1] };
}
function r(t) {
  if (!t.hasVisualVariables("size")) return { kind: "constant", value: [u(t.trailWidth)] };
  const n = t.getVisualVariablesForType("size")[0], i2 = [], o2 = [];
  let s2;
  if (n.stops) {
    for (const t2 of n.stops) i2.push(t2.value), o2.push(u(t2.size));
    s2 = n.stops.length;
  } else i2.push(n.minDataValue, n.maxDataValue), o2.push(u(n.minSize), u(n.maxSize)), s2 = 2;
  return { kind: "ramp", stops: i2, values: o2, count: s2 };
}
function l(e) {
  if (!e.hasVisualVariables("color")) return { kind: "constant", value: s(e.color) };
  const t = e.getVisualVariablesForType("color")[0], n = [], i2 = [];
  for (const o2 of t.stops) n.push(o2.value), Array.prototype.push.apply(i2, s(o2.color));
  return { kind: "ramp", stops: n, values: i2, count: t.stops.length };
}
function u2(e) {
  if (!e.hasVisualVariables("opacity")) return { kind: "constant", value: [1] };
  const t = e.getVisualVariablesForType("opacity")[0], n = [], i2 = [];
  for (const o2 of t.stops) n.push(o2.value), i2.push(o2.opacity);
  return { kind: "ramp", stops: n, values: i2, count: t.stops.length };
}
function c(e, t, n, i2) {
  switch (t) {
    case "int":
      e.setUniform1iv(n, i2);
      break;
    case "float":
      e.setUniform1fv(n, i2);
      break;
    case "vec2":
      e.setUniform2fv(n, i2);
      break;
    case "vec3":
      e.setUniform3fv(n, i2);
      break;
    case "vec4":
      e.setUniform4fv(n, i2);
  }
}
function m(e, t, n, i2) {
  "constant" === i2.kind ? c(e, n, `u_${t}`, i2.value) : (c(e, "float", `u_${t}_stops`, i2.stops), c(e, n, `u_${t}_values`, i2.values), e.setUniform1i(`u_${t}_count`, i2.count));
}
function p(e, t) {
  let n = true;
  return n = n && e.collisions === t.collisions, n = n && e.density === t.density, n = n && e.interpolate === t.interpolate, n = n && e.lineCollisionWidth === t.lineCollisionWidth, n = n && e.lineSpacing === t.lineSpacing, n = n && e.maxTurnAngle === t.maxTurnAngle, n = n && e.minSpeedThreshold === t.minSpeedThreshold, n = n && e.segmentLength === t.segmentLength, n = n && e.smoothing === t.smoothing, n = n && e.velocityScale === t.velocityScale, n = n && e.verticesPerLine === t.verticesPerLine, n;
}
function f2(e, t) {
  return e === t || null != e && null != t && e.equals(t);
}
function h(e, t) {
  if (!p(e.simulationSettings, t.simulationSettings)) return false;
  if (!f2(e.timeExtent, t.timeExtent)) return false;
  let n = true;
  return n = n && e.loadImagery === t.loadImagery, n = n && e.createFlowMesh === t.createFlowMesh, n = n && e.color.kind === t.color.kind, n = n && e.opacity.kind === t.opacity.kind, n = n && e.size.kind === t.size.kind, n;
}
function d(e, t, n, i2) {
  return e /= n.size[0], t = 1 - (t /= n.size[1]), e *= n.extent.xmax - n.extent.xmin, t *= n.extent.ymax - n.extent.ymin, e += n.extent.xmin, t += n.extent.ymin, e -= i2.extent.xmin, t -= i2.extent.ymin, e /= i2.extent.xmax - i2.extent.xmin, t = 1 - (t /= i2.extent.ymax - i2.extent.ymin), { x: e *= i2.size[0], y: t *= i2.size[1] };
}
function x(e, i2, o2, s2) {
  const a3 = [], r2 = o2.time - i2.time;
  if (0 === e.length) return [];
  const l2 = a(e), u3 = l2 ? f(e[0].hasMagnitude) : 1, c2 = l2 ? ({ vertices: e2 }, t) => {
    const n = e2, i3 = t * u3;
    return { x: n[i3], y: n[i3 + 1], t: n[i3 + 2], speed: 0 };
  } : ({ vertices: e2 }, t) => e2[t];
  for (const t of e) {
    const { stage: e2, vertices: n } = t;
    if (2 === e2) continue;
    const l3 = n.length / u3, m2 = c2(t, 0), p2 = m2.t, f3 = (r2 * s2 - p2) % (c2(t, l3 - 1).t - p2) + p2;
    let h2, x2 = m2.x, g = m2.y, v = m2.t;
    for (h2 = 1; h2 < l3 && v < f3; h2++) {
      const e3 = c2(t, h2), n2 = Math.min(e3.t, f3) - v, i3 = e3.t - v;
      x2 += (e3.x - x2) * (n2 / i3), g += (e3.y - g) * (n2 / i3), v += n2;
    }
    a3.push(d(x2, g, i2, o2));
  }
  return a3;
}

export {
  i,
  o,
  a2 as a,
  r,
  l,
  u2 as u,
  m,
  p,
  f2 as f,
  h,
  x
};
//# sourceMappingURL=chunk-NDM2JI53.js.map
