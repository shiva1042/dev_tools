import {
  t
} from "./chunk-CFXCM2NO.js";
import {
  n
} from "./chunk-XL3L5YCX.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/attributes/NormalAttribute.glsl.js
function o(o2, a) {
  switch (o2.fragment.code.add(t`vec3 screenDerivativeNormal(vec3 positionView) {
return normalize(cross(dFdx(positionView), dFdy(positionView)));
}`), a.normalType) {
    case 1:
      o2.attributes.add("normalCompressed", "vec2"), o2.vertex.code.add(t`vec3 decompressNormal(vec2 normal) {
float z = 1.0 - abs(normal.x) - abs(normal.y);
return vec3(normal + sign(normal) * min(z, 0.0), z);
}
vec3 normalModel() {
return decompressNormal(normalCompressed);
}`);
      break;
    case 0:
      o2.attributes.add("normal", "vec3"), o2.vertex.code.add(t`vec3 normalModel() {
return normal;
}`);
      break;
    default:
      n(a.normalType);
    case 2:
    case 3:
  }
}

export {
  o
};
//# sourceMappingURL=chunk-5TEIXJZ3.js.map
