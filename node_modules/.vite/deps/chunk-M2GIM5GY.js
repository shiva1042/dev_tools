import {
  i
} from "./chunk-22FXYGNS.js";
import {
  w
} from "./chunk-LMIJQUS2.js";
import {
  I as I2,
  L,
  N,
  O,
  P,
  U,
  tt,
  z
} from "./chunk-TGAE5BHG.js";
import {
  k
} from "./chunk-CC5SADKO.js";
import {
  I,
  g,
  u,
  v
} from "./chunk-5ZRDULGW.js";
import {
  n,
  r as r2
} from "./chunk-SWU3RIKJ.js";
import {
  e
} from "./chunk-X4EBC2WG.js";
import {
  r
} from "./chunk-MGGSVGV4.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/lib/Octree.js
var S = class _S {
  get bounds() {
    return this._root.bounds;
  }
  get halfSize() {
    return this._root.halfSize;
  }
  get root() {
    return this._root.node;
  }
  get maximumObjectsPerNode() {
    return this._maximumObjectsPerNode;
  }
  get maximumDepth() {
    return this._maximumDepth;
  }
  get objectCount() {
    return this._objectCount;
  }
  constructor(t, e2) {
    this.objectToBoundingSphere = t, this._maximumObjectsPerNode = 10, this._maximumDepth = 20, this._degenerateObjects = /* @__PURE__ */ new Set(), this._root = new j(), this._objectCount = 0, e2 && (void 0 !== e2.maximumObjectsPerNode && (this._maximumObjectsPerNode = e2.maximumObjectsPerNode), void 0 !== e2.maximumDepth && (this._maximumDepth = e2.maximumDepth));
  }
  destroy() {
    this._degenerateObjects.clear(), j.clearPool(), C[0] = null, V.prune(), Y.prune();
  }
  add(t) {
    const e2 = Array.from(t);
    this._grow(e2);
    const n2 = j.acquire();
    for (const o of e2) ++this._objectCount, this._isDegenerate(o) ? this._degenerateObjects.add(o) : (n2.init(this._root), this._add(o, n2));
    j.release(n2);
  }
  remove(t, e2 = null) {
    this._objectCount -= t.length;
    const n2 = j.acquire();
    for (const o of t) {
      const t2 = e2 ?? O(this.objectToBoundingSphere(o), G);
      F(t2[3]) ? (n2.init(this._root), z2(o, t2, n2)) : this._degenerateObjects.delete(o);
    }
    j.release(n2), this._shrink();
  }
  update(t, e2) {
    if (!F(e2[3]) && this._isDegenerate(t)) return;
    const n2 = I3(t);
    this.remove(n2, e2), this.add(n2);
  }
  forEachAlongRay(t, e2, n2) {
    const o = k(t, e2);
    x(this._root, (t2) => {
      if (!N2(o, t2)) return false;
      const e3 = t2.node;
      return e3.terminals.forAll((t3) => {
        this._intersectsObject(o, t3) && n2(t3);
      }), null !== e3.residents && e3.residents.forAll((t3) => {
        this._intersectsObject(o, t3) && n2(t3);
      }), true;
    });
  }
  forEachAlongRayWithVerticalOffset(t, e2, n2, o) {
    const i2 = k(t, e2);
    x(this._root, (t2) => {
      if (!T(i2, t2, o)) return false;
      const e3 = t2.node;
      return e3.terminals.forAll((t3) => {
        this._intersectsObjectWithOffset(i2, t3, o) && n2(t3);
      }), null !== e3.residents && e3.residents.forAll((t3) => {
        this._intersectsObjectWithOffset(i2, t3, o) && n2(t3);
      }), true;
    });
  }
  forEach(t) {
    x(this._root, (e2) => {
      const n2 = e2.node;
      return n2.terminals.forAll(t), null !== n2.residents && n2.residents.forAll(t), true;
    }), this._degenerateObjects.forEach(t);
  }
  forEachDegenerateObject(t) {
    this._degenerateObjects.forEach(t);
  }
  findClosest(t, e2, n2, o = () => true, r3 = 1 / 0) {
    let h = 1 / 0, l = 1 / 0, d = null;
    const u2 = P2(t, e2), c = (i2) => {
      if (--r3, !o(i2)) return;
      const s = this.objectToBoundingSphere(i2);
      if (!w(n2, s)) return;
      const u3 = R(t, e2, I2(s)), c2 = u3 - s[3], f = u3 + s[3];
      c2 < h && (h = c2, l = f, d = i2);
    };
    return O2(this._root, (o2) => {
      if (r3 <= 0 || !w(n2, o2.bounds)) return false;
      g(H, u2, o2.halfSize), u(H, H, I2(o2.bounds));
      if (R(t, e2, H) > l) return false;
      const h2 = o2.node;
      return h2.terminals.forAll((t2) => c(t2)), null !== h2.residents && h2.residents.forAll((t2) => c(t2)), true;
    }, t, e2), d;
  }
  forEachInDepthRange(t, e2, n2, o, r3, h, l) {
    let d = -1 / 0, u2 = 1 / 0;
    const c = { setRange: (t2) => {
      1 === n2 ? (d = Math.max(d, t2.near), u2 = Math.min(u2, t2.far)) : (d = Math.max(d, -t2.far), u2 = Math.min(u2, -t2.near));
    } };
    c.setRange(o);
    const f = R(e2, n2, t), m = P2(e2, n2), _ = P2(e2, -n2), p = (t2) => {
      if (!l(t2)) return;
      const o2 = this.objectToBoundingSphere(t2), i2 = R(e2, n2, I2(o2)) - f, s = i2 - o2[3], m2 = i2 + o2[3];
      s > u2 || m2 < d || !w(h, o2) || r3(t2, c);
    };
    O2(this._root, (t2) => {
      if (!w(h, t2.bounds)) return false;
      g(H, m, t2.halfSize), u(H, H, I2(t2.bounds));
      if (R(e2, n2, H) - f > u2) return false;
      g(H, _, t2.halfSize), u(H, H, I2(t2.bounds));
      if (R(e2, n2, H) - f < d) return false;
      const o2 = t2.node;
      return o2.terminals.forAll((t3) => p(t3)), null !== o2.residents && o2.residents.forAll((t3) => p(t3)), true;
    }, e2, n2);
  }
  forEachNode(t) {
    x(this._root, (e2) => t(e2.node, e2.bounds, e2.halfSize, e2.depth));
  }
  forEachNeighbor(t, e2) {
    const n2 = L(e2), i2 = z(e2, n()), s = (e3) => {
      const s2 = this.objectToBoundingSphere(e3), r3 = L(s2), h2 = n2 + r3;
      return !(v(I2(s2), i2) - h2 * h2 <= 0) || t(e3);
    };
    let h = true;
    const a = (t2) => {
      h && (h = s(t2));
    };
    x(this._root, (t2) => {
      const e3 = L(t2.bounds), s2 = n2 + e3;
      if (v(I2(t2.bounds), i2) - s2 * s2 > 0) return false;
      const r3 = t2.node;
      return r3.terminals.forAll(a), h && null !== r3.residents && r3.residents.forAll(a), h;
    }), h && this.forEachDegenerateObject(a);
  }
  _intersectsObject(t, e2) {
    const n2 = this.objectToBoundingSphere(e2);
    return !(n2[3] > 0) || tt(n2, t);
  }
  _intersectsObjectWithOffset(t, e2, n2) {
    const o = this.objectToBoundingSphere(e2);
    return !(o[3] > 0) || tt(n2.applyToBoundingSphere(o), t);
  }
  _add(t, e2) {
    e2.advanceTo(this.objectToBoundingSphere(t)) ? e2.node.terminals.push(t) : (e2.node.residents.push(t), e2.node.residents.length > this._maximumObjectsPerNode && e2.depth < this._maximumDepth && this._split(e2));
  }
  _split(t) {
    const e2 = t.node.residents;
    t.node.residents = null;
    for (let n2 = 0; n2 < e2.length; n2++) {
      const o = j.acquire().init(t);
      this._add(e2.at(n2), o), j.release(o);
    }
  }
  _grow(t) {
    if (w2(t, (t2) => this.objectToBoundingSphere(t2), U2), F(U2[3]) && !this._fitsInsideTree(U2)) if (D(this._root.node)) O(U2, this._root.bounds), this._root.halfSize = 1.25 * this._root.bounds[3], this._root.updateBoundsRadiusFromHalfSize();
    else {
      const t2 = this._rootBoundsForRootAsSubNode(U2);
      this._placingRootViolatesMaxDepth(t2) ? this._rebuildTree(U2, t2) : this._growRootAsSubNode(t2), j.release(t2);
    }
  }
  _rebuildTree(t, e2) {
    U(K, e2.bounds), K[3] = e2.halfSize, w2([t, K], (t2) => t2, Q);
    const n2 = j.acquire().init(this._root);
    this._root.initFrom(null, Q, Q[3]), this._root.increaseHalfSize(1.25), x(n2, (t2) => (this.add(t2.node.terminals.data), null !== t2.node.residents && this.add(t2.node.residents.data), true)), j.release(n2);
  }
  _placingRootViolatesMaxDepth(t) {
    const e2 = Math.log(t.halfSize / this._root.halfSize) * Math.LOG2E;
    let n2 = 0;
    return x(this._root, (t2) => (n2 = Math.max(n2, t2.depth), n2 + e2 <= this._maximumDepth)), n2 + e2 > this._maximumDepth;
  }
  _rootBoundsForRootAsSubNode(t) {
    const e2 = t[3], n2 = t;
    let o = -1 / 0;
    const i2 = this._root.bounds, s = this._root.halfSize;
    for (let h = 0; h < 3; h++) {
      const t2 = i2[h] - s - (n2[h] - e2), r4 = n2[h] + e2 - (i2[h] + s), a = Math.max(0, Math.ceil(t2 / (2 * s))), l = Math.max(0, Math.ceil(r4 / (2 * s))) + 1, d = 2 ** Math.ceil(Math.log(a + l) * Math.LOG2E);
      o = Math.max(o, d), X[h].min = a, X[h].max = l;
    }
    for (let h = 0; h < 3; h++) {
      let t2 = X[h].min, e3 = X[h].max;
      const n3 = (o - (t2 + e3)) / 2;
      t2 += Math.ceil(n3), e3 += Math.floor(n3);
      const r4 = i2[h] - s - t2 * s * 2;
      L2[h] = r4 + (e3 + t2) * s;
    }
    const r3 = o * s;
    return L2[3] = r3 * k2, j.acquire().initFrom(null, L2, r3, 0);
  }
  _growRootAsSubNode(t) {
    const e2 = this._root.node;
    U(U2, this._root.bounds), U2[3] = this._root.halfSize, this._root.init(t), t.advanceTo(U2, null, true), t.node.children = e2.children, t.node.residents = e2.residents, t.node.terminals = e2.terminals;
  }
  _shrink() {
    for (; ; ) {
      const t = this._findShrinkIndex();
      if (-1 === t) break;
      this._root.advance(t), this._root.depth = 0;
    }
  }
  _findShrinkIndex() {
    if (0 !== this._root.node.terminals.length || this._root.isLeaf()) return -1;
    let t = null;
    const e2 = this._root.node.children;
    let n2 = 0, o = 0;
    for (; o < e2.length && null == t; ) n2 = o++, t = e2[n2];
    for (; o < e2.length; ) if (e2[o++]) return -1;
    return n2;
  }
  _isDegenerate(t) {
    return !F(this.objectToBoundingSphere(t)[3]);
  }
  _fitsInsideTree(t) {
    const e2 = this._root.bounds, n2 = this._root.halfSize;
    return t[3] <= n2 && t[0] >= e2[0] - n2 && t[0] <= e2[0] + n2 && t[1] >= e2[1] - n2 && t[1] <= e2[1] + n2 && t[2] >= e2[2] - n2 && t[2] <= e2[2] + n2;
  }
  toJSON() {
    const { maximumDepth: t, maximumObjectsPerNode: e2, _objectCount: n2 } = this, o = this._nodeToJSON(this._root.node);
    return { maximumDepth: t, maximumObjectsPerNode: e2, objectCount: n2, root: { bounds: this._root.bounds, halfSize: this._root.halfSize, depth: this._root.depth, node: o } };
  }
  _nodeToJSON(t) {
    const e2 = t.children.map((t2) => t2 ? this._nodeToJSON(t2) : null), n2 = t.residents?.map((t2) => this.objectToBoundingSphere(t2)), o = t.terminals?.map((t2) => this.objectToBoundingSphere(t2));
    return { children: e2, residents: n2, terminals: o };
  }
  static fromJSON(t) {
    const e2 = new _S((t2) => t2, { maximumDepth: t.maximumDepth, maximumObjectsPerNode: t.maximumObjectsPerNode });
    return e2._objectCount = t.objectCount, e2._root.initFrom(t.root.node, t.root.bounds, t.root.halfSize, t.root.depth), e2;
  }
};
var _j = class _j {
  constructor() {
    this.bounds = P(), this.halfSize = 0, this.initFrom(null, null, 0, 0);
  }
  init(t) {
    return this.initFrom(t.node, t.bounds, t.halfSize, t.depth);
  }
  initFrom(t, e2, n2, o = this.depth) {
    return this.node = null != t ? t : _j.createEmptyNode(), e2 && O(e2, this.bounds), this.halfSize = n2, this.depth = o, this;
  }
  increaseHalfSize(t) {
    this.halfSize *= t, this.updateBoundsRadiusFromHalfSize();
  }
  updateBoundsRadiusFromHalfSize() {
    this.bounds[3] = this.halfSize * k2;
  }
  advance(t) {
    let e2 = this.node.children[t];
    e2 || (e2 = _j.createEmptyNode(), this.node.children[t] = e2), this.node = e2, this.halfSize /= 2, this.depth++;
    const n2 = y[t];
    return this.bounds[0] += n2[0] * this.halfSize, this.bounds[1] += n2[1] * this.halfSize, this.bounds[2] += n2[2] * this.halfSize, this.updateBoundsRadiusFromHalfSize(), this;
  }
  advanceTo(t, e2, n2 = false) {
    for (; ; ) {
      if (this.isTerminalFor(t)) return e2?.(this, -1), true;
      if (this.isLeaf()) {
        if (!n2) return e2?.(this, -1), false;
        this.node.residents = null;
      }
      const o = this._childIndex(t);
      e2?.(this, o), this.advance(o);
    }
  }
  isLeaf() {
    return null != this.node.residents;
  }
  isTerminalFor(t) {
    return t[3] > this.halfSize / 2;
  }
  _childIndex(t) {
    const e2 = this.bounds;
    return (e2[0] < t[0] ? 1 : 0) + (e2[1] < t[1] ? 2 : 0) + (e2[2] < t[2] ? 4 : 0);
  }
  static createEmptyNode() {
    return { children: [null, null, null, null, null, null, null, null], terminals: new r({ shrink: true }), residents: new r({ shrink: true }) };
  }
  static acquire() {
    return _j._pool.acquire();
  }
  static release(t) {
    _j._pool.release(t);
  }
  static clearPool() {
    _j._pool.prune();
  }
};
_j._pool = new e(() => new _j());
var j = _j;
function x(t, e2) {
  let n2 = j.acquire().init(t);
  const o = [n2];
  for (; 0 !== o.length; ) {
    if (n2 = o.pop(), e2(n2) && !n2.isLeaf()) for (let t2 = 0; t2 < n2.node.children.length; t2++) {
      n2.node.children[t2] && o.push(j.acquire().init(n2).advance(t2));
    }
    j.release(n2);
  }
}
function O2(t, e2, n2, o = 1) {
  let i2 = j.acquire().init(t);
  const s = [i2];
  for (E(n2, o, Z); 0 !== s.length; ) {
    if (i2 = s.pop(), e2(i2) && !i2.isLeaf()) for (let t2 = 7; t2 >= 0; --t2) {
      const e3 = Z[t2];
      i2.node.children[e3] && s.push(j.acquire().init(i2).advance(e3));
    }
    j.release(i2);
  }
}
function z2(t, e2, n2) {
  V.clear();
  const o = n2.advanceTo(e2, (t2, e3) => {
    V.push(t2.node), V.push(e3);
  }) ? n2.node.terminals : n2.node.residents;
  if (o.removeUnordered(t), 0 === o.length) for (let i2 = V.length - 2; i2 >= 0; i2 -= 2) {
    if (!M(V.data[i2], V.data[i2 + 1])) break;
  }
}
function M(t, n2) {
  return n2 >= 0 && (t.children[n2] = null), !!D(t) && (null === t.residents && (t.residents = new r({ shrink: true })), true);
}
function N2(t, e2) {
  return v2(I2(e2.bounds), 2 * -e2.halfSize, J), v2(I2(e2.bounds), 2 * e2.halfSize, W), i(t.origin, t.direction, J, W);
}
function T(t, e2, n2) {
  return v2(I2(e2.bounds), 2 * -e2.halfSize, J), v2(I2(e2.bounds), 2 * e2.halfSize, W), n2.applyToMinMax(J, W), i(t.origin, t.direction, J, W);
}
function D(t) {
  if (0 !== t.terminals.length) return false;
  if (null !== t.residents) return 0 === t.residents.length;
  for (let e2 = 0; e2 < t.children.length; e2++) if (t.children[e2]) return false;
  return true;
}
function A(t, e2) {
  t[0] = Math.min(t[0], e2[0] - e2[3]), t[1] = Math.min(t[1], e2[1] - e2[3]), t[2] = Math.min(t[2], e2[2] - e2[3]);
}
function B(t, e2) {
  t[0] = Math.max(t[0], e2[0] + e2[3]), t[1] = Math.max(t[1], e2[1] + e2[3]), t[2] = Math.max(t[2], e2[2] + e2[3]);
}
function v2(t, e2, n2) {
  n2[0] = t[0] + e2, n2[1] = t[1] + e2, n2[2] = t[2] + e2;
}
function w2(t, e2, o) {
  J[0] = 1 / 0, J[1] = 1 / 0, J[2] = 1 / 0, W[0] = -1 / 0, W[1] = -1 / 0, W[2] = -1 / 0;
  for (const n2 of t) {
    const t2 = e2(n2);
    F(t2[3]) && (A(J, t2), B(W, t2));
  }
  N(o, I($, J, W, 0.5)), o[3] = Math.max(W[0] - J[0], W[1] - J[1], W[2] - J[2]) / 2;
}
function E(t, e2, n2) {
  if (!Y.length) for (let o = 0; o < 8; ++o) Y.push({ index: 0, distance: 0 });
  for (let o = 0; o < 8; ++o) {
    const n3 = y[o];
    Y.data[o].index = o, Y.data[o].distance = R(t, e2, n3);
  }
  Y.sort((t2, e3) => t2.distance - e3.distance);
  for (let o = 0; o < 8; ++o) n2[o] = Y.data[o].index;
}
function P2(t, e2) {
  let n2, o = 1 / 0;
  for (let i2 = 0; i2 < 8; ++i2) {
    const s = R(t, e2, q[i2]);
    s < o && (o = s, n2 = q[i2]);
  }
  return n2;
}
function R(t, e2, n2) {
  return e2 * (t[0] * n2[0] + t[1] * n2[1] + t[2] * n2[2]);
}
function F(t) {
  return !isNaN(t) && t !== -1 / 0 && t !== 1 / 0 && t > 0;
}
var y = [r2(-1, -1, -1), r2(1, -1, -1), r2(-1, 1, -1), r2(1, 1, -1), r2(-1, -1, 1), r2(1, -1, 1), r2(-1, 1, 1), r2(1, 1, 1)];
var q = [r2(-1, -1, -1), r2(-1, -1, 1), r2(-1, 1, -1), r2(-1, 1, 1), r2(1, -1, -1), r2(1, -1, 1), r2(1, 1, -1), r2(1, 1, 1)];
var k2 = Math.sqrt(3);
var C = [null];
function I3(t) {
  return C[0] = t, C;
}
var L2 = P();
var H = n();
var J = n();
var W = n();
var V = new r();
var G = P();
var U2 = P();
var K = P();
var Q = P();
var X = [{ min: 0, max: 0 }, { min: 0, max: 0 }, { min: 0, max: 0 }];
var Y = new r();
var Z = [0, 0, 0, 0, 0, 0, 0, 0];
var $ = n();

export {
  S
};
//# sourceMappingURL=chunk-M2GIM5GY.js.map
