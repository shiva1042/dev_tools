import {
  f as f4
} from "./chunk-EPNUDFKP.js";
import {
  p
} from "./chunk-DVSCYIDY.js";
import {
  W
} from "./chunk-2Z6FEJSU.js";
import {
  e as e2
} from "./chunk-G5PVC77C.js";
import {
  V,
  g,
  w as w3
} from "./chunk-HZOL4XN3.js";
import {
  c as c3,
  e
} from "./chunk-L7CWFLJS.js";
import {
  w as w2
} from "./chunk-FABN3375.js";
import {
  n as n4
} from "./chunk-LYFRDNEJ.js";
import {
  c as c4
} from "./chunk-WJTC22OR.js";
import {
  t
} from "./chunk-K77JVS4S.js";
import {
  n as n3
} from "./chunk-H4FTHFNN.js";
import {
  c as c2
} from "./chunk-4TML7VDN.js";
import {
  f as f3
} from "./chunk-4YYACKPD.js";
import {
  B,
  c
} from "./chunk-4NT74R5D.js";
import {
  n
} from "./chunk-SA2COZ6L.js";
import {
  n as n2
} from "./chunk-IQHITGCI.js";
import {
  f as f2
} from "./chunk-CSMCPN64.js";
import {
  f,
  l as l2,
  w
} from "./chunk-6UXW2GWP.js";
import {
  l
} from "./chunk-WYOGFT35.js";
import {
  m as m2
} from "./chunk-4ZW3SSJV.js";
import {
  _
} from "./chunk-V2SN4FBW.js";
import {
  b,
  m2 as m
} from "./chunk-NKMQAYFX.js";
import {
  a as a2
} from "./chunk-27GS23W4.js";
import {
  __decorate
} from "./chunk-SS5CLIUY.js";
import {
  u,
  y
} from "./chunk-JMK7P26I.js";
import {
  a,
  i3 as i
} from "./chunk-45CZB3H2.js";

// node_modules/@arcgis/core/widgets/support/MeasurementWidgetContent.js
var a3 = "esri-measurement-widget-content";
var m3 = { base: a3, actions: `${a3}__actions`, error: `${a3}__error`, hint: `${a3}__hint`, hintText: `${a3}__hint-text`, settings: `${a3}__settings`, measurement: `${a3}__measurement`, measurementItem: `${a3}__measurement-item`, measurementItemDisabled: `${a3}__measurement-item--disabled`, measurementItemTitle: `${a3}__measurement-item__title`, measurementItemValue: `${a3}__measurement-item__value` };
var u2 = class extends B {
  constructor(e4, t2) {
    super(e4, t2);
  }
  loadDependencies() {
    return c({ button: () => import("./calcite-button-N7RA2N6K.js") });
  }
  render() {
    return n("div", { class: m3.base }, this._renderContent());
  }
  _renderContent() {
    const { supported: e4, active: t2, state: s2 } = this;
    switch (s2) {
      case "disabled":
      case "ready":
        return e4 ? t2 ? this._renderHint() : this._renderActions() : this._renderUnsupportedMessage();
      case "measuring":
      case "measured":
        return [this._renderSettings(), this._renderMeasurement(), this._renderActions()];
    }
  }
  _renderUnsupportedMessage() {
    return n("section", { class: m3.error, key: "unsupported" }, n("p", null, this.messages.unsupported));
  }
  _renderHint() {
    const { messages: e4 } = this;
    return n("section", { class: m3.hint, key: "hint" }, n("p", { class: m3.hintText }, e4.hint));
  }
  _renderSettings() {
    return n("div", { class: m3.settings, key: "settings" }, n(n4, { options: this.unitOptions, selectLabel: this.messages.unit, value: this.unit, onChange: this.onUnitChange }));
  }
  _renderMeasurement() {
    return n("section", { class: m3.measurement, key: "measurement" }, this.measurementItems.map((e4) => this._renderMeasurementItem(e4)));
  }
  _renderMeasurementItem({ key: e4, title: t2, value: s2 }) {
    return n("div", { class: this.classes(m3.measurementItem, null == s2 && m3.measurementItemDisabled), key: e4 }, n("span", { class: m3.measurementItemTitle }, t2), n("span", { "aria-live": "polite", class: m3.measurementItemValue }, s2 ?? this.messages.notApplicable));
  }
  _renderActions() {
    const { messages: e4 } = this;
    return n("div", { class: m3.actions }, n("calcite-button", { class: this.newMeasurementButtonClass, disabled: "disabled" === this.state, onclick: this.onNewMeasurementClick }, e4.newMeasurement));
  }
};
__decorate([m()], u2.prototype, "active", void 0), __decorate([m()], u2.prototype, "measurementItems", void 0), __decorate([m()], u2.prototype, "messages", void 0), __decorate([m()], u2.prototype, "newMeasurementButtonClass", void 0), __decorate([m()], u2.prototype, "onNewMeasurementClick", void 0), __decorate([m()], u2.prototype, "onUnitChange", void 0), __decorate([m()], u2.prototype, "state", void 0), __decorate([m()], u2.prototype, "supported", void 0), __decorate([m()], u2.prototype, "unit", void 0), __decorate([m()], u2.prototype, "unitOptions", void 0), u2 = __decorate([a2("esri.widgets.support.MeasurementWidgetContent")], u2);

// node_modules/@arcgis/core/widgets/support/InteractiveToolViewModel.js
var p2 = class extends b {
  constructor(t2) {
    super(t2), this.tool = null, this._loggedUnsupportedErrorOnce = false, null != t2?.visible && (this.visible = t2.visible);
  }
  initialize() {
    this.addHandles(l2(() => ({ ready: null != this.view && this.view.ready, supported: this.supported }), ({ ready: t2, supported: e4 }) => {
      !t2 || e4 || this._loggedUnsupportedErrorOnce || (this.logError(this.unsupportedErrorMessage), this._loggedUnsupportedErrorOnce = true);
    }, w));
  }
  destroy() {
    this.removeTool(), this.view = null;
  }
  get active() {
    return null != this.tool && this.tool.active;
  }
  get disabled() {
    return null == this.view || !this.view.ready || !this.supported;
  }
  get supported() {
    return null == this.view || this.view.type === this.supportedViewType;
  }
  get view() {
    return this._get("view");
  }
  set view(t2) {
    if (t2 === this.view) return;
    this.removeTool(), this._set("view", t2);
    const e4 = "tools";
    this.removeHandles(e4), null != t2 && this.addHandles(t2.tools.on("after-remove", (t3) => {
      t3.item === this.tool && this._set("tool", null);
    }), e4);
  }
  set visible(t2) {
    this._set("visible", t2), null != this.tool && (this.tool.visible = t2);
  }
  createTool(t2 = { interactive: false }) {
    if (this.removeTool(), null == this.view || !this.view.ready || !this.supported) return;
    const e4 = this.constructTool();
    e4.created ? (this._set("tool", e4), this.view.tools.add(e4)) : t2.interactive ? (this._set("tool", e4), this.view.addAndActivateTool(e4), f(() => e4.created, () => {
      e4.active && null != this.view && (this.view.activeTool = null);
    }, { initial: true, once: true })) : e4.destroy();
  }
  removeTool() {
    const t2 = this.tool;
    if (null == t2) return;
    const e4 = this.view?.tools;
    null != e4 ? e4.remove(t2) : t2.destroy(), this._set("tool", null);
  }
  logError(...t2) {
    i.getLogger(this).error(...t2);
  }
};
__decorate([m({ constructOnly: true })], p2.prototype, "tool", void 0), __decorate([m()], p2.prototype, "active", null), __decorate([m()], p2.prototype, "disabled", null), __decorate([m()], p2.prototype, "supported", null), __decorate([m({ value: null })], p2.prototype, "view", null), __decorate([m({ type: Boolean, value: true })], p2.prototype, "visible", null), p2 = __decorate([a2("esri.widgets.support.InteractiveToolViewModel")], p2);

// node_modules/@arcgis/core/views/draw/DrawAction.js
var v;
var _a;
var f5 = (_a = class extends m2(l) {
  constructor(e4) {
    super(e4), this._hasZ = null, this._cursorScreenPoint = null, this._activePointerId = null, this._stagedVertexUnsnapped = null, this._lastVertexUnsnapped = null, this.interactiveUndoDisabled = false, this.history = [], this.redoHistory = [], this.snapToScene = false, this.view = null, this.elevationInfo = null, this.defaultZ = 0, this._coordinateHelper = w2(!!e4.hasZ, false, e4.view.spatialReference), this._snappingManager = e4.snappingManager, this._editGeometryOperations = new g(new w3(e4.editGeometryType ?? "polygon", this._coordinateHelper), 2), this._snappingGraphicsLayer = new n2({ id: v.SNAPPING_GRAPHICS_LAYER_ID, listMode: "hide", internal: true, title: "Snapping graphics" }), this._snappingContext = new e2({ editGeometryOperations: this._editGeometryOperations, elevationInfo: { mode: "on-the-ground", offset: 0 }, pointer: "mouse", visualizer: new f4(this._snappingGraphicsLayer) }), this._activeComponent = new V(e4.view.spatialReference, 2), this._editGeometryOperations.data.parts.push(this._activeComponent);
  }
  normalizeCtorArgs(e4) {
    const t2 = { ...e4 };
    return delete t2.editGeometryType, delete t2.snappingManager, t2;
  }
  initialize() {
    this._snappingOperation = new p({ view: this.view }), "2d" === this.view.type && this.view.map.layers.add(this._snappingGraphicsLayer);
  }
  destroy() {
    this.view.map.layers.remove(this._snappingGraphicsLayer), this._snappingGraphicsLayer.destroy(), null != this._snappingManager && this._snappingManager.doneSnapping(), this._snappingOperation = u(this._snappingOperation), this._editGeometryOperations.destroy();
  }
  get _committedVertices() {
    return this._editGeometryOperations.data.parts[0].vertices.map((e4) => e4.pos);
  }
  get vertices() {
    return null != this._stagedVertex ? [...this._committedVertices, this._coordinateHelper.pointToArray(this._stagedVertex)] : this._committedVertices;
  }
  get hasZ() {
    return null != this._hasZ ? this._hasZ : "3d" === this.view.type;
  }
  set hasZ(e4) {
    this._hasZ = e4, this.notifyChange("hasZ");
  }
  get _stagedVertex() {
    return this._snappingOperation?.stagedPoint;
  }
  set _stagedVertex(e4) {
    this._snappingOperation && (this._snappingOperation.stagedPoint = a(e4));
  }
  canUndo() {
    return this._editGeometryOperations.canUndo;
  }
  canRedo() {
    return this._editGeometryOperations.canRedo;
  }
  undo() {
    this.canUndo && this._editGeometryOperations.undo();
  }
  redo() {
    this.canRedo && this._editGeometryOperations.redo();
  }
  getCoordsFromScreenPoint(e4) {
    const t2 = e4 && this.screenToMap(e4);
    return null == t2 ? null : t2.hasZ ? [t2.x, t2.y, t2.z] : [t2.x, t2.y];
  }
  getCoordsAndPointFromScreenPoint(e4) {
    const t2 = this.screenToMap(e4);
    return null == t2 ? null : t2.hasZ ? { vertex: [t2.x, t2.y, t2.z], mapPoint: t2 } : { vertex: [t2.x, t2.y], mapPoint: t2 };
  }
  screenToMap(e4) {
    let t2 = null;
    if ("3d" === this.view.type) if (this.hasZ) {
      const i3 = this.elevationInfo ?? x;
      t2 = this.view.sceneIntersectionHelper.intersectElevationFromScreen(f2(e4.x, e4.y), i3, this._getGeometryZValue());
    } else {
      const i3 = this.elevationInfo ?? G;
      t2 = this.view.sceneIntersectionHelper.intersectElevationFromScreen(f2(e4.x, e4.y), i3, 0), null != t2 && (t2.z = void 0);
    }
    else t2 = this.view.toMap(e4), null != t2 && (t2.z = this.hasZ ? this._getGeometryZValue() : void 0);
    return t2;
  }
  _pushCursorVertex(e4, t2) {
    const i3 = t(e4[0], e4[1], void 0, this.view.spatialReference);
    this._stagedVertexUnsnapped = i3;
    const r2 = this._snappingManager;
    if (null == r2) return this._stagedVertex = i3, void t2();
    y(this._snappingOperation.snap({ point: i3 }, r2, this._snappingContext)).then(() => {
      t2();
    });
  }
  _popCursorVertex() {
    this._stagedVertexUnsnapped = null, this._stagedVertex = null;
  }
  _getGeometryZValue() {
    return this.defaultZ;
  }
  _abortSnapping() {
    this._snappingOperation.abort();
  }
  _isDuplicateOfLastVertex(e4) {
    const t2 = this._editGeometryOperations.data.parts[0].getLastVertex()?.pos;
    if (t2 && e4[0] === t2[0] && e4[1] === t2[1]) return true;
    const { x: i3, y: r2 } = this._coordinateHelper.vectorToDehydratedPoint(e4);
    return null != this._lastVertexUnsnapped && i3 === this._lastVertexUnsnapped.x && r2 === this._lastVertexUnsnapped.y;
  }
  _shouldHandlePointerEvent(e4) {
    return w4(e4) && (null == this._activePointerId || this._activePointerId === e4.pointerId);
  }
  _vertexAddHandler(e4) {
    const t2 = null != this._stagedVertex ? this._coordinateHelper.pointToArray(this._stagedVertex) : this.getCoordsFromScreenPoint(this._cursorScreenPoint);
    null != t2 && this._addVertex(t2, e4.native);
  }
  _drawCompleteHandler(e4) {
    this._completeDrawing(e4.native);
  }
}, v = _a, _a.SNAPPING_GRAPHICS_LAYER_ID = "DrawAction-snapping-graphics-layer", _a);
__decorate([m({ readOnly: true })], f5.prototype, "vertices", null), __decorate([m({ type: Boolean, nonNullable: true })], f5.prototype, "interactiveUndoDisabled", void 0), __decorate([m({ readOnly: true })], f5.prototype, "history", void 0), __decorate([m({ readOnly: true })], f5.prototype, "redoHistory", void 0), __decorate([m()], f5.prototype, "snapToScene", void 0), __decorate([m()], f5.prototype, "view", void 0), __decorate([m()], f5.prototype, "elevationInfo", void 0), __decorate([m({ nonNullable: true })], f5.prototype, "defaultZ", void 0), __decorate([m()], f5.prototype, "hasZ", null), __decorate([m()], f5.prototype, "_snappingOperation", void 0), __decorate([m()], f5.prototype, "_stagedVertex", null), f5 = v = __decorate([a2("esri.views.draw.DrawAction")], f5);
var x = { mode: "absolute-height", offset: 0 };
var G = { mode: "on-the-ground", offset: 0 };
function w4(e4) {
  return "mouse" !== e4.pointerType || 0 === e4.button;
}
var V2 = f5;

// node_modules/@arcgis/core/views/draw/input/DrawEvents.js
var e3 = class {
  constructor(e4, t2, s2) {
    this.view = e4, this.defaultPrevented = false, this.type = "vertex-add", this.vertexIndex = t2, this.vertices = s2;
  }
  preventDefault() {
    this.defaultPrevented = true;
  }
};
var s = class {
  constructor(e4, t2, s2) {
    this.view = e4, this.defaultPrevented = false, this.type = "vertex-remove", this.vertexIndex = t2, this.vertices = s2;
  }
  preventDefault() {
    this.defaultPrevented = true;
  }
};
var i2 = class {
  constructor(e4, t2, s2, i3 = null) {
    this.view = e4, this.coordinates = null, this.defaultPrevented = false, this.type = "cursor-update", this.coordinates = 1 === s2.length ? s2[0] : null, this.vertexIndex = t2, this.vertices = s2, this.mapPoint = i3;
  }
  preventDefault() {
    this.defaultPrevented = true;
  }
};
var r = class {
  constructor(e4) {
    this.vertices = e4, this.coordinates = null, this.defaultPrevented = false, this.type = "draw-complete", this.coordinates = 1 === e4.length ? e4[0] : null;
  }
  preventDefault() {
    this.defaultPrevented = true;
  }
};

// node_modules/@arcgis/core/views/draw/MultipointDrawAction.js
var l3 = /* @__PURE__ */ Symbol("view-handles");
var c5 = /* @__PURE__ */ Symbol("undo-redo-handles");
var _2 = class extends V2 {
  constructor(e4) {
    super(e4), this._popVertexOnPointerMove = false, this._addVertexOnPointerUp = false;
  }
  initialize() {
    this._addViewHandles(), this._addUndoRedoHandles();
  }
  destroy() {
    this._removeViewHandles(), this._removeUndoRedoHandles(), this.emit("destroy");
  }
  undo() {
    super.undo(), this.notifyChange("vertices");
  }
  redo() {
    super.redo(), this.notifyChange("vertices");
  }
  complete() {
    this._completeDrawing();
  }
  _addViewHandles() {
    this._removeViewHandles(), this.addHandles([this.view.on("click", (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("pointer-down", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._activePointerId = e4.pointerId, this._addVertexOnPointerUp = true, this._cursorScreenPoint = n3(e4), "touch" === e4.pointerType && this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-move", (e4) => {
      this._popVertexOnPointerMove && (this.undo(), this._popVertexOnPointerMove = false), this._abortSnapping(), this._cursorScreenPoint = n3(e4), "touch" !== e4.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-drag", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._addVertexOnPointerUp = false);
    }, f3.TOOL), this.view.on("pointer-up", (e4) => {
      if (this._shouldHandlePointerEvent(e4)) {
        if (this._abortSnapping(), this._activePointerId = null, !this._addVertexOnPointerUp) {
          const t2 = "touch" === e4.pointerType;
          return void this._updateCursor(t2);
        }
        this._vertexAddHandler(e4);
      }
    }, f3.TOOL), this.view.on("drag", ["Shift"], (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("double-click", (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("double-click", ["Control"], (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("key-down", (e4) => {
      const { key: t2, repeat: i3 } = e4;
      t2 === c2.vertexAdd && !i3 && this._cursorScreenPoint ? (e4.stopPropagation(), this._abortSnapping(), this._vertexAddHandler(e4)) : t2 !== c2.complete || i3 ? t2 !== c2.undo || this.interactiveUndoDisabled || i3 ? t2 !== c2.redo || this.interactiveUndoDisabled || i3 ? t2 !== c2.pan || i3 || e4.stopPropagation() : (e4.stopPropagation(), this.redo()) : (e4.stopPropagation(), this.undo()) : (e4.stopPropagation(), this._drawCompleteHandler(e4));
    }, f3.TOOL), this.view.on("key-up", (e4) => {
      e4.key === c2.pan && e4.stopPropagation();
    }, f3.TOOL)], l3);
  }
  _addUndoRedoHandles() {
    this._removeUndoRedoHandles(), this.addHandles([this._editGeometryOperations.on("vertex-remove", (e4) => {
      if (this.notifyChange("vertices"), "undo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("undo", new s(this.view, e4.vertices[0].index, t2));
      }
    }), this._editGeometryOperations.on("vertex-add", (e4) => {
      if (this.notifyChange("vertices"), "apply" === e4.operation) {
        const e5 = this._committedVertices.length - 1, t2 = new e3(this.view, e5, this.vertices);
        this.emit("vertex-add", t2), t2.defaultPrevented && (this._popVertexOnPointerMove = true);
      } else if ("redo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("redo", new e3(this.view, e4.vertices[0].index, t2));
      }
    })], c5);
  }
  _removeViewHandles() {
    this.removeHandles(l3);
  }
  _removeUndoRedoHandles() {
    this.removeHandles(c5);
  }
  _addVertex(e4) {
    const t2 = this._coordinateHelper.arrayToVector(e4);
    this._isDuplicateOfLastVertex(t2) || this._editGeometryOperations.appendVertexToFirstPart(t2);
  }
  _updateCursor(e4 = false) {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const t2 = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null == t2 || e4 || this._pushCursorVertex(t2.vertex, () => this.emit("cursor-update", new i2(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new _(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._activePointerId = null, this._popCursorVertex(), this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping(), this.vertices.length < 1) return;
    const e4 = new r(this.vertices);
    this.emit("draw-complete", e4), e4.defaultPrevented || this._removeViewHandles();
  }
};
_2 = __decorate([a2("esri.views.draw.MultipointDrawAction")], _2);
var u3 = _2;

// node_modules/@arcgis/core/views/draw/PointDrawAction.js
var p3 = class extends V2 {
  constructor(t2) {
    super(t2), this._addVertexOnPointerUp = false, this._drawTool = null;
  }
  initialize() {
    this._addViewHandles(), "3d" === this.view.type && this.addResolvingPromise(this._addDrawTool());
  }
  destroy() {
    this._removeDrawTool(), this.emit("destroy");
  }
  complete() {
    this._completeDrawing();
  }
  _addViewHandles() {
    this._addViewHandles2DOnly(), this.addHandles([this.view.on("key-down", (t2) => {
      t2.key === c2.complete && (this._drawTool ? this.complete() : (this._abortSnapping(), this._vertexAddHandler(t2)));
    }, f3.TOOL)]);
  }
  _addViewHandles2DOnly() {
    "2d" === this.view.type && this.addHandles([this.view.on("pointer-down", (t2) => {
      this._shouldHandlePointerEvent(t2) && (this._abortSnapping(), this._activePointerId = t2.pointerId, this._addVertexOnPointerUp = true, this._cursorScreenPoint = n3(t2), "touch" === t2.pointerType && this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-move", (t2) => {
      this._abortSnapping(), this._cursorScreenPoint = n3(t2), "touch" !== t2.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-drag", (t2) => {
      this._shouldHandlePointerEvent(t2) && (this._abortSnapping(), this._addVertexOnPointerUp = false);
    }, f3.TOOL), this.view.on("pointer-up", (t2) => {
      if (this._shouldHandlePointerEvent(t2)) {
        if (this._abortSnapping(), this._activePointerId = null, !this._addVertexOnPointerUp) {
          const e4 = "touch" === t2.pointerType;
          return void this._updateCursor(e4);
        }
        t2.stopPropagation(), this._vertexAddHandler(t2);
      }
    }, f3.TOOL), this.view.on("drag", ["Shift"], (t2) => {
      t2.stopPropagation();
    }, f3.TOOL)]);
  }
  async _addDrawTool() {
    const [{ LegacyDrawTool: t2 }, e4] = await Promise.all([import("./LegacyDrawTool-IIHHRGLV.js"), c4()]), i3 = new t2({ view: this.view, elevationInfo: this.elevationInfo, hasZ: this.hasZ, geometryType: "point", mode: "click", automaticLengthMeasurementUtils: e4 });
    this.destroyed ? i3.destroy() : (this._drawTool = i3, this.view.addAndActivateTool(i3), this.addHandles([i3.on("cursor-update", (t3) => {
      1 === t3.vertices.length && this.emit("cursor-update", new i2(this.view, t3.vertices[0].vertexIndex, i3.getVertexCoords()));
    }), i3.on("complete", (t3) => {
      this.emit("draw-complete", new r(i3.getVertexCoords())), this._removeDrawTool(), this.removeAllHandles();
    })]));
  }
  _removeDrawTool() {
    this._drawTool && (this.view.tools.remove(this._drawTool), this._drawTool = null);
  }
  _addVertex(t2) {
    const e4 = this._coordinateHelper.arrayToVector(t2);
    this._isDuplicateOfLastVertex(e4) || (this._lastVertexUnsnapped = this._stagedVertexUnsnapped, this._popCursorVertex(), this._editGeometryOperations.appendVertexToFirstPart(e4), this.notifyChange("vertices"), this._completeDrawing());
  }
  _updateCursor(t2 = false) {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const e4 = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null == e4 || t2 || this._pushCursorVertex(e4.vertex, () => this.emit("cursor-update", new i2(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new _(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._drawTool) return void this._drawTool.completeCreateOperation();
    this._activePointerId = null, this._popCursorVertex(), this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping();
    const t2 = new r(this.vertices);
    this.emit("draw-complete", t2), t2.defaultPrevented || this.removeAllHandles();
  }
};
p3 = __decorate([a2("esri.views.draw.PointDrawAction")], p3);
var l4 = p3;

// node_modules/@arcgis/core/views/draw/PolygonDrawAction.js
var u4 = class extends V2 {
  constructor(e4) {
    super(e4), this._panEnabled = false, this._popVertexOnPointerMove = false, this._addVertexOnPointerUp = false, this._drawTool = null, this.mode = e;
  }
  initialize() {
    this._addViewHandles(), "3d" === this.view.type && this.addResolvingPromise(this._addDrawTool());
  }
  destroy() {
    this._removeDrawTool(), this.emit("destroy");
  }
  get _dragEnabled() {
    return "freehand" === this.mode || "hybrid" === this.mode;
  }
  get _clickEnabled() {
    return "click" === this.mode || "hybrid" === this.mode;
  }
  undo() {
    this._drawTool ? this._drawTool.undo() : (super.undo(), this.notifyChange("vertices"));
  }
  redo() {
    this._drawTool ? this._drawTool.redo() : (super.redo(), this.notifyChange("vertices"));
  }
  canUndo() {
    return this._drawTool?.canUndo() ?? super.canUndo();
  }
  canRedo() {
    return this._drawTool?.canRedo() ?? super.canRedo();
  }
  complete() {
    this._completeDrawing();
  }
  _getGeometryZValue() {
    return this.hasZ && this.vertices.length > 0 ? this.vertices[0][2] : this.defaultZ;
  }
  _addViewHandles() {
    this._addViewHandles2DOnly(), this.addHandles(this.view.on("key-down", (e4) => {
      const { key: t2, repeat: i3 } = e4;
      t2 !== c2.vertexAdd || i3 ? t2 !== c2.complete || i3 ? t2 !== c2.undo || this.interactiveUndoDisabled || i3 ? t2 !== c2.redo || this.interactiveUndoDisabled || i3 || (e4.stopPropagation(), this.redo()) : (e4.stopPropagation(), this.undo()) : (e4.stopPropagation(), this._drawCompleteHandler(e4)) : (e4.stopPropagation(), this._handleVertexAddKey(e4));
    }, f3.TOOL));
  }
  _addViewHandles2DOnly() {
    "2d" === this.view.type && (this.addHandles([this.view.on("click", (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("pointer-down", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._activePointerId = e4.pointerId, this._addVertexOnPointerUp = true, this._cursorScreenPoint = n3(e4), "touch" === e4.pointerType && this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-move", (e4) => {
      this._abortSnapping(), this._popVertexOnPointerMove && (this.undo(), this._popVertexOnPointerMove = false), this._cursorScreenPoint = n3(e4), "touch" !== e4.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-drag", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._cursorScreenPoint = n3(e4), this._dragEnabled && !this._panEnabled ? this._vertexAddHandler(e4) : this._addVertexOnPointerUp = false);
    }, f3.TOOL), this.view.on("pointer-up", (e4) => {
      if (this._shouldHandlePointerEvent(e4)) {
        if (this._abortSnapping(), this._activePointerId = null, !this._addVertexOnPointerUp) {
          const t2 = "touch" === e4.pointerType;
          return void this._updateCursor(t2);
        }
        if (!this._clickEnabled) return 1 === this.vertices.length && this.vertices.pop(), void this._drawCompleteHandler(e4);
        this._vertexAddHandler(e4);
      }
    }, f3.TOOL), this.view.on("drag", (e4) => {
      this._dragEnabled && null != this._activePointerId && !this._panEnabled && e4.stopPropagation();
    }, f3.TOOL), this.view.on("drag", ["Shift"], (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("double-click", (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("double-click", ["Control"], (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("key-down", (e4) => {
      const { key: t2, repeat: i3 } = e4;
      t2 !== c2.pan || i3 || (e4.stopPropagation(), this._panEnabled = true);
    }, f3.TOOL), this.view.on("key-up", (e4) => {
      e4.key === c2.pan && (e4.stopPropagation(), this._panEnabled = false);
    }, f3.TOOL)]), this._addUndoRedoHandles());
  }
  _handleVertexAddKey(e4) {
    this._drawTool ? this._drawTool.drawOperation.commitStagedVertex() : this._cursorScreenPoint && (this._abortSnapping(), this._vertexAddHandler(e4));
  }
  _addUndoRedoHandles() {
    this.addHandles([this._editGeometryOperations.on("vertex-remove", (e4) => {
      if (this.notifyChange("vertices"), "undo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("undo", new s(this.view, e4.vertices[0].index, t2));
      }
    }), this._editGeometryOperations.on("vertex-add", (e4) => {
      if (this.notifyChange("vertices"), "apply" === e4.operation) {
        const e5 = this._committedVertices.length - 1, t2 = new e3(this.view, e5, this.vertices);
        this.emit("vertex-add", t2), t2.defaultPrevented && (this._popVertexOnPointerMove = true);
      } else if ("redo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("redo", new e3(this.view, e4.vertices[0].index, t2));
      }
    })]);
  }
  async _addDrawTool() {
    const [{ LegacyDrawTool: e4 }, t2] = await Promise.all([import("./LegacyDrawTool-IIHHRGLV.js"), c4()]), i3 = new e4({ view: this.view, elevationInfo: this.elevationInfo, hasZ: this.hasZ, geometryType: "polygon", mode: this.mode, automaticLengthMeasurementUtils: t2 });
    this.destroyed ? i3.destroy() : (this._drawTool = i3, this.view.addAndActivateTool(i3), i3.on("vertex-add", (e5) => {
      if (1 === e5.vertices.length) {
        const { view: t3 } = this, o = e5.vertices[0].vertexIndex, r2 = i3.getVertexCoords();
        this.emit("vertex-add", new e3(t3, o, r2)), "undo" !== e5.operation && "redo" !== e5.operation || this.emit(e5.operation, new e3(t3, o, r2));
      }
    }), i3.on("vertex-remove", (e5) => {
      if (1 === e5.vertices.length) {
        const { view: t3 } = this, o = e5.vertices[0].vertexIndex, r2 = i3.getVertexCoords();
        this.emit("vertex-remove", new s(t3, o, r2)), "undo" !== e5.operation && "redo" !== e5.operation || this.emit(e5.operation, new s(t3, o, r2));
      }
    }), i3.on("cursor-update", (e5) => {
      1 === e5.vertices.length && this.emit("cursor-update", new i2(this.view, e5.vertices[0].vertexIndex, i3.getVertexCoords()));
    }), i3.on("complete", (e5) => {
      this.emit("draw-complete", new r(i3.getVertexCoords())), this._removeDrawTool(), this.removeAllHandles();
    }));
  }
  _removeDrawTool() {
    this._drawTool && (this.view.tools.remove(this._drawTool), this._drawTool = null);
  }
  _addVertex(e4) {
    const t2 = this._coordinateHelper.arrayToVector(e4);
    this._isDuplicateOfLastVertex(t2) || (this._lastVertexUnsnapped = this._stagedVertexUnsnapped, this._popCursorVertex(), this._editGeometryOperations.appendVertexToFirstPart(t2));
  }
  _updateCursor(e4 = false) {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const t2 = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null == t2 || e4 || this._pushCursorVertex(t2.vertex, () => this.emit("cursor-update", new i2(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new _(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._drawTool) return void this._drawTool.completeCreateOperation();
    if (this._activePointerId = null, this._popCursorVertex(), this._committedVertices.length < 3) return;
    this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping();
    const e4 = new r(this.vertices);
    this.emit("draw-complete", e4), e4.defaultPrevented || this.removeAllHandles();
  }
};
__decorate([m()], u4.prototype, "_dragEnabled", null), __decorate([m()], u4.prototype, "_clickEnabled", null), __decorate([m({ type: c3 })], u4.prototype, "mode", void 0), u4 = __decorate([a2("esri.views.draw.PolygonDrawAction")], u4);
var m4 = u4;

// node_modules/@arcgis/core/views/draw/PolylineDrawAction.js
var u5 = class extends V2 {
  constructor(e4) {
    super(e4), this._panEnabled = false, this._popVertexOnPointerMove = false, this._addVertexOnPointerUp = false, this._drawTool = null, this.mode = e;
  }
  initialize() {
    this._addViewHandles(), "3d" === this.view.type && this.addResolvingPromise(this._addDrawTool());
  }
  destroy() {
    this._removeDrawTool(), this.emit("destroy");
  }
  get _clickEnabled() {
    return "click" === this.mode || "hybrid" === this.mode;
  }
  get _dragEnabled() {
    return "freehand" === this.mode || "hybrid" === this.mode;
  }
  undo() {
    this._drawTool ? this._drawTool.undo() : (super.undo(), this.notifyChange("vertices"));
  }
  redo() {
    this._drawTool ? this._drawTool.redo() : (super.redo(), this.notifyChange("vertices"));
  }
  canUndo() {
    return this._drawTool?.canUndo() ?? super.canUndo();
  }
  canRedo() {
    return this._drawTool?.canRedo() ?? super.canRedo();
  }
  complete() {
    this._completeDrawing();
  }
  _addViewHandles() {
    this._addViewHandles2DOnly(), this.addHandles([this.view.on("key-down", (e4) => {
      const { key: t2, repeat: i3 } = e4;
      t2 !== c2.vertexAdd || i3 ? t2 !== c2.complete || i3 ? t2 !== c2.undo || this.interactiveUndoDisabled || i3 ? t2 !== c2.redo || this.interactiveUndoDisabled || i3 || (e4.stopPropagation(), this.redo()) : (e4.stopPropagation(), this.undo()) : (e4.stopPropagation(), this._drawCompleteHandler(e4)) : (e4.stopPropagation(), this._handleVertexAddKey(e4));
    }, f3.TOOL)]);
  }
  _addViewHandles2DOnly() {
    "2d" === this.view.type && (this.addHandles([this.view.on("click", (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("pointer-down", (e4) => {
      this._shouldHandlePointerEvent(e4) && !this._panEnabled && (this._abortSnapping(), this._activePointerId = e4.pointerId, this._addVertexOnPointerUp = true, this._cursorScreenPoint = n3(e4), "touch" === e4.pointerType && this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-move", (e4) => {
      this._popVertexOnPointerMove && (this.undo(), this._popVertexOnPointerMove = false), this._abortSnapping(), this._cursorScreenPoint = n3(e4), "touch" !== e4.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-drag", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._cursorScreenPoint = n3(e4), this._dragEnabled && !this._panEnabled ? this._vertexAddHandler(e4) : this._addVertexOnPointerUp = false);
    }, f3.TOOL), this.view.on("pointer-up", (e4) => {
      if (this._shouldHandlePointerEvent(e4)) {
        if (this._abortSnapping(), this._activePointerId = null, !this._addVertexOnPointerUp) {
          const t2 = "touch" === e4.pointerType;
          return void this._updateCursor(t2);
        }
        if (!this._clickEnabled) return 1 === this.vertices.length && this.vertices.pop(), void this._drawCompleteHandler(e4);
        this._vertexAddHandler(e4);
      }
    }, f3.TOOL), this.view.on("drag", (e4) => {
      this._dragEnabled && null != this._activePointerId && !this._panEnabled && e4.stopPropagation();
    }, f3.TOOL), this.view.on("drag", ["Shift"], (e4) => {
      e4.stopPropagation();
    }, f3.TOOL), this.view.on("double-click", (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("double-click", ["Control"], (e4) => {
      e4.stopPropagation(), this._drawCompleteHandler(e4);
    }, f3.TOOL), this.view.on("key-down", (e4) => {
      const { key: t2, repeat: i3 } = e4;
      t2 !== c2.pan || i3 || (e4.stopPropagation(), this._panEnabled = true);
    }, f3.TOOL), this.view.on("key-up", (e4) => {
      e4.key === c2.pan && (e4.stopPropagation(), this._panEnabled = false);
    }, f3.TOOL)]), this._addUndoRedoHandles());
  }
  _handleVertexAddKey(e4) {
    this._drawTool ? this._drawTool.drawOperation.commitStagedVertex() : this._cursorScreenPoint && (this._abortSnapping(), this._vertexAddHandler(e4));
  }
  _addUndoRedoHandles() {
    this.addHandles([this._editGeometryOperations.on("vertex-remove", (e4) => {
      if (this.notifyChange("vertices"), "undo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("undo", new s(this.view, e4.vertices[0].index, t2));
      }
    }), this._editGeometryOperations.on("vertex-add", (e4) => {
      if (this.notifyChange("vertices"), "apply" === e4.operation) {
        const e5 = this._committedVertices.length - 1, t2 = new e3(this.view, e5, this.vertices);
        this.emit("vertex-add", t2), t2.defaultPrevented && (this._popVertexOnPointerMove = true);
      } else if ("redo" === e4.operation) {
        const t2 = [...this._committedVertices];
        null != this._stagedVertex && t2.push(this._coordinateHelper.pointToArray(this._stagedVertex)), this.emit("redo", new e3(this.view, e4.vertices[0].index, t2));
      }
    })]);
  }
  async _addDrawTool() {
    const [{ LegacyDrawTool: e4 }, t2] = await Promise.all([import("./LegacyDrawTool-IIHHRGLV.js"), c4()]), i3 = new e4({ view: this.view, elevationInfo: this.elevationInfo, hasZ: this.hasZ, geometryType: "polyline", mode: this.mode, automaticLengthMeasurementUtils: t2 });
    this.destroyed ? i3.destroy() : (this._drawTool = i3, this.view.addAndActivateTool(i3), this.addHandles([i3.on("vertex-add", (e5) => {
      if (1 !== e5.vertices.length) return;
      const { view: t3 } = this, o = e5.vertices[0].vertexIndex, r2 = i3.getVertexCoords();
      this.emit("vertex-add", new e3(t3, o, r2)), "undo" !== e5.operation && "redo" !== e5.operation || this.emit(e5.operation, new e3(t3, o, r2));
    }), i3.on("vertex-remove", (e5) => {
      if (1 !== e5.vertices.length) return;
      const { view: t3 } = this, o = e5.vertices[0].vertexIndex, r2 = i3.getVertexCoords();
      this.emit("vertex-remove", new s(t3, o, r2)), "undo" !== e5.operation && "redo" !== e5.operation || this.emit(e5.operation, new s(t3, o, r2));
    }), i3.on("cursor-update", (e5) => {
      1 === e5.vertices.length && this.emit("cursor-update", new i2(this.view, e5.vertices[0].vertexIndex, i3.getVertexCoords()));
    }), i3.on("complete", (e5) => {
      this.emit("draw-complete", new r(i3.getVertexCoords())), this._removeDrawTool(), this.removeAllHandles();
    })]));
  }
  _removeDrawTool() {
    this._drawTool && (this.view.tools.remove(this._drawTool), this._drawTool = null);
  }
  _addVertex(e4) {
    const t2 = this._coordinateHelper.arrayToVector(e4);
    this._isDuplicateOfLastVertex(t2) || (this._lastVertexUnsnapped = this._stagedVertexUnsnapped, this._popCursorVertex(), this._editGeometryOperations.appendVertexToFirstPart(t2));
  }
  _updateCursor(e4 = false) {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const t2 = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null == t2 || e4 || this._pushCursorVertex(t2.vertex, () => this.emit("cursor-update", new i2(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new _(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._drawTool) return void this._drawTool.completeCreateOperation();
    if (this._activePointerId = null, this._popCursorVertex(), this._committedVertices.length < 2) return;
    this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping();
    const e4 = new r(this.vertices);
    this.emit("draw-complete", e4), e4.defaultPrevented || this.removeAllHandles();
  }
};
__decorate([m()], u5.prototype, "_clickEnabled", null), __decorate([m()], u5.prototype, "_dragEnabled", null), __decorate([m({ type: c3 })], u5.prototype, "mode", void 0), u5 = __decorate([a2("esri.views.draw.PolylineDrawAction")], u5);
var m5 = u5;

// node_modules/@arcgis/core/views/draw/SegmentDrawAction.js
var v2 = ["freehand", "click"];
var u6 = class extends V2 {
  constructor(e4) {
    super(e4), this._isDragging = false, this._panEnabled = false, this._addVertexOnPointerUp = false, this._drawTool = null, this.viewAlignedCoordinateSystem = null, this.mode = "freehand";
  }
  initialize() {
    "2d" === this.view.type ? this._addViewHandles() : this.addResolvingPromise(this._addDrawTool());
  }
  destroy() {
    this._removeDrawTool(), this.emit("destroy");
  }
  complete() {
    this._completeDrawing();
  }
  _getGeometryZValue() {
    return this.hasZ && this.vertices.length > 0 ? this.vertices[0][2] : this.defaultZ;
  }
  _addViewHandles() {
    "click" === this.mode ? this.addHandles(this._getClickModeViewHandles()) : this.addHandles(this._getDragModeViewHandles());
  }
  _getDragModeViewHandles() {
    return [this.view.on("immediate-click", (e4) => {
      if (e4.stopPropagation(), e4.mapPoint && !this._panEnabled) {
        null != this.getCoordsFromScreenPoint(n3(e4)) && (this._vertexAddHandler(e4), this._drawCompleteHandler(e4));
      }
    }, f3.TOOL), this.view.on("pointer-down", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._panEnabled || (this._resetGeometry(), this._addVertexOnPointerUp = true, this._cursorScreenPoint = n3(e4), this._activePointerId = e4.pointerId, this._vertexAddHandler(e4), this._isDragging = false, "touch" === e4.pointerType && this._updateCursor()));
    }, f3.TOOL), this.view.on("pointer-move", (e4) => {
      this._abortSnapping(), null == this._activePointerId && "touch" !== e4.pointerType && (this._cursorScreenPoint = n3(e4), this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-drag", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._isDragging = true, this._cursorScreenPoint = n3(e4), this._updateCursor());
    }, f3.TOOL), this.view.on("pointer-up", (e4) => {
      this._shouldHandlePointerEvent(e4) && this._addVertexOnPointerUp && (this._abortSnapping(), this._activePointerId = null, this._isDragging && this._vertexAddHandler(e4), 2 === this._committedVertices.length && this._drawCompleteHandler(e4), this._isDragging = false);
    }, f3.TOOL), this.view.on("key-down", (e4) => {
      e4.key === c2.complete && this._cursorScreenPoint ? (this._abortSnapping(), this._vertexAddHandler(e4), this._drawCompleteHandler(e4)) : e4.key === c2.pan && (this._panEnabled = true);
    }, f3.TOOL), this.view.on("key-up", (e4) => {
      e4.key === c2.pan && (this._panEnabled = false);
    }, f3.TOOL), this.view.on("drag", (e4) => {
      null != this._activePointerId && e4.stopPropagation();
    }, f3.TOOL), this.view.on("drag", ["Shift"], (e4) => {
      e4.stopPropagation();
    }, f3.TOOL)];
  }
  _getClickModeViewHandles() {
    return [this.view.on("pointer-down", (e4) => {
      this._abortSnapping(), this._cursorScreenPoint = n3(e4), this._activePointerId = e4.pointerId, this._isDragging = false, "touch" === e4.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-move", (e4) => {
      this._abortSnapping(), this._cursorScreenPoint = n3(e4), null == this._activePointerId && "touch" !== e4.pointerType && this._updateCursor();
    }, f3.TOOL), this.view.on("pointer-drag", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._isDragging = true);
    }, f3.TOOL), this.view.on("pointer-up", (e4) => {
      this._shouldHandlePointerEvent(e4) && (this._abortSnapping(), this._activePointerId = null, e4.stopPropagation(), this._isDragging || this._vertexAddHandler(e4), 2 !== this.vertices.length || this._isDragging || this._drawCompleteHandler(e4), this._isDragging = false);
    }, f3.TOOL), this.view.on("key-down", (e4) => {
      e4.key === c2.vertexAdd && this._cursorScreenPoint && (this._vertexAddHandler(e4), 2 === this.vertices.length && this._drawCompleteHandler(e4)), e4.key === c2.complete && this._cursorScreenPoint && 2 === this.vertices.length && (this._vertexAddHandler(e4), this._drawCompleteHandler(e4));
    }, f3.TOOL)];
  }
  async _addDrawTool() {
    const [{ LegacyDrawTool: e4 }, t2] = await Promise.all([import("./LegacyDrawTool-IIHHRGLV.js"), c4()]), i3 = new e4({ view: this.view, elevationInfo: this.elevationInfo, hasZ: this.hasZ, geometryType: "segment", mode: this.mode, automaticLengthMeasurementUtils: t2 });
    this.destroyed ? i3.destroy() : (this._drawTool = i3, this.view.addAndActivateTool(i3), this.addHandles([i3.on("vertex-add", (e5) => {
      1 === e5.vertices.length && this.emit("vertex-add", new e3(this.view, e5.vertices[0].vertexIndex, i3.getVertexCoords()));
    }), i3.on("cursor-update", (e5) => {
      1 === e5.vertices.length && this.emit("cursor-update", new i2(this.view, e5.vertices[0].vertexIndex, i3.getVertexCoords()));
    }), i3.on("complete", (e5) => {
      this.emit("draw-complete", new r(i3.getVertexCoords())), this._removeDrawTool();
    }), this.view.on("key-down", (e5) => {
      e5.key !== c2.vertexAdd || e5.repeat || "click" !== this.mode ? e5.key !== c2.complete || e5.repeat || i3.completeCreateOperation() : i3.drawOperation.numCommittedVertices > 0 ? i3.completeCreateOperation() : i3.drawOperation.commitStagedVertex();
    }, f3.TOOL)]));
  }
  _removeDrawTool() {
    this._drawTool && (this.view.tools.remove(this._drawTool), this._drawTool = null);
  }
  _addVertex(e4) {
    const t2 = this._coordinateHelper.arrayToVector(e4);
    if (this._isDuplicateOfLastVertex(t2)) return;
    this._lastVertexUnsnapped = this._stagedVertexUnsnapped, this._popCursorVertex(), this._editGeometryOperations.appendVertexToFirstPart(t2), 1 === this._committedVertices.length && (this.viewAlignedCoordinateSystem = W(this.view, this._committedVertices[0]));
    const i3 = this._committedVertices.length - 1, r2 = new e3(this.view, i3, this.vertices);
    this.emit("vertex-add", r2);
  }
  _updateCursor() {
    if (this._popCursorVertex(), !this._cursorScreenPoint) return;
    const e4 = this.getCoordsAndPointFromScreenPoint(this._cursorScreenPoint);
    null != e4 && this._pushCursorVertex(e4.vertex, () => this.emit("cursor-update", new i2(this.view, this._activeComponent.vertices.length, this.vertices, null != this._stagedVertex ? new _(this._stagedVertex) : null)));
  }
  _completeDrawing() {
    if (this._drawTool) return this._drawTool.completeCreateOperation(), void this.removeAllHandles();
    if (this._activePointerId = null, this._popCursorVertex(), this._cursorScreenPoint = null, this._isDragging = false, this._abortSnapping(), null != this._snappingManager && this._snappingManager.doneSnapping(), this.vertices.length < 1) return;
    const e4 = new r(this.vertices);
    this.emit("draw-complete", e4), e4.defaultPrevented || this.removeAllHandles();
  }
  _resetGeometry() {
    this._editGeometryOperations.destroy(), this._editGeometryOperations = new g(new w3("polygon", this._coordinateHelper), 2), this._activeComponent = new V(this._coordinateHelper.spatialReference, 2), this._editGeometryOperations.data.parts.push(this._activeComponent);
  }
};
__decorate([m({ type: v2 })], u6.prototype, "mode", void 0), u6 = __decorate([a2("esri.views.draw.SegmentDrawAction")], u6);
var w5 = u6;

// node_modules/@arcgis/core/views/draw/Draw.js
var p4 = class extends b {
  constructor(t2) {
    super(t2), this.activeAction = null, this.type = "draw", this.view = null;
  }
  destroy() {
    this.activeAction && (this.activeAction.destroy(), this.activeAction = null);
  }
  create(t2, e4) {
    this.reset();
    const o = { view: this.view, ...e4 };
    switch (t2) {
      case "point":
        o.editGeometryType = "point", this.activeAction = new l4(o);
        break;
      case "polyline":
        o.editGeometryType = "polyline", this.activeAction = new m5(o);
        break;
      case "multipoint":
        o.editGeometryType = "polygon", this.activeAction = new u3(o);
        break;
      case "polygon":
        o.editGeometryType = "polygon", this.activeAction = new m4(o);
        break;
      case "rectangle":
      case "circle":
      case "ellipse":
      case "triangle":
        o.editGeometryType = "polygon", this.activeAction = new w5(o);
    }
    return this.activeAction;
  }
  complete() {
    this.activeAction?.complete(), this.activeAction = null;
  }
  reset() {
    this.activeAction?.destroy(), this.activeAction = null;
  }
};
__decorate([m()], p4.prototype, "activeAction", void 0), __decorate([m({ readOnly: true })], p4.prototype, "type", void 0), __decorate([m()], p4.prototype, "view", void 0), p4 = __decorate([a2("esri.views.draw.Draw")], p4);
var l5 = p4;

export {
  l5 as l,
  p2 as p,
  u2 as u
};
//# sourceMappingURL=chunk-VHEWDG4N.js.map
