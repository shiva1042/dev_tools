import {
  t
} from "./chunk-YYGF76T3.js";
import {
  _,
  e,
  i
} from "./chunk-CXUJAI27.js";
import {
  r
} from "./chunk-O5NPUMOD.js";
import {
  s
} from "./chunk-SHCBT5YZ.js";

// node_modules/@arcgis/core/layers/graphics/data/StreamFeatureManager.js
var d = 1e3;
var a = class {
  constructor(t2, s2, r2, o, d2 = 128) {
    if (this._trackIdToObservations = /* @__PURE__ */ new Map(), this._idCounter = 0, this._lastPurge = performance.now(), this._addOrUpdated = /* @__PURE__ */ new Map(), this._removed = [], this._maxAge = 0, this._timeInfo = r2, this._purgeOptions = o, this.store = t2, "unique-id-composite" === s2.type) throw new s("stream-layer", "composite uniqueIds are not supported");
    this.idField = s2.fieldName, this.purgeInterval = d2, this._useGeneratedIds = this.idField === _;
  }
  removeById(t2) {
    this._removed.push(t2);
  }
  removeByTrackId(t2) {
    const e2 = this._trackIdToObservations.get(t2);
    if (e2) for (const s2 of e2.entries) this._removed.push(s2);
  }
  add(e2) {
    if (this._useGeneratedIds) {
      const t2 = this._nextId();
      e2.attributes[this.idField] = t2, e2.objectId = t2;
    } else e2.objectId = e2.attributes[this.idField];
    const i2 = e2.objectId;
    if (this._addOrUpdated.set(i2, e2), this._maxAge = Math.max(this._maxAge, e2.attributes[this._timeInfo.startTimeField]), !this._timeInfo.trackIdField) return null == this._trackIdLessObservations && (this._trackIdLessObservations = new t(1e5)), void this._trackIdLessObservations.enqueue(i2);
    const r2 = e2.attributes[this._timeInfo.trackIdField];
    if (!this._trackIdToObservations.has(r2)) {
      const e3 = null != this._purgeOptions?.maxObservations ? this._purgeOptions.maxObservations : d, i3 = r(e3, 0, d);
      this._trackIdToObservations.set(r2, new t(i3));
    }
    const o = this._trackIdToObservations.get(r2), a2 = o?.enqueue(i2);
    null != a2 && (this._addOrUpdated.has(a2) ? this._addOrUpdated.delete(a2) : this._removed.push(a2));
  }
  checkForUpdates() {
    const t2 = this._getToAdd(), e2 = this._getToRemove(), s2 = performance.now(), i2 = s2 - this._lastPurge, d2 = Date.now();
    i2 >= this.purgeInterval && (this._purge(s2), this._lastPurge = s2);
    const a2 = [];
    if (null != e2) for (const r2 of e2) {
      const t3 = this.store.removeById(r2);
      null != t3 && a2.push(t3);
    }
    const n = [];
    if (null != t2) {
      const i3 = new Set(e2 ?? []);
      for (const e3 of t2) i3.has(e3.objectId) || (e3.attributes[e] = s2, e3.attributes[i] = d2, this.store.add(e3), n.push(e3));
    }
    return !(!n.length && !a2?.length) && (this.store.update(n, a2), true);
  }
  _getToAdd() {
    if (!this._addOrUpdated.size) return null;
    const t2 = new Array(this._addOrUpdated.size);
    let e2 = 0;
    return this._addOrUpdated.forEach((s2) => t2[e2++] = s2), this._addOrUpdated.clear(), t2;
  }
  _getToRemove() {
    const t2 = this._removed;
    return this._removed.length ? (this._removed = [], t2) : null;
  }
  _nextId() {
    const t2 = this._idCounter;
    return this._idCounter = (this._idCounter + 1) % 4294967294 + 1, t2;
  }
  _purge(t2) {
    const e2 = this._purgeOptions;
    null != e2 && (this._purgeSomeByDisplayCount(e2), this._purgeByAge(e2), this._purgeByAgeReceived(t2, e2), this._purgeTracks());
  }
  _purgeSomeByDisplayCount(t2) {
    if (!t2.displayCount) return;
    let e2 = this.store.size;
    if (e2 > t2.displayCount) {
      if (this._timeInfo.trackIdField) {
        for (const s2 of this._trackIdToObservations.values()) if (e2 > t2.displayCount && s2.size) {
          const t3 = s2.dequeue();
          this._removed.push(t3), e2--;
        }
      }
      if (null != this._trackIdLessObservations) {
        let s2 = e2 - t2.displayCount;
        for (; s2-- > 0; ) {
          const t3 = this._trackIdLessObservations.dequeue();
          null != t3 && this._removed.push(t3);
        }
      }
    }
  }
  _purgeByAge(t2) {
    const e2 = this._timeInfo?.startTimeField;
    if (!t2.age || !e2) return;
    const s2 = 60 * t2.age * 1e3, i2 = this._maxAge - s2;
    this.store.forEach((t3) => {
      t3.attributes[e2] < i2 && this._removed.push(t3.objectId);
    });
  }
  _purgeByAgeReceived(t2, e2) {
    if (!e2.ageReceived) return;
    const s2 = t2 - 60 * e2.ageReceived * 1e3;
    this.store.forEach((t3) => {
      t3.attributes[e] < s2 && this._removed.push(t3.objectId);
    });
  }
  _purgeTracks() {
    this._trackIdToObservations.forEach((t2, e2) => {
      0 === t2.size && this._trackIdToObservations.delete(e2);
    });
  }
};

export {
  a
};
//# sourceMappingURL=chunk-AVTQNXHZ.js.map
