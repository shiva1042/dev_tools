import {
  o
} from "./chunk-PL5DDSFM.js";
import {
  f
} from "./chunk-W4PJYQIZ.js";
import {
  e
} from "./chunk-COMSADUT.js";
import {
  s
} from "./chunk-SDI3N3QP.js";
import {
  r
} from "./chunk-YOYO2XMY.js";
import {
  t
} from "./chunk-CFXCM2NO.js";

// node_modules/@arcgis/core/chunks/MeasurementArrow.glsl.js
function s2(s3) {
  const a2 = new s(), { vertex: d, fragment: l } = a2;
  f(d, s3), d.uniforms.add(new r("width", (e2) => e2.width)), a2.attributes.add("position", "vec3"), a2.attributes.add("normal", "vec3"), a2.attributes.add("uv0", "vec2"), a2.attributes.add("length", "float"), a2.varyings.add("vtc", "vec2"), a2.varyings.add("vlength", "float"), a2.varyings.add("vradius", "float"), d.main.add(t`vec3 bitangent = normal;
vtc = uv0;
vlength = length;
vradius = 0.5 * width;
vec4 pos = view * vec4(position + vradius * bitangent * uv0.y, 1.0);
gl_Position = proj * pos;`), l.uniforms.add(new r("outlineSize", (e2) => e2.outlineSize), new e("outlineColor", (e2) => e2.outlineColor), new r("stripeLength", (e2) => e2.stripeLength), new e("stripeEvenColor", (e2) => e2.stripeEvenColor), new e("stripeOddColor", (e2) => e2.stripeOddColor));
  const c = 1 / Math.sqrt(2);
  return l.code.add(t`
    const float INV_SQRT2 = ${t.float(c)};

    vec4 arrowColor(vec2 tc, float len) {
      float d = INV_SQRT2 * (tc.x - abs(tc.y));
      d = min(d, INV_SQRT2 * (len - tc.x - abs(tc.y)));
      d = min(d, 1.0 - abs(tc.y));

      if (d < 0.0) {
        return vec4(0.0);
      }
      if (d < outlineSize) {
        return outlineColor;
      }
      return fract(0.5 / stripeLength * tc.x * vradius) >= 0.5 ? stripeOddColor : stripeEvenColor;
    }`), l.main.add(t`
    vec2 ntc = vec2(vtc.x / vradius, vtc.y);
    vec4 color = arrowColor(ntc, vlength / vradius);
    if (color.a < ${t.float(o)}) {
      discard;
    }
    fragColor = color;`), a2;
}
var a = Object.freeze(Object.defineProperty({ __proto__: null, build: s2 }, Symbol.toStringTag, { value: "Module" }));

export {
  s2 as s,
  a
};
//# sourceMappingURL=chunk-EPBHKV22.js.map
