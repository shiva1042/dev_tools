import {
  n
} from "./chunk-NR52NJHY.js";
import {
  o2 as o
} from "./chunk-SBKCZHIO.js";
import {
  i as i3,
  o2
} from "./chunk-LRDKCD3M.js";
import {
  m
} from "./chunk-2N4GFVCO.js";
import {
  A,
  h
} from "./chunk-YNGW52OC.js";
import {
  D,
  E,
  F,
  G,
  H,
  I,
  J,
  K
} from "./chunk-JE2NJSBU.js";
import {
  i as i2
} from "./chunk-KBUR23KW.js";
import {
  s
} from "./chunk-SHCBT5YZ.js";
import {
  i3 as i
} from "./chunk-45CZB3H2.js";
import {
  has
} from "./chunk-LLFDHRTA.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/AttributeStoreView.js
var f = () => i.getLogger("esri.views.2d.engine.webgl.AttributeStoreView");
var b = class {
  constructor(t, e, i6) {
    this._texture = null, this._lastTexture = null, this._fbos = {}, this.texelSize = 4;
    const { buffer: s3, pixelType: r2, textureOnly: a } = t, n3 = i3(r2);
    this.blockIndex = i6, this.pixelType = r2, this.size = e, this.textureOnly = a, a || (this.data = new n3(s3)), this._resetRange();
  }
  destroy() {
    this._texture?.dispose();
    for (const t in this._fbos) {
      const e = this._fbos[t];
      e && ("0" === t && e.detachColorTexture(), e.dispose()), this._fbos[t] = null;
    }
    this._texture = null;
  }
  get _textureDesc() {
    const t = new h(this.size);
    return t.wrapMode = 33071, t.samplingMode = 9728, t.dataType = this.pixelType, t;
  }
  get usedMemory() {
    return null != this.data ? i2(this.data) : 0;
  }
  setData(t, e, i6) {
    const s3 = o(t), r2 = this.data, a = s3 * this.texelSize + e;
    !r2 || a >= r2.length || (r2[a] = i6, this.dirtyStart = Math.min(this.dirtyStart, s3), this.dirtyEnd = Math.max(this.dirtyEnd, s3));
  }
  getData(t, e) {
    if (null == this.data) return null;
    const i6 = o(t) * this.texelSize + e;
    return !this.data || i6 >= this.data.length ? null : this.data[i6];
  }
  getTexture(t) {
    return this._texture ?? this._initTexture(t);
  }
  getFBO(t, e = 0) {
    if (!this._fbos[e]) {
      const i6 = 0 === e ? this.getTexture(t) : this._textureDesc;
      this._fbos[e] = new m(t, i6);
    }
    return this._fbos[e];
  }
  get hasDirty() {
    const t = this.dirtyStart;
    return this.dirtyEnd >= t;
  }
  updateTexture(e, i6) {
    try {
      const s3 = this.dirtyStart, r2 = this.dirtyEnd;
      if (!this.hasDirty) return;
      has("esri-2d-update-debug") && console.debug(`Version[${i6}] AttributeStoreView.updateTexture`, { start: s3, end: r2, firstBytes: new Uint8Array(this.data.buffer.slice(0, 16)), block: this }), this._resetRange();
      const a = this.data.buffer, n3 = this.getTexture(e), u = 4, h2 = (s3 - s3 % this.size) / this.size, o3 = (r2 - r2 % this.size) / this.size, d = h2, l = this.size, g = o3, _ = h2 * this.size * u, c = (l + g * this.size) * u - _, x = i3(this.pixelType), b2 = new x(a, _ * x.BYTES_PER_ELEMENT, c), m3 = this.size, T = g - d + 1;
      if (T > this.size) return void f().error(new s("mapview-webgl", "Out-of-bounds index when updating AttributeData"));
      n3.updateData(0, 0, d, m3, T, b2);
    } catch (s3) {
    }
  }
  update(t) {
    const { data: e, start: i6, end: s3 } = t;
    if (null != e && null != this.data) {
      const s4 = this.data, r2 = i6 * this.texelSize;
      for (let i7 = 0; i7 < e.length; i7++) {
        const a = 1 << i7 % this.texelSize;
        t.layout & a && (s4[r2 + i7] = e[i7]);
      }
    }
    this.dirtyStart = Math.min(this.dirtyStart, i6), this.dirtyEnd = Math.max(this.dirtyEnd, s3);
  }
  resize(t, e) {
    const i6 = this.size;
    if (this.size = e, this.textureOnly) return void (i6 !== this.size && (this._lastTexture = this._texture, this._texture = null));
    const s3 = i3(this.pixelType);
    this.destroy(), this.data = new s3(t.buffer);
  }
  _resetRange() {
    this.dirtyStart = 2147483647, this.dirtyEnd = 0;
  }
  _initTexture(t) {
    const e = new A(t, this._textureDesc, this.data ?? void 0);
    if (null != this._lastTexture && this._fbos[0]) {
      const i6 = this._lastTexture.descriptor.width, s3 = this._lastTexture.descriptor.height, r2 = this._lastTexture.descriptor.dataType, a = this._lastTexture.descriptor.pixelFormat, n3 = this.getFBO(t), u = o2(r2), h2 = new (i3(r2))(new ArrayBuffer(i6 * s3 * u * this.texelSize)), o3 = t.getBoundFramebufferObject(), { x: d, y: l, width: _, height: c } = t.getViewport();
      t.bindFramebuffer(n3), n3.readPixels(0, 0, i6, s3, a, r2, h2), e.updateData(0, 0, 0, 2 * i6, s3 / 2, h2), t.setViewport(d, l, _, c), t.bindFramebuffer(o3);
    }
    return this.destroy(), this._texture = e, this._texture;
  }
};
var m2 = class {
  constructor() {
    this.size = 0, this._pendingAttributeUpdates = [], this._version = 0, this._epoch = 0, this._locked = false;
  }
  get locked() {
    return this._locked;
  }
  get usedMemory() {
    let t = 0;
    for (const e of this._data ?? []) null != e && (t += e.usedMemory);
    return t;
  }
  _initialize(t) {
    if (!t) throw new Error("InternalError: initArgs must be defined");
    const e = t.blockDescriptors;
    if (this.size = t.blockSize, has("esri-2d-update-debug") && console.debug("AttributeStoreView.initialize", { message: t }), null == this._data) this._data = e.map((t2, e2) => null != t2 ? new b(t2, this.size, e2) : null);
    else for (let i6 = 0; i6 < this._data.length; i6++) {
      const t2 = this._data[i6], s3 = e[i6];
      null != s3 && (null == t2 ? this._data[i6] = new b(s3, this.size, i6) : t2.resize(s3, this.size));
    }
  }
  destroy() {
    for (const t of this._data ?? []) t?.destroy();
    this._defaultTexture?.dispose(), this._defaultTexture = null, this._pendingAttributeUpdates = [];
  }
  isEmpty() {
    return null == this._data;
  }
  getBlock(t) {
    if (null == this._data) return null;
    return this._data[t];
  }
  setLabelMinZoom(t, e) {
    this.setData(t, 0, 1, e);
  }
  setLocalTimeOrigin(t, e) {
    this.setData(t, 7, 0, e);
  }
  getLabelMinZoom(t) {
    return this.getData(t, 0, 1, 255);
  }
  getFilterFlags(t) {
    return this.getData(t, 0, 0, 0);
  }
  getVisualVariableData(t, e) {
    return this.getData(t, 3, e, 0);
  }
  getData(t, e, i6, s3) {
    if (!this._data) return 0;
    const r2 = this._data[e];
    if (null == r2) return 0;
    const a = r2.getData(t, i6);
    return null != a ? a : s3;
  }
  setData(t, e, i6, s3) {
    this._data[e].setData(t, i6, s3);
  }
  lockTextureUploads() {
    this._locked = true;
  }
  unlockTextureUploads() {
    this._locked = false, this.update();
  }
  requestUpdate(t) {
    this._version = t.version, this._pendingAttributeUpdates.push(t), has("esri-2d-update-debug") && console.debug(`Version[${this._version}] AttributeStoreView.requestUpdate`, { message: t });
  }
  get currentEpoch() {
    return this._epoch;
  }
  get hasPendingUpdates() {
    return this._pendingAttributeUpdates.length > 0;
  }
  update() {
    if (this._locked) return;
    const t = this._pendingAttributeUpdates;
    this._pendingAttributeUpdates = [];
    for (const e of t) {
      const { blockData: t2, initArgs: i6, sendUpdateEpoch: s3, version: r2 } = e;
      has("esri-2d-update-debug") && console.debug(`Version[${this._version}] Epoch[${s3}] AttributeStoreView.applyUpdate`), this._version = r2, this._epoch = s3, null != i6 && this._initialize(i6);
      const a = this._data;
      for (let e2 = 0; e2 < t2.length; e2++) {
        const i7 = t2[e2], s4 = a[e2];
        null != s4 && null != i7 && (has("esri-2d-update-debug") && console.debug(`Version[${this._version}] CpuBlock[${e2}] AttributeStoreView.update`, { block: i7 }), s4.update(i7));
      }
    }
  }
  getUniforms(t) {
    return { filterFlags: { texture: this._getTexture(t, 0), unit: D }, animation: { texture: this._getTexture(t, 1), unit: E }, gpgpu: { texture: this._getTexture(t, 2), unit: J }, localTimeOrigin: { texture: this._getTexture(t, 7), unit: K }, visualVariableData: { texture: this._getTexture(t, 3), unit: F }, dataDriven0: { texture: this._getTexture(t, 4), unit: G }, dataDriven1: { texture: this._getTexture(t, 5), unit: H }, dataDriven2: { texture: this._getTexture(t, 6), unit: I }, size: this.size };
  }
  _getTexture(t, e) {
    const i6 = this._data?.[e];
    return i6 ? (i6.updateTexture(t, this._version), i6.getTexture(t)) : this._getDefaultTexture(t);
  }
  _getDefaultTexture(t) {
    if (null == this._defaultTexture) {
      const e = new h(1);
      e.wrapMode = 33071, e.samplingMode = 9728, this._defaultTexture = new A(t, e, new Uint8Array(4));
    }
    return this._defaultTexture;
  }
};

// node_modules/@arcgis/core/views/2d/engine/AFeatureContainer.js
var i4 = 60;
var s2 = class extends n {
  constructor(e) {
    super(e), this._statisticsByLevel = /* @__PURE__ */ new Map(), this._entityIndex = /* @__PURE__ */ new Map(), this.attributeView = new m2();
  }
  destroy() {
    super.destroy(), this.children.forEach((t) => t.destroy()), this.removeAllChildren(), this.attributeView.destroy();
  }
  doRender(t) {
    t.context.capabilities.enable("textureFloatLinear"), super.doRender(t);
  }
  get hasAnimations() {
    for (const t of this.children) if (t.hasAnimations) return true;
    return false;
  }
  _reindexAndUpdateFeaturesIfNeeded() {
    if (this.hasAnimations && (this._reindexFeatures(), 0 !== this.attributeView.size)) for (const t of this._entityIndex.values()) t.dirty && (this.attributeView.setData(t.displayId, 7, 0, t.firstIndexed), t.dirty = false);
  }
  restartAnimation(t) {
    const e = this._entityIndex.get(t);
    if (!e) return;
    const i6 = performance.now() / 1e3;
    e.firstIndexed = i6, e.dirty = true;
  }
  restartAllAnimations() {
    const t = performance.now() / 1e3;
    for (const [e, i6] of this._entityIndex) i6.firstIndexed = t, i6.dirty = true;
  }
  _reindexFeatures() {
    const t = performance.now() / 1e3;
    for (const e of this.children) for (const i6 of e.entityIds) {
      const { objectId: e2 } = i6;
      let s3 = this._entityIndex.get(e2);
      s3 || (s3 = { objectId: e2, displayId: 0, firstIndexed: t, lastIndexed: 0, dirty: true }, this._entityIndex.set(e2, s3)), s3.lastIndexed = t, s3.displayId = i6.displayId;
    }
    for (const [e, s3] of this._entityIndex) t - s3.lastIndexed > i4 && this._entityIndex.delete(e);
  }
  _updateAttributeView() {
    this.attributeView.update(), this._reindexAndUpdateFeaturesIfNeeded();
  }
  createRenderParams(t) {
    const e = super.createRenderParams(t);
    return e.attributeView = this.attributeView, e.instanceStore = this._instanceStore, e.statisticsByLevel = this._statisticsByLevel, e;
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/shaderGraph/techniques/mesh/instanceIdUtils.js
function n2(n3) {
  return n3;
}
function r(n3) {
  return n3;
}

// node_modules/@arcgis/core/views/2d/engine/webgl/shaderGraph/techniques/TechniqueInstance.js
var i5 = class {
  constructor(t, i6, e) {
    this._instanceId = t, this.techniqueRef = i6, this._input = e;
  }
  get instanceId() {
    return n2(this._instanceId);
  }
  createMeshInfo(i6) {
    return { id: n2(this._instanceId), techniqueType: this.techniqueRef.type, inputParams: i6, optionalAttributes: this._input.optionalAttributes };
  }
  getInput() {
    return this._input;
  }
  setInput(t) {
    this._input = t;
  }
};

export {
  s2 as s,
  r,
  i5 as i
};
//# sourceMappingURL=chunk-SCN7WMQJ.js.map
