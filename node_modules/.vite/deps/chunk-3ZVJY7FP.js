import {
  o
} from "./chunk-V4QELPWK.js";
import {
  t
} from "./chunk-AAJTKNPH.js";
import {
  DateTime,
  FixedOffsetZone,
  IANAZone,
  Zone
} from "./chunk-IWHCPKHC.js";
import {
  l
} from "./chunk-M526P4LY.js";
import {
  z
} from "./chunk-F22SQIOE.js";
import {
  r
} from "./chunk-45CZB3H2.js";

// node_modules/@arcgis/core/arcade/ArcadeDate.js
var a = { TimeZoneNotRecognized: "Timezone identifier has not been recognized." };
var d = class _d extends Error {
  constructor(e, n4) {
    super(r(a[e], n4)), this.declaredRootClass = "esri.arcade.arcadedate.dateerror", Error.captureStackTrace && Error.captureStackTrace(this, _d);
  }
};
function u(e, t2, n4) {
  return e < t2 ? e - t2 : e > n4 ? e - n4 : 0;
}
function c(e, t2, n4) {
  return e < t2 ? t2 : e > n4 ? n4 : e;
}
var m = class _m {
  constructor(e) {
    this._date = e, this.declaredRootClass = "esri.arcade.arcadedate";
  }
  static fromParts(e = 0, t2 = 1, n4 = 1, s = 0, i4 = 0, o4 = 0, a2 = 0, d2) {
    if (isNaN(e) || isNaN(t2) || isNaN(n4) || isNaN(s) || isNaN(i4) || isNaN(o4) || isNaN(a2)) return null;
    const l2 = DateTime.local(e, t2).daysInMonth;
    let f = DateTime.fromObject({ day: c(n4, 1, l2), year: e, month: c(t2, 1, 12), hour: c(s, 0, 23), minute: c(i4, 0, 59), second: c(o4, 0, 59), millisecond: c(a2, 0, 999) }, { zone: h(d2) });
    return f = f.plus({ months: u(t2, 1, 12), days: u(n4, 1, l2), hours: u(s, 0, 23), minutes: u(i4, 0, 59), seconds: u(o4, 0, 59), milliseconds: u(a2, 0, 999) }), new _m(f);
  }
  static get systemTimeZoneCanonicalName() {
    return Intl.DateTimeFormat().resolvedOptions().timeZone ?? "system";
  }
  static arcadeDateAndZoneToArcadeDate(e, t2) {
    const r3 = h(t2);
    return e.isUnknownTimeZone || r3 === o.instance ? _m.fromParts(e.year, e.monthJS + 1, e.day, e.hour, e.minute, e.second, e.millisecond, r3) : new _m(e._date.setZone(r3));
  }
  static dateJSToArcadeDate(e) {
    return new _m(DateTime.fromJSDate(e, { zone: "system" }));
  }
  static dateJSAndZoneToArcadeDate(e, t2 = "system") {
    const n4 = h(t2);
    return new _m(DateTime.fromJSDate(e, { zone: n4 }));
  }
  static unknownEpochToArcadeDate(e) {
    return new _m(DateTime.fromMillis(e, { zone: o.instance }));
  }
  static unknownDateJSToArcadeDate(e) {
    return new _m(DateTime.fromMillis(e.getTime(), { zone: o.instance }));
  }
  static epochToArcadeDate(e, t2 = "system") {
    const n4 = h(t2);
    return new _m(DateTime.fromMillis(e, { zone: n4 }));
  }
  static dateTimeToArcadeDate(e) {
    return new _m(e);
  }
  clone() {
    return new _m(this._date);
  }
  changeTimeZone(e) {
    const t2 = h(e);
    return _m.dateTimeToArcadeDate(this._date.setZone(t2));
  }
  static dateTimeAndZoneToArcadeDate(e, t2) {
    const r3 = h(t2);
    return e.zone === o.instance || r3 === o.instance ? _m.fromParts(e.year, e.month, e.day, e.hour, e.minute, e.second, e.millisecond, r3) : new _m(e.setZone(r3));
  }
  static nowToArcadeDate(e) {
    const t2 = h(e);
    return new _m(DateTime.fromJSDate(/* @__PURE__ */ new Date(), { zone: t2 }));
  }
  static nowUTCToArcadeDate() {
    return new _m(DateTime.utc());
  }
  get isSystem() {
    return "system" === this.timeZone || this.timeZone === _m.systemTimeZoneCanonicalName;
  }
  equals(e) {
    return this.isSystem && e.isSystem ? this.toNumber() === e.toNumber() : this.isUnknownTimeZone === e.isUnknownTimeZone && this._date.equals(e._date);
  }
  get isUnknownTimeZone() {
    return this._date.zone === o.instance;
  }
  get isValid() {
    return this._date.isValid;
  }
  get hour() {
    return this._date.hour;
  }
  get second() {
    return this._date.second;
  }
  get day() {
    return this._date.day;
  }
  get dayOfWeekISO() {
    return this._date.weekday;
  }
  get dayOfWeekJS() {
    let e = this._date.weekday;
    return e > 6 && (e = 0), e;
  }
  get millisecond() {
    return this._date.millisecond;
  }
  get monthISO() {
    return this._date.month;
  }
  get weekISO() {
    return this._date.weekNumber;
  }
  get yearISO() {
    return this._date.weekYear;
  }
  get monthJS() {
    return this._date.month - 1;
  }
  get year() {
    return this._date.year;
  }
  get minute() {
    return this._date.minute;
  }
  get zone() {
    return this._date.zone;
  }
  get timeZoneOffset() {
    return this.isUnknownTimeZone ? 0 : this._date.offset;
  }
  get timeZone() {
    if (this.isUnknownTimeZone) return "unknown";
    if ("system" === this._date.zone.type) return "system";
    const e = this.zone;
    return "fixed" === e.type ? 0 === e.fixed ? "UTC" : e.formatOffset(0, "short") : e.name;
  }
  stringify() {
    return JSON.stringify(this.toJSDate());
  }
  plus(e) {
    return new _m(this._date.plus(e));
  }
  diff(e, t2) {
    return this._date.diff(e._date, t2)[t2];
  }
  toISODate() {
    return this._date.toISODate();
  }
  toISOString(e) {
    return e ? this._date.toISO({ suppressMilliseconds: true, includeOffset: !this.isUnknownTimeZone }) : this._date.toISO({ includeOffset: !this.isUnknownTimeZone });
  }
  toISOTime(e, t2) {
    return this._date.toISOTime({ suppressMilliseconds: e, includeOffset: t2 && !this.isUnknownTimeZone });
  }
  toFormat(e, t2) {
    return this.isUnknownTimeZone && (e = e.replaceAll("Z", "")), this._date.toFormat(e, t2);
  }
  toJSDate() {
    return this._date.toJSDate();
  }
  toSQLValue() {
    return this._date.toFormat("yyyy-LL-dd HH:mm:ss");
  }
  toSQLWithKeyword() {
    return `timestamp '${this.toSQLValue()}'`;
  }
  toDateTime() {
    return this._date;
  }
  toNumber() {
    return this._date.toMillis();
  }
  getTime() {
    return this._date.toMillis();
  }
  toUTC() {
    return new _m(this._date.toUTC());
  }
  toLocal() {
    return new _m(this._date.toLocal());
  }
  toString() {
    return this.toISOString(true);
  }
  static fromReaderAsTimeStampOffset(e) {
    if (!e) return null;
    const t2 = DateTime.fromISO(e, { setZone: true });
    return new _m(t2);
  }
};
function h(t2, r3 = true) {
  if (t2 instanceof Zone) return t2;
  switch (t(t2)) {
    case "system":
      return "system";
    case "utc":
      return "UTC";
    case "unknown":
      return o.instance;
  }
  if (/^[+-]?[0-9]{1,2}(:[0-9]{2})?$/.test(t2)) {
    const e = FixedOffsetZone.parseSpecifier("UTC" + (t2.startsWith("+") || t2.startsWith("-") ? "" : "+") + t2);
    if (e) return e;
  }
  const a2 = IANAZone.create(t2);
  if (!a2.isValid) {
    if (r3) throw new d("TimeZoneNotRecognized");
    return null;
  }
  return a2;
}

// node_modules/@arcgis/core/core/sql/DateOnly.js
function o2(t2) {
  t2 = t2.replaceAll(/LTS|LT|L{1,4}|l{1,4}/g, "[$&]");
  let e = "";
  const a2 = /(\[[^[]*\])|(\\)?([Hh]mm(ss)?|Mo|M{1,4}|Do|DDDo|D{1,4}|d{2,4}|do?|w[o|w]?|W[o|W]?|Qo?|N{1,5}|YYYYYY|YYYYY|YYYY|YY|y{2,4}|yo?|gg(ggg?)?|GG(GGG?)?|e|E|a|A|hh?|HH?|kk?|mm?|ss?|S{1,9}|x|X|zz?|ZZ?|.)/g;
  for (const r3 of t2.match(a2) || []) switch (r3) {
    case "D":
      e += "d";
      break;
    case "DD":
      e += "dd";
      break;
    case "DDD":
      e += "o";
      break;
    case "d":
      e += "c";
      break;
    case "ddd":
      e += "ccc";
      break;
    case "dddd":
      e += "cccc";
      break;
    case "M":
      e += "L";
      break;
    case "MM":
      e += "LL";
      break;
    case "MMM":
      e += "LLL";
      break;
    case "MMMM":
      e += "LLLL";
      break;
    case "YY":
      e += "yy";
      break;
    case "Y":
    case "YYYY":
      e += "yyyy";
      break;
    case "Q":
      e += "q";
      break;
    case "X":
    case "x":
      e += r3;
      break;
    default:
      r3.length >= 2 && r3.startsWith("[") && r3.endsWith("]") ? e += `'${r3.slice(1, -1)}'` : e += `'${r3}'`;
  }
  return e;
}
var n = "esri.core.sql.dateonly";
var i = class _i {
  constructor(t2, e, a2) {
    this._year = t2, this._month = e, this._day = a2, this.declaredRootClass = n;
  }
  static isDateOnly(t2) {
    return "object" == typeof t2 && null != t2 && "declaredRootClass" in t2 && t2.declaredRootClass === n;
  }
  get month() {
    return this._month;
  }
  get monthJS() {
    return this._month - 1;
  }
  get year() {
    return this._year;
  }
  get day() {
    return this._day;
  }
  get isValid() {
    return this.toDateTime("unknown").isValid;
  }
  equals(t2) {
    return _i.isDateOnly(t2) && t2.day === this.day && t2.month === this.month && t2.year === this.year;
  }
  clone() {
    return new _i(this._year, this._month, this._day);
  }
  toDateTime(e) {
    return DateTime.fromObject({ day: this.day, month: this.month, year: this.year }, { zone: h(e) });
  }
  toDateTimeLuxon(e) {
    return DateTime.fromObject({ day: this.day, month: this.month, year: this.year }, { zone: h(e) });
  }
  toString() {
    return `${this.year.toString().padStart(4, "0")}-${this.month.toString().padStart(2, "0")}-${this.day.toString().padStart(2, "0")}`;
  }
  toFormat(t2 = null, r3 = true) {
    if (null === t2 || "" === t2) return this.toString();
    if (r3 && (t2 = o2(t2)), !t2) return "";
    const s = this.toDateTime("unknown");
    return m.dateTimeToArcadeDate(s).toFormat(t2, { locale: l(), numberingSystem: "latn" });
  }
  toArcadeDate() {
    const t2 = this.toDateTime("unknown");
    return m.dateTimeToArcadeDate(t2);
  }
  toNumber() {
    return this.toDateTime("unknown").toMillis();
  }
  toJSDate() {
    return this.toDateTime("unknown").toJSDate();
  }
  toStorageFormat() {
    return this.toFormat("yyyy-LL-dd", false);
  }
  toSQLValue() {
    return this.toFormat("yyyy-LL-dd", false);
  }
  toSQLWithKeyword() {
    return "date '" + this.toFormat("yyyy-LL-dd", false) + "'";
  }
  plus(t2, e) {
    return _i.fromDateTime(this.toUTCDateTime().plus({ [t2]: e }));
  }
  toUTCDateTime() {
    return DateTime.utc(this.year, this.month, this.day, 0, 0, 0, 0);
  }
  difference(t2, e) {
    switch (e.toLowerCase()) {
      case "days":
      case "day":
      case "d":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "days").days;
      case "months":
      case "month":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "months").months;
      case "minutes":
      case "minute":
      case "m":
        return "M" === e ? this.toUTCDateTime().diff(t2.toUTCDateTime(), "months").months : this.toUTCDateTime().diff(t2.toUTCDateTime(), "minutes").minutes;
      case "seconds":
      case "second":
      case "s":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "seconds").seconds;
      case "milliseconds":
      case "millisecond":
      case "ms":
      default:
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "milliseconds").milliseconds;
      case "hours":
      case "hour":
      case "h":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "hours").hours;
      case "years":
      case "year":
      case "y":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "years").years;
    }
  }
  static fromMilliseconds(t2) {
    const e = DateTime.fromMillis(t2, { zone: FixedOffsetZone.utcInstance });
    return e.isValid ? _i.fromParts(e.year, e.month, e.day) : null;
  }
  static fromSeconds(t2) {
    const e = DateTime.fromSeconds(t2, { zone: FixedOffsetZone.utcInstance });
    return e.isValid ? _i.fromParts(e.year, e.month, e.day) : null;
  }
  static fromReader(t2) {
    if (!t2) return null;
    const e = t2.split("-");
    return 3 !== e.length ? null : new _i(parseInt(e[0], 10), parseInt(e[1], 10), parseInt(e[2], 10));
  }
  static fromParts(t2, e, a2) {
    const r3 = new _i(t2, e, a2);
    return false === r3.isValid ? null : r3;
  }
  static fromDateJS(t2) {
    return _i.fromParts(t2.getFullYear(), t2.getMonth() + 1, t2.getDay());
  }
  static fromDateTime(t2) {
    return _i.fromParts(t2.year, t2.month, t2.day);
  }
  static fromSqlTimeStampOffset(t2) {
    return this.fromDateTime(t2.toDateTime());
  }
  static fromString(t2, e = null) {
    if ("" === t2) return null;
    if (null === t2) return null;
    const a2 = [];
    if (e) (e = o2(e)) && a2.push(e);
    else if (null === e || "" === e) {
      const e2 = DateTime.fromISO(t2, { setZone: true });
      return e2.isValid ? _i.fromParts(e2.year, e2.month, e2.day) : null;
    }
    for (const s of a2) {
      const a3 = DateTime.fromFormat(t2, e ?? s);
      if (a3.isValid) return new _i(a3.year, a3.month, a3.day);
    }
    return null;
  }
  static fromNow(e = "system") {
    const a2 = DateTime.fromJSDate(/* @__PURE__ */ new Date()).setZone(h(e));
    return new _i(a2.year, a2.month, a2.day);
  }
};

// node_modules/@arcgis/core/arcade/kernel.js
function n2(n4) {
  if (null == n4) return null;
  switch (n4.type) {
    case "polygon":
    case "multipoint":
    case "polyline":
      return n4.extent;
    case "point":
      return new z({ xmin: n4.x, ymin: n4.y, xmax: n4.x, ymax: n4.y, spatialReference: n4.spatialReference });
    case "extent":
      return n4;
  }
  return null;
}
function o3(e) {
  if (null == e) return null;
  const n4 = e.clone();
  return void 0 !== e.cache._geVersion && (n4.cache._geVersion = e.cache._geVersion), n4;
}
function i2(e) {
  return "number" == typeof e && isFinite(e) && Math.floor(e) === e;
}

// node_modules/@arcgis/core/core/sql/TimeOnly.js
function i3(t2) {
  if (!t2) return "";
  const s = /(a|A|hh?|HH?|mm?|ss?|SSS|S|.)/g;
  let e = "";
  for (const i4 of t2.match(s) || []) switch (i4) {
    case "SSS":
    case "m":
    case "mm":
    case "h":
    case "hh":
    case "H":
    case "HH":
    case "s":
    case "ss":
      e += i4;
      break;
    case "A":
    case "a":
      e += "a";
      break;
    default:
      e += `'${i4}'`;
  }
  return e;
}
var n3 = "esri.core.sql.timeonly";
var r2 = class _r {
  constructor(t2, s, e, i4) {
    this._hour = t2, this._minute = s, this._second = e, this._millisecond = i4, this.declaredRootClass = n3;
  }
  static isTimeOnly(t2) {
    return "object" == typeof t2 && null != t2 && "declaredRootClass" in t2 && t2.declaredRootClass === n3;
  }
  get hour() {
    return this._hour;
  }
  get minute() {
    return this._minute;
  }
  get second() {
    return this._second;
  }
  get millisecond() {
    return this._millisecond;
  }
  equals(t2) {
    return _r.isTimeOnly(t2) && t2.hour === this.hour && t2.minute === this.minute && t2.second === this.second && t2.millisecond === this.millisecond;
  }
  clone() {
    return new _r(this.hour, this.minute, this.second, this.millisecond);
  }
  isValid() {
    return i2(this.hour) && i2(this.minute) && i2(this.second) && i2(this.millisecond) && this.hour >= 0 && this.hour < 24 && this.minute >= 0 && this.minute < 60 && this.second >= 0 && this.second < 60 && this.millisecond >= 0 && this.millisecond < 1e3;
  }
  toString() {
    return `${this.hour.toString().padStart(2, "0")}:${this.minute.toString().padStart(2, "0")}:${this.second.toString().padStart(2, "0")}` + (this.millisecond > 0 ? "." + this.millisecond.toString().padStart(3, "0") : "");
  }
  toSQLValue() {
    return this.toString();
  }
  toSQLWithKeyword() {
    return `time '${this.hour.toString().padStart(2, "0")}:${this.minute.toString().padStart(2, "0")}:${this.second.toString().padStart(2, "0")}${this.millisecond > 0 ? "." + this.millisecond.toString().padStart(3, "0") : ""}'`;
  }
  toStorageString() {
    return `${this.hour.toString().padStart(2, "0")}:${this.minute.toString().padStart(2, "0")}:${this.second.toString().padStart(2, "0")}`;
  }
  toFormat(t2 = null) {
    if (null === t2 || "" === t2) return this.toString();
    if (!(t2 = i3(t2))) return "";
    return DateTime.local(1970, 1, 1, this._hour, this._minute, this._second, this._millisecond).toFormat(t2, { locale: l(), numberingSystem: "latn" });
  }
  toNumber() {
    return this.millisecond + 1e3 * this.second + 1e3 * this.minute * 60 + 60 * this.hour * 60 * 1e3;
  }
  static fromParts(t2, s, e, i4) {
    const n4 = new _r(t2, s, e, i4);
    return n4.isValid() ? n4 : null;
  }
  static fromReader(t2) {
    if (!t2) return null;
    const s = t2.split(":");
    return 3 !== s.length ? null : new _r(parseInt(s[0], 10), parseInt(s[1], 10), parseInt(s[2], 10), 0);
  }
  static fromMilliseconds(t2) {
    if (t2 > 864e5 || t2 < 0) return null;
    const s = Math.floor(t2 / 1e3 % 60), e = Math.floor(t2 / 6e4 % 60), i4 = Math.floor(t2 / 36e5 % 24), n4 = Math.floor(t2 % 1e3);
    return new _r(i4, e, s, n4);
  }
  static fromDateJS(t2) {
    return new _r(t2.getHours(), t2.getMinutes(), t2.getSeconds(), t2.getMilliseconds());
  }
  static fromDateTime(t2) {
    return new _r(t2.hour, t2.minute, t2.second, t2.millisecond);
  }
  static fromSqlTimeStampOffset(t2) {
    return this.fromDateTime(t2.toDateTime());
  }
  static fromString(t2, s = null) {
    if ("" === t2) return null;
    if (null === t2) return null;
    const n4 = [];
    s ? (s = i3(s)) && n4.push(s) : null !== s && "" !== s || (n4.push("HH:mm:ss"), n4.push("HH:mm:ss.SSS"), n4.push("hh:mm:ss a"), n4.push("hh:mm:ss.SSS a"), n4.push("HH:mm"), n4.push("hh:mm a"), n4.push("H:mm"), n4.push("h:mm a"), n4.push("H:mm:ss"), n4.push("h:mm:ss a"), n4.push("H:mm:ss.SSS"), n4.push("h:mm:ss.SSS a"));
    for (const i4 of n4) {
      const s2 = DateTime.fromFormat(t2, i4);
      if (s2.isValid) return new _r(s2.hour, s2.minute, s2.second, s2.millisecond);
    }
    return null;
  }
  plus(t2, s) {
    switch (t2) {
      case "days":
      case "years":
      case "months":
        return this.clone();
      case "hours":
      case "minutes":
      case "seconds":
      case "milliseconds":
        return _r.fromDateTime(this.toUTCDateTime().plus({ [t2]: s }));
    }
    return null;
  }
  toUTCDateTime() {
    return DateTime.utc(1970, 1, 1, this.hour, this.minute, this.second, this.millisecond);
  }
  difference(t2, s) {
    switch (s.toLowerCase()) {
      case "days":
      case "day":
      case "d":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "days").days;
      case "months":
      case "month":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "months").months;
      case "minutes":
      case "minute":
      case "m":
        return "M" === s ? this.toUTCDateTime().diff(t2.toUTCDateTime(), "months").months : this.toUTCDateTime().diff(t2.toUTCDateTime(), "minutes").minutes;
      case "seconds":
      case "second":
      case "s":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "seconds").seconds;
      case "milliseconds":
      case "millisecond":
      case "ms":
      default:
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "milliseconds").milliseconds;
      case "hours":
      case "hour":
      case "h":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "hours").hours;
      case "years":
      case "year":
      case "y":
        return this.toUTCDateTime().diff(t2.toUTCDateTime(), "years").years;
    }
  }
};

export {
  m,
  h,
  i,
  n2 as n,
  o3 as o,
  r2 as r
};
//# sourceMappingURL=chunk-3ZVJY7FP.js.map
