import {
  r as r4
} from "./chunk-QUUJ56MS.js";
import {
  h
} from "./chunk-LVC5F5JX.js";
import {
  r as r3
} from "./chunk-HMK33QLO.js";
import {
  e
} from "./chunk-U3B7YMY7.js";
import {
  e as e2
} from "./chunk-Y65YJSC2.js";
import {
  n,
  r as r2
} from "./chunk-UYMV6HXW.js";
import {
  R,
  _
} from "./chunk-QNVJVDYZ.js";
import {
  $,
  Y,
  Z
} from "./chunk-JE2NJSBU.js";
import {
  i
} from "./chunk-O5NPUMOD.js";
import {
  r
} from "./chunk-JMK7P26I.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrush.js
var t = class {
  constructor() {
    this.name = this.constructor.name || "UnnamedBrush", this.brushEffect = null;
  }
  prepareState(t3, r7) {
  }
  draw(t3, r7, s) {
  }
  drawMany(t3, r7, s) {
    for (const a3 of r7) a3.visible && this.draw(t3, a3, s);
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLBackground.js
var m = class extends t {
  constructor() {
    super(...arguments), this._color = r4(1, 0, 0, 1), this._patternMatrix = e2(), this._programOptions = { id: false, pattern: false };
  }
  dispose() {
    this._vao = r(this._vao);
  }
  drawMany(r7, e4) {
    const { context: o, painter: a3, requestRender: c3, allowDelayedRender: l2 } = r7;
    this._loadWGLResources(r7);
    const m2 = r7.displayLevel, f2 = r7.styleLayer, u2 = f2.backgroundMaterial, p = a3.vectorTilesMaterialManager, _2 = f2.getPaintValue("background-color", m2), d = f2.getPaintValue("background-opacity", m2), g = f2.getPaintValue("background-pattern", m2), h2 = void 0 !== g, x = 1 | window.devicePixelRatio, y = r7.spriteMosaic;
    let M, b;
    const v = x > $ ? 2 : 1, w = this._programOptions;
    w.pattern = h2;
    const j = p.getMaterialProgram(o, u2, w);
    if (!l2 || null == c3 || j.compiled) {
      if (o.bindVAO(this._vao), o.useProgram(j), h2) {
        const t3 = y.getMosaicItemPosition(g, true);
        if (null != t3) {
          const { tl: r8, br: e5, page: s } = t3;
          M = e5[0] - r8[0], b = e5[1] - r8[1];
          const a4 = y.getPageSize(s);
          null != a4 && (y.bind(o, 9729, s, Y), j.setUniform4f("u_tlbr", r8[0], r8[1], e5[0], e5[1]), j.setUniform2fv("u_mosaicSize", a4), j.setUniform1i("u_texture", Y));
        }
        j.setUniform1f("u_opacity", d);
      } else {
        const t3 = _2[3] * d;
        this._color[0] = t3 * _2[0], this._color[1] = t3 * _2[1], this._color[2] = t3 * _2[2], this._color[3] = t3, j.setUniform4fv("u_color", this._color);
      }
      j.setUniform1f("u_depth", f2.z || 0);
      for (const r8 of e4) {
        if (j.setUniform1f("u_coord_range", r8.rangeX), j.setUniformMatrix3fv("u_dvsMat3", r8.transforms.displayViewScreenMat3), h2) {
          const e5 = Math.max(2 ** (Math.round(m2) - r8.key.level), 1), o2 = v * r8.width * e5, i2 = o2 / i(M), s = o2 / i(b);
          this._patternMatrix[0] = i2, this._patternMatrix[4] = s, j.setUniformMatrix3fv("u_pattern_matrix", this._patternMatrix);
        }
        o.setStencilFunction(514, 0, 255), o.drawArrays(_.TRIANGLE_STRIP, 0, 4);
      }
    } else c3();
  }
  _loadWGLResources(t3) {
    if (this._vao) return;
    const { context: r7, styleLayer: e4 } = t3, o = e4.backgroundMaterial, i2 = new Int8Array([0, 0, 1, 0, 0, 1, 1, 1]), s = new r3(r7, o.geometryLayout, i2);
    this._vao = new h(r7, s);
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLCircle.js
var a = class extends t {
  constructor() {
    super(...arguments), this._programOptions = { id: false };
  }
  dispose() {
  }
  drawMany(e4, a3) {
    const { context: i2, displayLevel: n3, requiredLevel: l2, state: s, painter: o, spriteMosaic: c3, styleLayerUID: u2, requestRender: d, allowDelayedRender: f2 } = e4;
    if (!a3.some((e5) => e5.layerData.get(u2)?.circleIndexCount ?? false)) return;
    const p = e4.styleLayer, m2 = p.circleMaterial, y = o.vectorTilesMaterialManager, g = 1.2, M = p.getPaintValue("circle-translate", n3), v = p.getPaintValue("circle-translate-anchor", n3), x = this._programOptions, _2 = y.getMaterialProgram(i2, m2, x);
    if (f2 && null != d && !_2.compiled) return void d();
    i2.useProgram(_2), _2.setUniformMatrix3fv("u_displayMat3", 1 === v ? s.displayMat3 : s.displayViewMat3), _2.setUniform2fv("u_circleTranslation", M), _2.setUniform1f("u_depth", p.z), _2.setUniform1f("u_antialiasingWidth", g);
    let I = -1;
    for (const U of a3) {
      if (!U.layerData.has(u2)) continue;
      U.key.level !== I && (I = U.key.level, m2.setDataUniforms(_2, n3, p, I, c3));
      const e5 = U.layerData.get(u2);
      if (!e5.circleIndexCount) continue;
      e5.prepareForRendering(i2);
      const a4 = e5.vao;
      null != a4 && (i2.bindVAO(a4), _2.setUniformMatrix3fv("u_dvsMat3", U.transforms.displayViewScreenMat3), l2 !== U.key.level ? i2.setStencilFunction(514, U.stencilRef, 255) : i2.setStencilFunction(516, 255, 255), i2.drawElements(_.TRIANGLES, e5.circleIndexCount, R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * e5.circleIndexStart), U.triangleCount += e5.circleIndexCount / 3);
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLFill.js
var l = 1 / 65536;
var r5 = class extends t {
  constructor() {
    super(...arguments), this._fillProgramOptions = { id: false, pattern: false }, this._outlineProgramOptions = { id: false };
  }
  dispose() {
  }
  drawMany(t3, e4) {
    const { displayLevel: i2, renderPass: n3, spriteMosaic: a3, styleLayerUID: l2 } = t3;
    let r7 = false;
    for (const _2 of e4) if (_2.layerData.has(l2)) {
      const t4 = _2.layerData.get(l2);
      if (t4.fillIndexCount > 0 || t4.outlineIndexCount > 0) {
        r7 = true;
        break;
      }
    }
    if (!r7) return;
    const o = t3.styleLayer, s = o.getPaintProperty("fill-pattern"), f2 = void 0 !== s, u2 = f2 && s.isDataDriven;
    let c3;
    if (f2 && !u2) {
      const t4 = s.getValue(i2);
      c3 = a3.getMosaicItemPosition(t4, true);
    }
    const d = !f2 && o.getPaintValue("fill-antialias", i2);
    let p = true, m2 = 1;
    if (!f2) {
      const t4 = o.getPaintProperty("fill-color"), e5 = o.getPaintProperty("fill-opacity");
      if (!t4?.isDataDriven && !e5?.isDataDriven) {
        const t5 = o.getPaintValue("fill-color", i2);
        m2 = o.getPaintValue("fill-opacity", i2) * t5[3], m2 >= 1 && (p = false);
      }
    }
    if (p && "opaque" === n3) return;
    const y = o.getPaintValue("fill-translate", i2), g = o.getPaintValue("fill-translate-anchor", i2);
    (p || "translucent" !== n3) && this._drawFill(t3, l2, o, e4, y, g, f2, c3, u2);
    const M = !o.hasDataDrivenOutlineColor && o.outlineUsesFillColor && m2 < 1;
    d && "opaque" !== n3 && !M && this._drawOutline(t3, l2, o, e4, y, g);
  }
  _drawFill(i2, r7, o, s, f2, u2, c3, d, p) {
    if (c3 && !p && null == d) return;
    const { context: m2, displayLevel: y, state: g, painter: M, pixelRatio: _2, spriteMosaic: v, requestRender: P, allowDelayedRender: E } = i2, U = o.fillMaterial, x = M.vectorTilesMaterialManager, D = _2 > $ ? 2 : 1, I = this._fillProgramOptions;
    I.pattern = c3;
    const S = x.getMaterialProgram(m2, U, I);
    if (E && null != P && !S.compiled) return void P();
    if (m2.useProgram(S), null != d) {
      const { page: e4 } = d, i3 = v.getPageSize(e4);
      null != i3 && (v.bind(m2, 9729, e4, Y), S.setUniform2fv("u_mosaicSize", i3), S.setUniform1i("u_texture", Y));
    }
    S.setUniformMatrix3fv("u_displayMat3", 1 === u2 ? g.displayMat3 : g.displayViewMat3), S.setUniform2fv("u_fillTranslation", f2), S.setUniform1f("u_depth", o.z + l);
    let h2 = -1;
    for (const e4 of s) {
      if (!e4.layerData.has(r7)) continue;
      e4.key.level !== h2 && (h2 = e4.key.level, U.setDataUniforms(S, y, o, h2, v));
      const i3 = e4.layerData.get(r7);
      if (!i3.fillIndexCount) continue;
      i3.prepareForRendering(m2);
      const l2 = i3.fillVAO;
      if (null != l2) {
        if (m2.bindVAO(l2), S.setUniformMatrix3fv("u_dvsMat3", e4.transforms.displayViewScreenMat3), m2.setStencilFunction(514, e4.stencilRef, 255), c3) {
          const t3 = Math.max(2 ** (Math.round(y) - e4.key.level), 1), i4 = e4.rangeX / (D * e4.width * t3);
          S.setUniform1f("u_patternFactor", i4);
        }
        if (p) {
          const e5 = i3.patternMap;
          if (!e5) continue;
          for (const [i4, l3] of e5) {
            const e6 = v.getPageSize(i4);
            null != e6 && (v.bind(m2, 9729, i4, Y), S.setUniform2fv("u_mosaicSize", e6), S.setUniform1i("u_texture", Y), m2.drawElements(_.TRIANGLES, l3[1], R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * l3[0]));
          }
        } else m2.drawElements(_.TRIANGLES, i3.fillIndexCount, R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * i3.fillIndexStart);
        e4.triangleCount += i3.fillIndexCount / 3;
      }
    }
  }
  _drawOutline(t3, e4, i2, r7, o, s) {
    const { context: f2, displayLevel: u2, state: c3, painter: d, pixelRatio: p, spriteMosaic: m2, requestRender: y, allowDelayedRender: g } = t3, M = i2.outlineMaterial, _2 = d.vectorTilesMaterialManager, v = 0.75 / p, P = this._outlineProgramOptions, E = _2.getMaterialProgram(f2, M, P);
    if (g && null != y && !E.compiled) return void y();
    f2.useProgram(E), E.setUniformMatrix3fv("u_displayMat3", 1 === s ? c3.displayMat3 : c3.displayViewMat3), E.setUniform2fv("u_fillTranslation", o), E.setUniform1f("u_depth", i2.z + l), E.setUniform1f("u_outline_width", v);
    let U = -1;
    for (const l2 of r7) {
      if (!l2.layerData.has(e4)) continue;
      l2.key.level !== U && (U = l2.key.level, M.setDataUniforms(E, u2, i2, U, m2));
      const t4 = l2.layerData.get(e4);
      if (t4.prepareForRendering(f2), !t4.outlineIndexCount) continue;
      const r8 = t4.outlineVAO;
      null != r8 && (f2.bindVAO(r8), E.setUniformMatrix3fv("u_dvsMat3", l2.transforms.displayViewScreenMat3), f2.setStencilFunction(514, l2.stencilRef, 255), f2.drawElements(_.TRIANGLES, t4.outlineIndexCount, R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t4.outlineIndexStart), l2.triangleCount += t4.outlineIndexCount / 3);
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLLine.js
var a2 = class extends t {
  constructor() {
    super(...arguments), this._programOptions = { id: false, pattern: false, sdf: false };
  }
  dispose() {
  }
  drawMany(t3, a3) {
    const { context: r7, displayLevel: o, state: s, painter: l2, pixelRatio: f2, spriteMosaic: u2, styleLayerUID: c3, requestRender: d, allowDelayedRender: m2 } = t3;
    if (!a3.some((e4) => e4.layerData.get(c3)?.lineIndexCount ?? false)) return;
    const p = t3.styleLayer, g = p.lineMaterial, y = l2.vectorTilesMaterialManager, M = p.getPaintValue("line-translate", o), _2 = p.getPaintValue("line-translate-anchor", o), v = p.getPaintProperty("line-pattern"), U = void 0 !== v, E = U && v.isDataDriven;
    let x, I;
    if (U && !E) {
      const e4 = v.getValue(o);
      x = u2.getMosaicItemPosition(e4);
    }
    let P = false;
    if (!U) {
      const e4 = p.getPaintProperty("line-dasharray");
      if (I = void 0 !== e4, P = I && e4.isDataDriven, I && !P) {
        const t4 = e4.getValue(o), i2 = p.getDashKey(t4, p.getLayoutValue("line-cap", o));
        x = u2.getMosaicItemPosition(i2);
      }
    }
    const D = 1 / f2, S = this._programOptions;
    S.pattern = U, S.sdf = I;
    const N = y.getMaterialProgram(r7, g, S);
    if (m2 && null != d && !N.compiled) return void d();
    if (r7.useProgram(N), N.setUniformMatrix3fv("u_displayViewMat3", s.displayViewMat3), N.setUniformMatrix3fv("u_displayMat3", 1 === _2 ? s.displayMat3 : s.displayViewMat3), N.setUniform2fv("u_lineTranslation", M), N.setUniform1f("u_depth", p.z), N.setUniform1f("u_antialiasing", D), x && null != x) {
      const { page: t4 } = x, i2 = u2.getPageSize(t4);
      null != i2 && (u2.bind(r7, 9729, t4, Y), N.setUniform2fv("u_mosaicSize", i2), N.setUniform1i("u_texture", Y));
    }
    let T = -1;
    for (const V of a3) {
      if (!V.layerData.has(c3)) continue;
      V.key.level !== T && (T = V.key.level, g.setDataUniforms(N, o, p, T, u2));
      const t4 = 2 ** (o - T) / f2;
      N.setUniform1f("u_zoomFactor", t4);
      const a4 = V.layerData.get(c3);
      if (!a4.lineIndexCount) continue;
      a4.prepareForRendering(r7);
      const s2 = a4.vao;
      if (null != s2) {
        if (r7.bindVAO(s2), N.setUniformMatrix3fv("u_dvsMat3", V.transforms.displayViewScreenMat3), r7.setStencilFunction(514, V.stencilRef, 255), E || P) {
          const t5 = a4.patternMap;
          if (!t5) continue;
          for (const [a5, o2] of t5) {
            const t6 = u2.getPageSize(a5);
            null != t6 && (u2.bind(r7, 9729, a5, Y), N.setUniform2fv("u_mosaicSize", t6), N.setUniform1i("u_texture", Y), r7.drawElements(_.TRIANGLES, o2[1], R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * o2[0]));
          }
        } else r7.drawElements(_.TRIANGLES, a4.lineIndexCount, R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * a4.lineIndexStart);
        V.triangleCount += a4.lineIndexCount / 3;
      }
    }
  }
};

// node_modules/@arcgis/core/views/2d/engine/webgl/GeometryUtils.js
var n2 = 128 / Math.PI;
var t2 = 256 / 360;
var r6 = 1 / Math.LN2;
function u(n3, t3) {
  return (n3 %= t3) >= 0 ? n3 : n3 + t3;
}
function c(n3) {
  return u(n3 * t2, 256);
}
function e3(n3) {
  return Math.log(n3) * r6;
}

// node_modules/@arcgis/core/views/2d/engine/webgl/brushes/WGLBrushVTLSymbol.js
var f = 1 / 65536;
var c2 = class extends t {
  constructor() {
    super(...arguments), this._iconProgramOptions = { id: false, sdf: false }, this._sdfProgramOptions = { id: false }, this._spritesTextureSize = n();
  }
  dispose() {
  }
  drawMany(e4, t3) {
    const i2 = e4.styleLayer;
    this._drawIcons(e4, i2, t3), this._drawText(e4, i2, t3);
  }
  _drawIcons(e4, t3, r7) {
    const { context: s, displayLevel: o, painter: l2, spriteMosaic: f2, state: c3, styleLayerUID: u2, requestRender: p, allowDelayedRender: m2 } = e4, g = t3.iconMaterial, d = l2.vectorTilesMaterialManager;
    let y, _2 = false;
    for (const i2 of r7) if (i2.layerData.has(u2) && (y = i2.layerData.get(u2), y.iconPerPageElementsMap.size > 0)) {
      _2 = true;
      break;
    }
    if (!_2) return;
    const h2 = t3.getPaintValue("icon-translate", o), M = t3.getPaintValue("icon-translate-anchor", o);
    let U = t3.getLayoutValue("icon-rotation-alignment", o);
    2 === U && (U = 0 === t3.getLayoutValue("symbol-placement", o) ? 1 : 0);
    const x = 0 === U, P = t3.getLayoutValue("icon-keep-upright", o) && x, S = y.isIconSDF, E = this._iconProgramOptions;
    E.sdf = S;
    const v = d.getMaterialProgram(s, g, E);
    if (m2 && null != p && !v.compiled) return void p();
    s.useProgram(v), v.setUniformMatrix3fv("u_displayViewMat3", 0 === U ? c3.displayViewMat3 : c3.displayMat3), v.setUniformMatrix3fv("u_displayMat3", 1 === M ? c3.displayMat3 : c3.displayViewMat3), v.setUniform2fv("u_iconTranslation", h2), v.setUniform1f("u_depth", t3.z), v.setUniform1f("u_mapRotation", c(c3.rotation)), v.setUniform1f("u_keepUpright", P ? 1 : 0), v.setUniform1f("u_level", 10 * o), v.setUniform1i("u_texture", Y), v.setUniform1f("u_fadeDuration", e / 1e3), v.setUniform1i("u_isStencilPass", e4.stencilSymbols ? 1 : 0);
    let D = -1;
    for (const i2 of r7) {
      if (!i2.layerData.has(u2)) continue;
      if (i2.key.level !== D && (D = i2.key.level, g.setDataUniforms(v, o, t3, D, f2)), y = i2.layerData.get(u2), 0 === y.iconPerPageElementsMap.size) continue;
      y.prepareForRendering(s), y.updateOpacityInfo();
      const a3 = y.iconVAO;
      if (null != a3) {
        s.bindVAO(a3), v.setUniformMatrix3fv("u_dvsMat3", i2.transforms.displayViewScreenMat3), v.setUniform1f("u_time", (performance.now() - y.lastOpacityUpdate) / 1e3);
        for (const [t4, a4] of y.iconPerPageElementsMap) this._renderIconRange(e4, v, a4, t4, i2);
      }
    }
  }
  _renderIconRange(e4, t3, i2, r7, n3) {
    const { context: s, spriteMosaic: f2 } = e4;
    this._spritesTextureSize[0] = f2.getWidth(r7) / 4, this._spritesTextureSize[1] = f2.getHeight(r7) / 4, t3.setUniform2fv("u_mosaicSize", this._spritesTextureSize), f2.bind(s, 9729, r7, Y), this._setStencilState(e4, n3), s.drawElements(_.TRIANGLES, i2[1], R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * i2[0]), n3.triangleCount += i2[1] / 3;
  }
  _drawText(e4, a3, s) {
    const { context: o, displayLevel: l2, glyphMosaic: c3, painter: u2, pixelRatio: p, spriteMosaic: m2, state: g, styleLayerUID: d, requestRender: y, allowDelayedRender: _2 } = e4, h2 = a3.textMaterial, M = u2.vectorTilesMaterialManager;
    let U, x = false;
    for (const t3 of s) if (t3.layerData.has(d) && (U = t3.layerData.get(d), U.glyphPerPageElementsMap.size > 0)) {
      x = true;
      break;
    }
    if (!x) return;
    const P = a3.getPaintProperty("text-opacity");
    if (P && !P.isDataDriven && 0 === P.getValue(l2)) return;
    const S = a3.getPaintProperty("text-color"), E = !S || S.isDataDriven || S.getValue(l2)[3] > 0, v = a3.getPaintProperty("text-halo-width"), D = a3.getPaintProperty("text-halo-color"), T = (!v || v.isDataDriven || v.getValue(l2) > 0) && (!D || D.isDataDriven || D.getValue(l2)[3] > 0);
    if (!E && !T) return;
    const V = 24 / 8;
    let w = a3.getLayoutValue("text-rotation-alignment", l2);
    2 === w && (w = 0 === a3.getLayoutValue("symbol-placement", l2) ? 1 : 0);
    const R2 = 0 === w, I = a3.getLayoutValue("text-keep-upright", l2) && R2, L = 0.8 * V / p;
    this._glyphTextureSize || (this._glyphTextureSize = r2(c3.width / 4, c3.height / 4));
    const z = a3.getPaintValue("text-translate", l2), N = a3.getPaintValue("text-translate-anchor", l2), b = this._sdfProgramOptions, k = M.getMaterialProgram(o, h2, b);
    if (_2 && null != y && !k.compiled) return void y();
    o.useProgram(k), k.setUniformMatrix3fv("u_displayViewMat3", 0 === w ? g.displayViewMat3 : g.displayMat3), k.setUniformMatrix3fv("u_displayMat3", 1 === N ? g.displayMat3 : g.displayViewMat3), k.setUniform2fv("u_textTranslation", z), k.setUniform1f("u_depth", a3.z + f), k.setUniform2fv("u_mosaicSize", this._glyphTextureSize), k.setUniform1f("u_mapRotation", c(g.rotation)), k.setUniform1f("u_keepUpright", I ? 1 : 0), k.setUniform1f("u_level", 10 * l2), k.setUniform1i("u_texture", Z), k.setUniform1f("u_antialiasingWidth", L), k.setUniform1f("u_fadeDuration", e / 1e3);
    let O = -1;
    for (const t3 of s) {
      if (!t3.layerData.has(d)) continue;
      if (t3.key.level !== O && (O = t3.key.level, h2.setDataUniforms(k, l2, a3, O, m2)), U = t3.layerData.get(d), 0 === U.glyphPerPageElementsMap.size) continue;
      U.prepareForRendering(o), U.updateOpacityInfo();
      const i2 = U.textVAO;
      if (null == i2) continue;
      o.bindVAO(i2), k.setUniformMatrix3fv("u_dvsMat3", t3.transforms.displayViewScreenMat3), this._setStencilState(e4, t3);
      const r7 = (performance.now() - U.lastOpacityUpdate) / 1e3;
      k.setUniform1f("u_time", r7), U.glyphPerPageElementsMap.forEach((e5, i3) => {
        this._renderGlyphRange(o, e5, i3, c3, k, T, E, t3);
      });
    }
  }
  _renderGlyphRange(e4, t3, i2, a3, n3, s, f2, c3) {
    a3.bind(e4, 9729, i2, Z), s && (n3.setUniform1f("u_halo", 1), e4.drawElements(_.TRIANGLES, t3[1], R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t3[0]), c3.triangleCount += t3[1] / 3), f2 && (n3.setUniform1f("u_halo", 0), e4.drawElements(_.TRIANGLES, t3[1], R.UNSIGNED_INT, Uint32Array.BYTES_PER_ELEMENT * t3[0]), c3.triangleCount += t3[1] / 3);
  }
  _setStencilState(e4, t3) {
    const { context: i2, is3D: a3, stencilSymbols: r7 } = e4;
    if (i2.setStencilTestEnabled(true), r7) return i2.setStencilWriteMask(255), void i2.setStencilFunction(519, t3.stencilRef, 255);
    i2.setStencilWriteMask(0), a3 ? i2.setStencilFunction(514, t3.stencilRef, 255) : i2.setStencilFunction(516, 255, 255);
  }
};

export {
  t,
  m,
  a,
  r5 as r,
  a2,
  e3 as e,
  c2 as c
};
//# sourceMappingURL=chunk-VAFFGKHE.js.map
