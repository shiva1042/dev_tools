import {
  a,
  l,
  o,
  r,
  s,
  y
} from "./chunk-CGTKS5SN.js";
import {
  e,
  t
} from "./chunk-U3B7YMY7.js";
import {
  t as t2
} from "./chunk-Y65YJSC2.js";
import {
  Q,
  R
} from "./chunk-JE2NJSBU.js";

// node_modules/@arcgis/core/views/2d/engine/vectorTiles/decluttering/CollisionJob.js
var s2 = class {
  constructor(s4, o3, n3, i, l3, c2) {
    this._symbols = s4, this._styleRepository = i, this._zoom = l3, this._currentLayerCursor = 0, this._currentSymbolCursor = 0, this._styleProps = /* @__PURE__ */ new Map(), this._allNeededMatrices = /* @__PURE__ */ new Map(), this._gridIndex = new r(o3, n3, t), this._si = Math.sin(Math.PI * c2 / 180), this._co = Math.cos(Math.PI * c2 / 180), i.cachedStyles && (this._styleProps = i.cachedStyles);
    for (const t3 of s4) for (const r4 of t3.symbols) this._allNeededMatrices.has(r4.tile) || this._allNeededMatrices.set(r4.tile, t2(r4.tile.transforms.tileUnitsToPixels));
  }
  work(e2) {
    const t3 = performance.now();
    for (; this._currentLayerCursor < this._symbols.length; this._currentLayerCursor++, this._currentSymbolCursor = 0) {
      const r4 = this._symbols[this._currentLayerCursor], s4 = this._getProperties(r4.styleLayerUID), o3 = this._styleRepository.layerContexts?.get(r4.styleLayerUID);
      for (; this._currentSymbolCursor < r4.symbols.length; this._currentSymbolCursor++) {
        if (this._currentSymbolCursor % 100 == 99 && performance.now() - t3 > e2) return false;
        const n3 = r4.symbols[this._currentSymbolCursor];
        if (!n3.uniqueSymbol?.show) continue;
        const i = this._computeCoordinates(n3, s4, o3), l3 = n3.uniqueSymbol;
        if (!l3.show) continue;
        const { iconAllowOverlap: c2, textAllowOverlap: h2 } = s4;
        for (const e3 of i) {
          if (!e3.enabled) continue;
          const t4 = l3.parts[e3.partIndex];
          if (!t4.show) continue;
          !(e3.partIndex ? h2 : c2) && this._doesCollide(e3) && (e3.hard ? l3.show = false : t4.show = false);
        }
        l3.show && this._insertColliders(l3.parts, i, s4);
      }
    }
    return true;
  }
  _insertColliders(e2, t3, r4) {
    const { iconIgnorePlacement: s4, textIgnorePlacement: o3 } = r4;
    for (const n3 of t3) {
      if (!n3.enabled) continue;
      if (n3.partIndex ? o3 : s4) continue;
      if (!e2[n3.partIndex].show) continue;
      const t4 = n3.xScreen + n3.dxScreen, r5 = n3.yScreen + n3.dyScreen, i = t4 + n3.width, l3 = r5 + n3.height, [c2, h2, a4, d] = this._gridIndex.getCellSpan(t4, r5, i, l3);
      for (let e3 = h2; e3 <= d; e3++) for (let t5 = c2; t5 <= a4; t5++) {
        this._gridIndex.cells[e3][t5].push(n3);
      }
    }
  }
  _computeCoordinates(e2, t3, r4) {
    const { iconRotationAlignment: s4, textRotationAlignment: o3, iconTranslate: n3, iconTranslateAnchor: i, textTranslate: l3, textTranslateAnchor: c2 } = t3, h2 = this._si, a4 = this._co, d = this._zoom, y3 = this._allNeededMatrices.get(e2.tile), u2 = e2.uniqueSymbol, x = e2.colliders(r4);
    let _ = 0;
    for (const f of x) {
      const [e3, t4] = 0 === f.partIndex ? n3 : l3, r5 = 0 === f.partIndex ? i : c2, u3 = f.minLod <= d && d <= f.maxLod;
      _ += u3 ? 0 : 1, f.enabled = u3, f.xScreen = f.xTile * y3[0] + f.yTile * y3[3] + y3[6], f.yScreen = f.xTile * y3[1] + f.yTile * y3[4] + y3[7], 0 === r5 ? (f.xScreen += a4 * e3 - h2 * t4, f.yScreen += h2 * e3 + a4 * t4) : (f.xScreen += e3, f.yScreen += t4), 1 === (0 === f.partIndex ? s4 : o3) ? (f.dxScreen = f.dxPixels, f.dyScreen = f.dyPixels) : (f.dxScreen = a4 * (f.dxPixels + f.width / 2) - h2 * (f.dyPixels + f.height / 2) - f.width / 2, f.dyScreen = h2 * (f.dxPixels + f.width / 2) + a4 * (f.dyPixels + f.height / 2) - f.height / 2);
    }
    return x.length > 0 && _ === x.length && u2 && (u2.show = false), x;
  }
  _getProperties(e2) {
    const t3 = this._styleProps.get(e2);
    if (t3) return t3;
    const r4 = this._styleRepository.getLayerStyleProperties?.(e2, this._zoom);
    return this._styleProps.set(e2, r4), r4;
  }
  _doesCollide(e2) {
    const t3 = e2.xScreen + e2.dxScreen, r4 = e2.yScreen + e2.dyScreen, s4 = t3 + e2.width, o3 = r4 + e2.height, [n3, i, l3, c2] = this._gridIndex.getCellSpan(t3, r4, s4, o3);
    for (let h2 = i; h2 <= c2; h2++) for (let i2 = n3; i2 <= l3; i2++) {
      const n4 = this._gridIndex.cells[h2][i2];
      for (const i3 of n4) {
        if (null != i3.labelId && null != e2.labelId && i3.labelId === e2.labelId) continue;
        const n5 = i3.xScreen + i3.dxScreen, l4 = i3.yScreen + i3.dyScreen, c3 = n5 + i3.width, h3 = l4 + i3.height;
        if (!(s4 < n5 || t3 > c3 || o3 < l4 || r4 > h3)) return true;
      }
    }
    return false;
  }
};

// node_modules/@arcgis/core/views/2d/engine/vectorTiles/decluttering/SymbolDeclutterer.js
function s3(e2) {
  return (e2.uniqueSymbol?.show && e2.uniqueSymbol?.lastShow) ?? false;
}
function r2(e2, t3) {
  if (e2.priority - t3.priority) return e2.priority - t3.priority;
  if (s3(e2) && !s3(t3)) return -1;
  if (s3(t3) && !s3(e2)) return 1;
  const o3 = e2.tile.key, i = t3.tile.key;
  return o3.world - i.world ? o3.world - i.world : o3.level - i.level ? o3.level - i.level : o3.row - i.row ? o3.row - i.row : o3.col - i.col ? o3.col - i.col : e2.xTile - t3.xTile ? e2.xTile - t3.xTile : e2.yTile - t3.yTile;
}
var n = class {
  get running() {
    return this._running;
  }
  constructor(e2, t3, o3, i, s4, r4, n3, l3) {
    this.selectionMode = e2, this._visibleTiles = t3, this._symbolRepository = o3, this._styleRepository = i, this._createCollisionJob = s4, this._assignTileSymbolsOpacity = r4, this._symbolLayerSorter = n3, this._isLayerVisible = l3, this._selectionJob = null, this._selectionJobCompleted = false, this._collisionJob = null, this._collisionJobCompleted = false, this._opacityJob = null, this._opacityJobCompleted = false, this._running = true;
  }
  setScreenSize(e2, t3) {
    this._screenWidth === e2 && this._screenHeight === t3 || this.restart(), this._screenWidth = e2, this._screenHeight = t3;
  }
  restart() {
    this._selectionJob = null, this._selectionJobCompleted = false, this._collisionJob = null, this._collisionJobCompleted = false, this._opacityJob = null, this._opacityJobCompleted = false, this._running = true;
  }
  continue(e2) {
    if (this._selectionJob || (this._selectionJob = this._createSelectionJob()), !this._selectionJobCompleted) {
      const t3 = performance.now();
      if (!this._selectionJob.work(e2)) return false;
      if (this._selectionJobCompleted = true, 0 === (e2 = Math.max(0, e2 - (performance.now() - t3)))) return false;
    }
    if (this._collisionJob || (this._collisionJob = this._createCollisionJob(this._selectionJob.sortedSymbols, this._screenWidth, this._screenHeight)), !this._collisionJobCompleted) {
      const t3 = performance.now();
      if (!this._collisionJob.work(e2)) return false;
      if (this._collisionJobCompleted = true, 0 === (e2 = Math.max(0, e2 - (performance.now() - t3)))) return false;
    }
    if (this._opacityJob || (this._opacityJob = this._createOpacityJob()), !this._opacityJobCompleted) {
      const t3 = performance.now();
      if (!this._opacityJob.work(e2)) return false;
      if (this._opacityJobCompleted = true, 0 === (e2 = Math.max(0, e2 - (performance.now() - t3)))) return false;
    }
    return this._running = false, true;
  }
  _isFeatureFiltered(e2, t3, s4) {
    const r4 = t3.getFilterFlags(e2), n3 = r4 & Q, l3 = null == s4.featureEffect || s4.featureEffect.excludedLabelsVisible || r4 & R;
    return !(n3 && l3);
  }
  _getFilteredByLayer() {
    let e2;
    if (this._styleRepository?.layerContexts) for (const t3 of this._symbolRepository.uniqueSymbols) {
      const o3 = this._styleRepository.layerContexts?.get(t3.styleLayerUID);
      if (o3?.attributeView) for (const i of t3.uniqueSymbols) {
        e2 ??= /* @__PURE__ */ new Map(), e2.get(t3.styleLayerUID) || e2.set(t3.styleLayerUID, /* @__PURE__ */ new Set());
        const s4 = e2.get(t3.styleLayerUID), r4 = o3.attributeView, n3 = o3.layerView;
        this._isFeatureFiltered(i.id, r4, n3) && s4.add(i.id);
      }
    }
    return e2;
  }
  _resetSelection() {
    for (let e2 = 0; e2 < this._symbolRepository.uniqueSymbols.length; e2++) {
      const t3 = this._symbolRepository.uniqueSymbols[e2];
      for (let e3 = 0; e3 < t3.uniqueSymbols.length; e3++) {
        const o3 = t3.uniqueSymbols[e3];
        for (const e4 of o3.tileSymbols) e4.selectedForRendering = false;
      }
    }
  }
  _createSelectionJob() {
    const e2 = "feature-tile" === this.selectionMode ? c : a2, t3 = this._symbolRepository.uniqueSymbols;
    this._resetSelection();
    const o3 = [];
    let i = 0, s4 = 0;
    const n3 = this._isLayerVisible, l3 = this._getFilteredByLayer(), y3 = this._styleRepository?.layerContexts;
    function h2(c2) {
      let a4;
      const h3 = performance.now();
      for (; s4 < t3.length; s4++, i = 0) {
        const r4 = t3[s4], u3 = r4.styleLayerUID, f = l3?.get(u3);
        let b = 0;
        if (y3) {
          const e3 = y3.get(u3).layerView;
          b = e3.view.allLayerViews.items.indexOf(e3);
        }
        if (!n3(u3)) {
          o3[s4] || (o3[s4] = { styleLayerUID: u3, layerOrder: b, symbols: [] });
          continue;
        }
        o3[s4] ||= { styleLayerUID: u3, symbols: [], layerOrder: b };
        const m = o3[s4];
        for (; i < r4.uniqueSymbols.length; i++) {
          if (a4 = r4.uniqueSymbols[i], i % 100 == 99 && performance.now() - h3 > c2) return false;
          if (a4.lastShow = a4.show, a4.id && f?.has(a4.id)) {
            a4.show = false, a4.parts[0].show = false, a4.parts[1].show = false;
            continue;
          }
          const t4 = e2(a4);
          if (t4) {
            t4.selectedForRendering = true, m.symbols.push(t4), a4.show = true;
            for (const e3 of a4.parts) e3.show = true;
          } else a4.show = false;
        }
      }
      for (const e3 of o3) e3.symbols.sort(r2);
      return o3.sort((e3, t4) => t4.layerOrder - e3.layerOrder), true;
    }
    const u2 = this._symbolLayerSorter;
    return { work: h2, get sortedSymbols() {
      return o3.sort(u2);
    } };
  }
  _createOpacityJob() {
    const t3 = this._assignTileSymbolsOpacity, o3 = this._visibleTiles;
    let i = 0;
    function s4(e2, o4) {
      for (const t4 of e2.symbols.values()) l2(t4, o4);
      t3(e2, o4);
      for (const t4 of e2.childrenTiles) s4(t4, o4);
    }
    return { work(r4) {
      const n3 = performance.now();
      for (; i < o3.length; i++) {
        if (performance.now() - n3 > r4) return false;
        const l3 = o3[i];
        if (null != l3.parentTile) continue;
        const c2 = performance.now();
        l3 instanceof y ? s4(l3, c2) : t3(l3, c2);
      }
      return true;
    } };
  }
};
function l2(e2, o3) {
  for (const i of e2) {
    const e3 = i.uniqueSymbol;
    for (const i2 of e3.parts) {
      const s4 = i2.targetOpacity > 0.5 ? 1 : -1;
      i2.startOpacity += s4 * ((o3 - i2.startTime) / e), i2.startOpacity = Math.min(Math.max(i2.startOpacity, 0), 1), i2.startTime = o3, i2.targetOpacity = e3.show && i2.show ? 1 : 0;
    }
  }
}
function c(e2) {
  let t3 = null, o3 = null, i = null;
  for (const s4 of e2.tileSymbols) {
    const e3 = s4.tile;
    e3.isReady && e3.isCoverage ? t3 = s4 : e3.isReady ? o3 = s4 : e3.rendering && (i = s4);
  }
  return t3 ?? o3 ?? i;
}
function a2(e2) {
  let t3 = null, o3 = false, i = false;
  for (const s4 of e2.tileSymbols) if (!i || !o3) {
    const e3 = s4.tile;
    (!t3 || e3.isCoverage || e3.neededForCoverage && !o3) && (t3 = s4, (e3.neededForCoverage || e3.isCoverage) && (i = true), e3.isCoverage && (o3 = true));
  }
  return i ? t3 : null;
}

// node_modules/@arcgis/core/views/2d/engine/vectorTiles/decluttering/SymbolRepository.js
var o2 = class _o {
  static fromSymbols(e2, t3) {
    let s4 = e2.length;
    if (s4 >= n2) {
      let i = t3;
      do {
        i /= 2, s4 /= 4;
      } while (s4 > r3 && i > y2);
      const n3 = new r(t3, t3, i);
      for (const t4 of e2) n3.getCell(t4.xTile, t4.yTile).push(t4);
      return new _o(t3, e2, n3);
    }
    return new _o(t3, e2, null);
  }
  constructor(e2, t3, s4) {
    this.tileCoordRange = e2, this._symbols = t3, this._index = s4;
  }
  addSymbols(e2) {
    for (const t3 of e2) this._symbols.push(t3);
    if (this._index) for (const t3 of e2) this._index.getCell(t3.xTile, t3.yTile).push(t3);
  }
  removeSymbols(e2) {
    const t3 = new Set(e2);
    if (this._symbols = this._symbols.filter((e3) => !t3.has(e3)), this._index) for (const s4 of this._index.cells) for (let e3 = 0; e3 < s4.length; e3++) s4[e3] = s4[e3].filter((e4) => !t3.has(e4));
  }
  getSymbols() {
    return this._symbols;
  }
  getCandidate(e2, t3, s4, i) {
    if (!this._index) {
      for (const l4 of this._symbols) if (s4 === l4.hash && Math.abs(e2 - l4.xTile) <= i && Math.abs(t3 - l4.yTile) <= i) return l4;
      return null;
    }
    const l3 = this._index.getCellSpan(e2 - i, t3 - i, e2 + i, t3 + i), [o3, n3, r4, y3] = l3;
    for (let a4 = n3; a4 <= y3; a4++) for (let l4 = o3; l4 <= r4; l4++) {
      const o4 = this._index.cells[a4][l4];
      for (const l5 of o4) if (s4 === l5.hash && Math.abs(e2 - l5.xTile) <= i && Math.abs(t3 - l5.yTile) <= i) return l5;
    }
    return null;
  }
};
var n2 = 32;
var r3 = 8;
var y2 = 64;
var a3 = 20;
var u = class {
  constructor(e2, t3) {
    this.tileCoordRange = e2, this._visibleTiles = t3, this._indexMapByTile = /* @__PURE__ */ new Map(), this._uniqueSymbolsByStyleLayerId = /* @__PURE__ */ new Map();
  }
  get uniqueSymbols() {
    return null == this._uniqueSymbolLayerArray && (this._uniqueSymbolLayerArray = this._createUniqueSymbolLayerArray()), this._uniqueSymbolLayerArray;
  }
  registerVectorTile(e2, t3) {
    const s4 = this._ensureIndexMap(e2), i = t3?.values() ?? s4.keys();
    for (const l3 of i) {
      const e3 = s4.get(l3);
      e3 && (this._removeSymbols(l3, e3.getSymbols()), s4.delete(l3));
    }
    this._addSymbols(e2.key, s4, e2.symbols), this._invalidate();
  }
  unregisterVectorTile(e2) {
    this._removeTile(e2), this._invalidate();
  }
  registerFeatureTile(e2) {
    this._ensureIndexMap(e2), this._invalidate();
  }
  unregisterFeatureTile(e2) {
    this._removeTile(e2), this._invalidate();
  }
  insertFeatureTileMetrics(e2, t3) {
    const s4 = this._indexMapByTile.get(e2);
    if (!s4) throw new Error(`tile ${e2.id} not registered!`);
    this._addSymbols(e2.key, s4, h(t3)), this._invalidate();
  }
  removeFeatureTileMetrics(e2, t3) {
    const s4 = this._indexMapByTile.get(e2);
    if (!s4) return;
    const i = h(t3);
    for (const [l3, o3] of s4.entries()) {
      const e3 = i.get(l3);
      e3 && (o3.removeSymbols(e3), this._removeSymbols(l3, e3));
    }
    this._invalidate();
  }
  deleteStyleLayers(e2) {
    for (const t3 of this._indexMapByTile.values()) for (const s4 of e2) {
      const e3 = t3.get(s4);
      e3 && (this._removeSymbols(s4, e3.getSymbols()), t3.delete(s4));
    }
    this._invalidate();
  }
  querySymbols(e2, s4, i, l3) {
    const o3 = [];
    for (const [n3, r4] of this._uniqueSymbolsByStyleLayerId.entries()) for (const l4 of r4) {
      const r5 = l4.tileSymbols.find((e3) => e3.selectedForRendering);
      r5 && a(r5, e2, s4 * (window.devicePixelRatio || 1), i) && o3.push({ vtlSymbol: r5, styleLayerUID: n3, tileKey: r5.tile.key });
    }
    return o3;
  }
  _ensureIndexMap(e2) {
    let t3 = this._indexMapByTile.get(e2);
    return t3 || (t3 = /* @__PURE__ */ new Map(), this._indexMapByTile.set(e2, t3)), t3;
  }
  _invalidate() {
    this._uniqueSymbolLayerArray = null;
  }
  _addSymbols(e2, t3, s4) {
    for (const [i, l3] of s4) {
      let e3 = t3.get(i);
      e3 ? e3.addSymbols(l3) : (e3 = o2.fromSymbols(l3, this.tileCoordRange), t3.set(i, e3));
    }
    this._updateUniqueSymbols(e2, s4);
  }
  _removeTile(e2) {
    const t3 = this._indexMapByTile.get(e2);
    if (t3) {
      for (const [e3, s4] of t3.entries()) this._removeSymbols(e3, s4.getSymbols());
      this._indexMapByTile.delete(e2), this._invalidate();
    }
  }
  _removeSymbols(e2, t3) {
    for (const s4 of t3) {
      const t4 = s4.uniqueSymbol;
      if (t4) {
        if (t4.tileSymbols = t4.tileSymbols.filter((e3) => e3 !== s4), 0 === t4.tileSymbols.length) {
          const s5 = this._uniqueSymbolsByStyleLayerId.get(e2);
          s5.delete(t4), 0 === s5.size && this._uniqueSymbolsByStyleLayerId.delete(e2);
        }
        s4.uniqueSymbol = null;
      }
    }
  }
  _updateUniqueSymbols(t3, s4) {
    if (0 !== s4.size) {
      for (const e2 of this._visibleTiles) e2.parentTile || e2.key.world !== t3.world || e2.key.level === t3.level && !e2.key.equals(t3) || this._matchSymbols(e2, t3, s4);
      for (const [t4, i] of s4) for (const s5 of i) if (!s5.uniqueSymbol) {
        s5.uniqueSymbol = new s(s5);
        let i2 = this._uniqueSymbolsByStyleLayerId.get(t4);
        i2 || (i2 = /* @__PURE__ */ new Set(), this._uniqueSymbolsByStyleLayerId.set(t4, i2)), i2.add(s5.uniqueSymbol);
      }
    }
  }
  _matchSymbols(e2, t3, l3) {
    if (e2.key.level > t3.level) {
      const s4 = e2.key.level - t3.level;
      if (e2.key.row >> s4 !== t3.row || e2.key.col >> s4 !== t3.col) return;
    }
    if (t3.level > e2.key.level) {
      const s4 = t3.level - e2.key.level;
      if (t3.row >> s4 !== e2.key.row || t3.col >> s4 !== e2.key.col) return;
    }
    const o3 = /* @__PURE__ */ new Map();
    for (const [n3, r4] of l3) {
      const l4 = [], y3 = e2.key.level < t3.level ? 1 : 1 << Math.abs(e2.key.level - t3.level), u2 = this._indexMapByTile.get(e2), h2 = u2?.get(n3);
      if (h2) for (const o4 of r4) {
        if (o4.uniqueSymbol) continue;
        const n4 = o(this.tileCoordRange, o4.xTile, t3, e2.key), r5 = l(this.tileCoordRange, o4.yTile, t3, e2.key), u3 = -a3, c2 = this.tileCoordRange + a3;
        if (!(n4 >= u3 && n4 < c2 && r5 >= u3 && r5 < c2)) {
          l4.push(o4);
          continue;
        }
        const f = h2.getCandidate(n4, r5, o4.hash, y3), m = f?.uniqueSymbol;
        m ? (o4.uniqueSymbol = m, m.tileSymbols.push(o4)) : l4.push(o4);
      }
      l4.length > 0 && o3.set(n3, l4);
    }
    for (const s4 of e2.childrenTiles || []) this._matchSymbols(s4, t3, o3);
  }
  _createUniqueSymbolLayerArray() {
    const e2 = this._uniqueSymbolsByStyleLayerId, t3 = new Array(e2.size);
    let s4, i = 0;
    for (const [l3, o3] of e2) {
      const e3 = new Array(o3.size);
      s4 = 0;
      for (const t4 of o3) e3[s4++] = t4;
      t3[i] = { styleLayerUID: l3, uniqueSymbols: e3 }, i++;
    }
    return t3;
  }
};
function h(e2) {
  const t3 = /* @__PURE__ */ new Map();
  for (const s4 of e2) {
    const e3 = s4.labelClassId;
    let i = t3.get(e3);
    i || (i = [], t3.set(e3, i)), i.push(s4);
  }
  return t3;
}

export {
  s2 as s,
  n,
  u
};
//# sourceMappingURL=chunk-MTUAMNRB.js.map
