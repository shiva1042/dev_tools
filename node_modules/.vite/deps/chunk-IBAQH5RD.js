import {
  p as p2
} from "./chunk-QOCVKXV3.js";
import {
  e as e8
} from "./chunk-IH4WC6YT.js";
import {
  l,
  y
} from "./chunk-556AIKHG.js";
import {
  e as e7
} from "./chunk-UOLTC2WE.js";
import {
  d as d4
} from "./chunk-7UW3C5TH.js";
import {
  d,
  i as i2,
  s as s3
} from "./chunk-57TMGMSR.js";
import {
  e as e6,
  n2 as n3
} from "./chunk-OVNFKKPT.js";
import {
  f as f2,
  v
} from "./chunk-L5FZCSSN.js";
import {
  a
} from "./chunk-LIEJ3XIJ.js";
import {
  p
} from "./chunk-SU3UP5OY.js";
import {
  a as a3,
  t as t3
} from "./chunk-NROC6H4B.js";
import {
  r as r2
} from "./chunk-7YJLACUB.js";
import {
  s as s2
} from "./chunk-G5FVMECV.js";
import {
  f
} from "./chunk-TNSNCYOP.js";
import {
  n as n4
} from "./chunk-MRIRIMGR.js";
import {
  r2 as r4
} from "./chunk-UC2FSPGH.js";
import {
  r as r3
} from "./chunk-DYYIMT3Q.js";
import {
  d as d2
} from "./chunk-7MVET5FS.js";
import {
  o
} from "./chunk-GMNFOBDO.js";
import {
  i
} from "./chunk-VAEW3HN2.js";
import {
  d as d3
} from "./chunk-FQFAOSSS.js";
import {
  o as o2
} from "./chunk-PL5DDSFM.js";
import {
  P
} from "./chunk-K46OLRTU.js";
import {
  e as e2
} from "./chunk-66Y6I5GR.js";
import {
  e as e3
} from "./chunk-U5J3MCMX.js";
import {
  e as e4
} from "./chunk-MCWHCMX6.js";
import {
  e as e5
} from "./chunk-3PAAA3FK.js";
import {
  a as a2
} from "./chunk-E3D6AUQJ.js";
import {
  s as s4
} from "./chunk-SDI3N3QP.js";
import {
  n as n2,
  t as t2
} from "./chunk-CFXCM2NO.js";
import {
  j
} from "./chunk-5ZRDULGW.js";
import {
  r
} from "./chunk-ZVTEFYZX.js";
import {
  L
} from "./chunk-O5NPUMOD.js";
import {
  R
} from "./chunk-7IDU4Z6B.js";
import {
  e,
  s,
  t
} from "./chunk-6KIXN4DJ.js";
import {
  n
} from "./chunk-XL3L5YCX.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/ComputeFragmentNormals.glsl.js
function d5(d6, i4) {
  const l3 = d6.fragment;
  switch (i4.doubleSidedMode) {
    case 0:
      l3.code.add(t2`vec3 _adjustDoublesided(vec3 normal) {
return normal;
}`);
      break;
    case 1:
      d6.include(f, i4), l3.code.add(t2`vec3 _adjustDoublesided(vec3 normal) {
return dot(normal, vPositionWorldCameraRelative) > 0.0 ? -normal : normal;
}`);
      break;
    case 2:
      l3.code.add(t2`vec3 _adjustDoublesided(vec3 normal) {
return gl_FrontFacing ? normal : -normal;
}`);
      break;
    default:
      n(i4.doubleSidedMode);
    case 3:
  }
  switch (i4.normalType) {
    case 0:
    case 1:
      d6.include(d, i4), l3.main.add(t2`vec3 fragmentFaceNormal = _adjustDoublesided(normalize(vNormalWorld));
vec3 fragmentFaceNormalView = gl_FrontFacing ? normalize(vNormalView) : -normalize(vNormalView);`);
      break;
    case 2:
      d6.include(f, i4), l3.main.add(t2`vec3 fragmentFaceNormal = normalize(cross(dFdx(vPositionWorldCameraRelative), dFdy(vPositionWorldCameraRelative)));
vec3 fragmentFaceNormalView = normalize(cross(dFdx(vPosition_view), dFdy(vPosition_view)));`);
  }
  i4.shadeNormals ? l3.main.add(t2`vec3 fragmentShadingNormal = fragmentFaceNormal;`) : i4.spherical ? (d6.include(f, i4), l3.main.add(t2`vec3 fragmentShadingNormal = normalize(positionWorld());`)) : l3.main.add(t2`vec3 fragmentShadingNormal = vec3(0.0, 0.0, 1.0);`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/ComputeMaterialColor.glsl.js
function l2(l3, s5) {
  l3.include(r2, s5), l3.fragment.include(i2);
  const i4 = l3.fragment;
  i4.uniforms.add(new e7("baseColor", (o3) => o3.baseColor)), i4.uniforms.add(new r3("objectOpacity", (o3) => o3.objectOpacity)), s5.hasVertexColors ? i4.code.add(t2`vec3 _baseColor() {
return baseColor.rgb * vColor.rgb;
}
float _baseOpacity() {
return baseColor.a * vColor.a;
}`) : i4.code.add(t2`vec3 _baseColor() {
return baseColor.rgb;
}
float _baseOpacity() {
return baseColor.a;
}`), i4.code.add(t2`vec4 computeMaterialColor(vec4 textureColor, vec4 externalColor, int externalColorMixMode) {
vec3 baseColor = _baseColor();
float baseOpacity = _baseOpacity();
vec3 color = mixExternalColor(
baseColor,
textureColor.rgb,
externalColor.rgb,
externalColorMixMode
);
float opacity = objectOpacity * mixExternalOpacity(
baseOpacity,
textureColor.a,
externalColor.a,
externalColorMixMode
);
return vec4(color, opacity);
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/ReadBaseColorTexture.glsl.js
function u(u2, s5) {
  const a4 = s5.hasColorTexture && (o(s5.output) || 1 !== s5.alphaDiscardMode);
  a4 && (u2.include(r4, s5), u2.fragment.uniforms.add(new e4("baseColorTexture", (e9) => e9.texture))), u2.fragment.code.add(t2`
    vec4 readBaseColorTexture() {
      return ${a4 ? "textureLookup(baseColorTexture, vuv0)" : "vec4(1.0)"};
    }
  `);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/SphereDepthInterpolate.glsl.js
function i3(i4, h) {
  i4.fragment.uniforms.add(new e5("heightParameters", (a4) => {
    const o3 = a4.camera, s5 = j(o3.eye), n5 = Math.sqrt(s5), i5 = h.radius, l3 = s5 - i5 * i5;
    let m = L(4e6, 5e6, n5 - i5);
    return m = Math.min(m, 0.98), r(m, l3, 0, 0);
  }), new e2("cameraPosition", (e9) => e9.camera.eye)), i4.fragment.include(e8), i4.fragment.code.add(t2`float sphereFade() {return heightParameters[0];}
float sphereDepthInterpolate(vec3 worldRay, vec3 viewRay, float currentLinearDepth) {
vec2 rayPlanetIntersect = sphereIntersect(cameraPosition, worldRay, heightParameters[1]);
bool hitsPlanet = (rayPlanetIntersect.x <= rayPlanetIntersect.y) && rayPlanetIntersect.x > 0.0;
if (hitsPlanet) {
float sphereDepth = rayPlanetIntersect.x;
viewRay *= viewRay.z*sphereDepth;
float linearDepth = length(viewRay);
float sphereFade = heightParameters[0];
return (-linearDepth)*sphereFade + currentLinearDepth*(1.0-sphereFade);
}
return currentLinearDepth;
}`);
}

// node_modules/@arcgis/core/chunks/ComponentShader.glsl.js
function F(F2) {
  const B2 = new s4(), { vertex: _, fragment: I } = B2;
  B2.include(f, F2), B2.include(d, F2), B2.include(r2, F2), B2.include(d2, F2), B2.include(p2, F2), B2.include(s3, F2), I.include(P, F2), B2.include(u, F2), B2.include(i, F2);
  const { output: E, pbrMode: H, hasNormalTexture: V, snowCover: G, receiveShadows: k, shadeNormals: U, spherical: q, ellipsoidMode: J, overlayEnabled: K, componentData: Q, vertexDiscardMode: X, renderOccluded: Y } = F2, Z = 1 === H || 2 === H;
  Z && (B2.include(n3, F2), V && B2.include(d4, F2));
  const ee = 4 === E || 5 === E || 6 === E, re = ee && 1 === Q, oe = 1 === J, ae = 1 === J ? t.radius : oe ? e.radius : s.radius;
  K && (B2.include(f2, F2), B2.include(y, F2), _.code.add(`
      ${n2(q, `const float invRadius = ${t2.float(1 / ae)};`)}
      vec2 projectOverlay(vec3 pos) { return pos.xy ${n2(q, "/ (1.0 + invRadius * pos.z);")}; }`));
  const ie = K && o(E) && 4 === H;
  ie && (B2.varyings.add("tbnTangent", "vec3"), B2.varyings.add("tbnBiTangent", "vec3"), B2.varyings.add("groundNormal", "vec3"));
  const le = 0 === X, te = 2 === X, ne = 1 - 1 / 255;
  if (B2.include(p, F2), B2.include(s2, F2), _.main.add(t2`
    bool castShadows;
    vec4 externalColor = forwardExternalColor(castShadows);
    ${n2(re, "if(!castShadows) { gl_Position = vec4(vec3(1e38), 1.0); return; }")}

    ${n2(!le, `{ if (externalColor.a ${te ? ">" : "<="} ${t2.float(ne)}) { gl_Position = vec4(vec3(1e38), 1.0); return; } }`)}

    ${n2(10 === E, "externalColor.a = 1.0;")}

    forwardPosition(readElevationOffset());
    forwardViewPosDepth(vPosition_view);
    forwardNormal();
    forwardTextureCoordinates();
    forwardVertexColor();
    forwardLinearDepthToReadShadowMap();
    forwardLinearDepthToWriteShadowMap();
    forwardEmissiveStrength();
    forwardObjectAndLayerIdColor();
    ${n2(ie, q ? t2`
            groundNormal = normalize(positionWorld());
            tbnTangent = normalize(cross(vec3(0.0, 0.0, 1.0), groundNormal));
            tbnBiTangent = normalize(cross(groundNormal, tbnTangent));` : t2`
            groundNormal = vec3(0.0, 0.0, 1.0);
            tbnTangent = vec3(1.0, 0.0, 0.0);
            tbnBiTangent = vec3(0.0, 1.0, 0.0);`)}
    ${n2(K, "setOverlayVTC(projectOverlay(position));")}

    if (externalColor.a < ${t2.float(o2)}) {
      // Discard this vertex
      gl_Position = vec4(1e38, 1e38, 1e38, 1.0);
      return;
    }
  `), o(E)) return B2.include(l2, F2), B2.include(d5, F2), B2.include(f2, F2), B2.include(n4, F2), I.include(e6, F2), I.code.add(t2`
      float evaluateShadow() {
        return ${k ? "readShadowMap(vPositionWorldCameraRelative, linearDepth)" : "0.0"};
      }`), I.main.add(t2`
      discardBySlice(vPositionWorldCameraRelative);
      discardByTerrainDepth();

      vec4 textureColor = readBaseColorTexture();
      discardOrAdjustAlpha(textureColor);

      /* When rendering the occluded overlay, we still need to read the base color texture
       * because we need to use the same discard logic. However after that to render only the
       * draped overlay, we simply set the base texture color to zero. */
      ${n2(Y, t2`textureColor = vec4(0);`)}

      ${n2(K, t2`vec4 overlayColor = getOverlayColor(ovColorTex, vtcOverlay);`)}

      /* Early discard to only emit when we have overlay */
      ${n2(K && Y, t2`if (overlayColor.a < ${t2.float(o2)}) { discard; }`)}

      vec4 externalColor;
      int externalColorMixMode;
      readExternalColor(externalColor, externalColorMixMode);

      vec4 materialColor = computeMaterialColor(textureColor, externalColor, externalColorMixMode);
    `), Z ? (a3(I), q && v(I), I.main.add(t2`
        applyPBRFactors();
        ${n2(1 === H, t2`if (externalColorMixMode == 3) {
              mrr = vec3(0.0, 0.6, 0.2);
            }`)}
        float additionalIrradiance = 0.02 * mainLightIntensity[2];
        ${n2(V, "mat3 tangentSpace = computeTangentSpace(fragmentFaceNormal, vPositionWorldCameraRelative, vuv0);")}
        vec3 shadingNormal = ${V ? "computeTextureNormal(tangentSpace, vuv0)" : "fragmentShadingNormal"};
        vec3 groundNormal = ${q ? t2`normalize(positionWorld())` : t2`vec3(0.0, 0.0, 1.0)`};

        vec3 viewDir = normalize(vPositionWorldCameraRelative);
        float ssao = 1.0 - occlusion * evaluateAmbientOcclusionInverse();
        ${n2(G, t2`float snow = getSnow(fragmentFaceNormal, normalize(positionWorld()));
                 materialColor.rgb = mix(materialColor.rgb, vec3(1.1), snow);
                 ssao = mix(ssao, 0.5 * ssao, snow);
                 shadingNormal = mix(shadingNormal, fragmentFaceNormal, snow);`)}
        ${n2(K, "materialColor = materialColor * (1.0 - overlayColor.a) + overlayColor;")}

        vec3 additionalLight = evaluateAdditionalLighting(ssao, positionWorld());
        ${n2(q, "float additionalAmbientScale = additionalDirectedAmbientLight(positionWorld());")}
        ${q ? t2`float shadow = max(lightingGlobalFactor * (1.0 - additionalAmbientScale), evaluateShadow());` : "float shadow = evaluateShadow();"}
        vec4 shadedColor = vec4(evaluateSceneLightingPBR(shadingNormal, materialColor.rgb, shadow, ssao, additionalLight, viewDir, groundNormal, mrr, additionalIrradiance), materialColor.a);
        `)) : (t3(I), q && v(I), ie && I.uniforms.add(new e3("ovNormalTex", (e9) => e9.overlay?.getTexture(3))), I.main.add(t2`
        ${n2(q, "float additionalAmbientScale = additionalDirectedAmbientLight(positionWorld());")}
        float shadow = ${k ? q ? "max(lightingGlobalFactor * (1.0 - additionalAmbientScale), evaluateShadow())" : "evaluateShadow()" : q ? "lightingGlobalFactor * (1.0 - additionalAmbientScale)" : "0.0"};

        ${n2(k && !U, t2`
            float dotFL = dot(fragmentFaceNormal, mainLightDirection);
            if( dotFL <= 0.0) shadow = 1.0;
        `)}
        ${n2(G, t2`float snow = getSnow(fragmentFaceNormal, normalize(positionWorld()));
               materialColor.rgb = mix(materialColor.rgb, vec3(1), snow);`)}

        // At global scale we create some additional ambient light based on the main light to simulate global illumination
        float ssao = evaluateAmbientOcclusion();
        vec3 additionalLight = evaluateAdditionalLighting(ssao, positionWorld());

        ${n2(K, "materialColor = materialColor * (1.0 - overlayColor.a) + overlayColor;")}

        vec4 shadedColor = vec4(evaluateSceneLighting(fragmentShadingNormal, materialColor.rgb, shadow, ssao, additionalLight), materialColor.a);
        ${n2(ie, t2`vec4 overlayWaterMask = getOverlayColor(ovNormalTex, vtcOverlay);
                 float waterNormalLength = length(overlayWaterMask);
                 if (waterNormalLength > 0.95) {
                   mat3 tbnMatrix = mat3(tbnTangent, tbnBiTangent, groundNormal);
                   vec4 waterColorLinear = getOverlayWaterColor(overlayWaterMask, overlayColor, -normalize(vPositionWorldCameraRelative), shadow, groundNormal, tbnMatrix, vPosition_view, positionWorld());
                   vec4 waterColorNonLinear = delinearizeGamma(vec4(waterColorLinear.xyz, 1.0));
                   // un-gamma the ground color to mix in linear space
                   shadedColor = mix(shadedColor, waterColorNonLinear, waterColorLinear.w);
                 }`)}
      `)), I.main.add(`outputColorHighlightOID(shadedColor, vPositionWorldCameraRelative, materialColor.rgb ${n2(G, ", snow")});`), F2.sphereDepthInterpolate && (B2.include(i3, { radius: ae * R }), B2.fragment.include(a2), I.main.add(t2`if (sphereFade()>0.0) {
vec3 worldRay = normalize(vPositionWorldCameraRelative);
vec3 viewRay = normalize(vPosition_view);
gl_FragDepth = delinearizeDepth(sphereDepthInterpolate(worldRay, viewRay, linearizeDepth(gl_FragCoord.z)));
} else {
gl_FragDepth = gl_FragCoord.z;
}`)), B2;
  const de = 3 === E, se = 10 === E, ge = 9 === E, me = ee || 7 === E || 8 === E;
  return me && B2.include(a, F2), de && B2.include(d5, F2), K && B2.include(l, F2), B2.include(d3, F2), I.main.add(t2`
    discardBySlice(vPositionWorldCameraRelative);

    vec4 textureColor = readBaseColorTexture();
    discardOrAdjustAlpha(textureColor);

    ${n2(me, "outputDepth(linearDepth);")}
    ${n2(de, t2`fragColor = vec4(vec3(0.5) + 0.5 * fragmentFaceNormalView, 1.0);`)}
    ${n2(se, K ? "fragColor = getOverlayColorTexel();" : "outputObjectAndLayerIdColor();")}
    ${n2(ge, n2(K, t2`calculateOcclusionAndOutputHighlight(getAllOverlayHighlightValuesEncoded());`, t2`calculateOcclusionAndOutputHighlight();`))}`), B2;
}
var B = Object.freeze(Object.defineProperty({ __proto__: null, build: F }, Symbol.toStringTag, { value: "Module" }));

export {
  F,
  B
};
//# sourceMappingURL=chunk-IBAQH5RD.js.map
