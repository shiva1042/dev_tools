import {
  _,
  u
} from "./chunk-6APPUJSW.js";
import {
  E,
  G,
  N,
  O,
  S,
  T,
  Y
} from "./chunk-QNVJVDYZ.js";
import {
  d,
  e,
  s as s2
} from "./chunk-UO3JSZDI.js";
import {
  s
} from "./chunk-SHCBT5YZ.js";
import {
  i3 as i,
  l2 as l
} from "./chunk-45CZB3H2.js";
import {
  has
} from "./chunk-LLFDHRTA.js";

// node_modules/@arcgis/core/views/webgl/TextureDescriptor.js
var h = class {
  constructor(i2 = 0, h3 = i2) {
    this.width = i2, this.height = h3, this.type = 0, this.target = 3553, this.pixelFormat = 6408, this.dataType = N.UNSIGNED_BYTE, this.samplingMode = 9729, this.wrapMode = 10497, this.maxAnisotropy = 1, this.flipped = false, this.hasMipmap = false, this.isOpaque = false, this.unpackAlignment = 4, this.preMultiplyAlpha = false, this.compareEnabled = false, this.linearFilterDepth = false, this.depth = 1, this.isImmutable = false;
  }
};
function s3(t2) {
  return t2.width <= 0 || t2.height <= 0 || t2.depth <= 0 ? 0 : Math.round(t2.width * t2.height * t2.depth * (t2.hasMipmap ? 4 / 3 : 1) * (null == t2.internalFormat ? 4 : _(t2.internalFormat)) * (34067 === t2.target ? 6 : 1));
}

// node_modules/@arcgis/core/views/webgl/textureUtils.js
var c = () => i.getLogger("esri/views/webgl/textureUtils");
function s4(t2) {
  const { width: e2, height: r, depth: n } = t2;
  (null != e2 && e2 < 0 || null != r && r < 0 || null != n && n < 0) && c().error("Negative dimension parameters are not allowed!");
  const { internalFormat: o } = t2;
  if (o && (m(o) || p(o))) {
    const { linearFilterDepth: e3, compareEnabled: r2, samplingMode: n2, hasMipmap: o2 } = t2;
    o2 && c().error("Depth textures cannot have mipmaps"), e3 ? 9729 !== n2 && 9728 !== n2 && c().error("Depth textures cannot sample mipmaps") : (9728 !== n2 && c().error("Depth textures without filtering must use NEAREST filtering"), r2 && c().error("Depth textures without filtering cannot use compare function"));
  }
}
function h2(t2) {
  return S.includes(t2);
}
function m(t2) {
  return l(T, t2);
}
function p(t2) {
  return l(G, t2);
}
function f(t2) {
  return null != t2 && l(Y, t2);
}
function d2(t2) {
  return null != t2 && "type" in t2 && "compressed" === t2.type;
}
function g(t2) {
  return null != t2 && "byteLength" in t2;
}
function w(t2) {
  return null != t2 && !d2(t2) && !g(t2);
}
function x(t2) {
  return 32879 === t2 || 35866 === t2;
}
function F(t2, e2, r, n = 1) {
  let o = Math.max(e2, r);
  return 32879 === t2 && (o = Math.max(o, n)), Math.floor(Math.log2(o)) + 1;
}
function E2(e2) {
  if (null != e2.internalFormat) return e2.internalFormat;
  switch (e2.dataType) {
    case N.FLOAT:
      switch (e2.pixelFormat) {
        case 6408:
          return E.RGBA32F;
        case 6407:
          return E.RGB32F;
        default:
          throw new s("texture:unknown-format", "Unable to derive format");
      }
    case N.UNSIGNED_BYTE:
      switch (e2.pixelFormat) {
        case 6408:
          return E.RGBA8;
        case 6407:
          return E.RGB8;
      }
  }
  const { pixelFormat: r } = e2;
  return e2.internalFormat = 34041 === r ? G.DEPTH24_STENCIL8 : 6402 === r ? T.DEPTH_COMPONENT24 : r, e2.internalFormat;
}
function T2(t2) {
  let e2 = "width" in t2 ? t2.width : t2.codedWidth, r = "height" in t2 ? t2.height : t2.codedHeight;
  const n = 1;
  return t2 instanceof HTMLVideoElement && (e2 = t2.videoWidth, r = t2.videoHeight), { width: e2, height: r, depth: n };
}

// node_modules/@arcgis/core/views/webgl/ValidatedTextureDescriptor.js
var t = class _t extends h {
  constructor(e2, t2) {
    switch (super(), this.context = e2, Object.assign(this, t2), this.internalFormat) {
      case E.R16F:
      case E.R32F:
      case E.R8_SNORM:
      case E.R8:
        this.pixelFormat = 6403;
        break;
      case E.R8I:
      case E.R8UI:
      case E.R16I:
      case E.R16UI:
      case E.R32I:
      case E.R32UI:
        this.pixelFormat = 36244;
    }
  }
  static validate(s5, e2) {
    return new _t(s5, e2);
  }
};

// node_modules/@arcgis/core/views/webgl/Texture.js
var D = !!has("esri-tests-disable-gpu-memory-measurements");
var E3 = () => i.getLogger("esri/views/webgl/Texture");
var _a;
var A = (_a = class {
  constructor(e2, i2 = null, r = null) {
    if (this.type = 1, this._glName = null, this._samplingModeDirty = false, this._wrapModeDirty = false, this._shadowFilterDirty = false, this._wasImmutablyAllocated = false, "context" in e2) this._descriptor = e2, r = i2;
    else {
      const r2 = t.validate(e2, i2);
      if (!r2) throw new s("texture:invalid-descriptor", "Texture descriptor invalid");
      this._descriptor = r2;
    }
    34067 === this._descriptor.target ? this._setDataCubeMap(r) : this.setData(r);
  }
  get glName() {
    return this._glName;
  }
  get descriptor() {
    return this._descriptor;
  }
  get usedMemory() {
    return D ? 0 : s3(this._descriptor);
  }
  get isDirty() {
    return this._samplingModeDirty || this._wrapModeDirty || this._shadowFilterDirty;
  }
  get hasWebGLTextureObject() {
    return !!this._glName;
  }
  dispose() {
    this.abortCompression(), this.hasWebGLTextureObject && this._descriptor.context?.gl && (this._descriptor.context.instanceCounter.decrement(O.Texture, this), this._descriptor.context.unbindTexture(this), this._descriptor.context.gl.deleteTexture(this._glName), this._glName = null, this._descriptor = null);
  }
  release() {
    this.dispose();
  }
  [Symbol.dispose]() {
    this.dispose();
  }
  resize(e2, i2) {
    const r = this._descriptor;
    if (r.width !== e2 || r.height !== i2) {
      if (this._wasImmutablyAllocated) throw new s("texture:immutable-resize", "Immutable textures can't be resized!");
      r.width = e2, r.height = i2, 34067 === this._descriptor.target ? this._setDataCubeMap(null) : this.setData(null);
    }
  }
  enableCompression(t2) {
    this._descriptor.compress = t2;
  }
  disableCompression() {
    this._descriptor.compress = void 0;
  }
  setData(t2) {
    this.abortCompression(), !d2(t2) && this._descriptor.internalFormat && l(Y, this._descriptor.internalFormat) && (this._descriptor.internalFormat = void 0), this._setData(t2), !d2(t2) && this._descriptor.compress && this._compressOnWorker(t2);
  }
  updateData(i2, r, s5, o, a, n, p2 = 0) {
    n || E3().error("An attempt to use uninitialized data!"), this.hasWebGLTextureObject || E3().error("An attempt to update uninitialized texture!");
    const l2 = this._descriptor;
    l2.internalFormat = E2(l2);
    const { context: c2, pixelFormat: _2, dataType: u2, target: g2, isImmutable: x2 } = l2;
    if (x2 && !this._wasImmutablyAllocated) throw new s("texture:uninitialized", "Cannot update immutable texture before allocation!");
    const T3 = c2.bindTexture(this, _a.TEXTURE_UNIT_FOR_UPDATES, true);
    (r < 0 || s5 < 0 || r + o > l2.width || s5 + a > l2.height) && E3().error("An attempt to update out of bounds of the texture!"), this._configurePixelStorage();
    const { gl: M } = c2;
    p2 && (o && a || E3().warn("Must pass width and height if `UNPACK_SKIP_ROWS` is used"), M.pixelStorei(M.UNPACK_SKIP_ROWS, p2)), w(n) ? M.texSubImage2D(g2, i2, r, s5, o, a, _2, u2, n) : d2(n) ? M.compressedTexSubImage2D(g2, i2, r, s5, o, a, l2.internalFormat, n.levels[i2]) : M.texSubImage2D(g2, i2, r, s5, o, a, _2, u2, n), p2 && M.pixelStorei(M.UNPACK_SKIP_ROWS, 0), c2.bindTexture(T3, _a.TEXTURE_UNIT_FOR_UPDATES);
  }
  updateData3D(i2, r, s5, o, a, n, p2, l2) {
    l2 || E3().error("An attempt to use uninitialized data!"), this.hasWebGLTextureObject || E3().error("An attempt to update an uninitialized texture!");
    const d3 = this._descriptor;
    d3.internalFormat = E2(d3);
    const { context: _2, pixelFormat: u2, dataType: g2, isImmutable: x2, target: T3 } = d3;
    if (x2 && !this._wasImmutablyAllocated) throw new s("texture:uninitialized", "Cannot update immutable texture before allocation!");
    x(T3) || E3().warn("Attempting to set 3D texture data on a non-3D texture");
    const M = _2.bindTexture(this, _a.TEXTURE_UNIT_FOR_UPDATES);
    _2.setActiveTexture(_a.TEXTURE_UNIT_FOR_UPDATES), (r < 0 || s5 < 0 || o < 0 || r + a > d3.width || s5 + n > d3.height || o + p2 > d3.depth) && E3().error("An attempt to update out of bounds of the texture!"), this._configurePixelStorage();
    const { gl: b } = _2;
    if (d2(l2)) l2 = l2.levels[i2], b.compressedTexSubImage3D(T3, i2, r, s5, o, a, n, p2, d3.internalFormat, l2);
    else {
      const t2 = l2;
      b.texSubImage3D(T3, i2, r, s5, o, a, n, p2, u2, g2, t2);
    }
    _2.bindTexture(M, _a.TEXTURE_UNIT_FOR_UPDATES);
  }
  generateMipmap() {
    const i2 = this._descriptor;
    if (0 === i2.width || 0 === i2.height) return;
    if (!i2.hasMipmap) {
      if (this._wasImmutablyAllocated) throw new s("texture:immutable-change", "Cannot add mipmaps to immutable texture after allocation");
      i2.hasMipmap = true, this._samplingModeDirty = true, s4(i2);
    }
    9729 === i2.samplingMode ? (this._samplingModeDirty = true, i2.samplingMode = 9985) : 9728 === i2.samplingMode && (this._samplingModeDirty = true, i2.samplingMode = 9984);
    const r = this._descriptor.context.bindTexture(this, _a.TEXTURE_UNIT_FOR_UPDATES);
    this._descriptor.context.setActiveTexture(_a.TEXTURE_UNIT_FOR_UPDATES), this._descriptor.context.gl.generateMipmap(i2.target), this._descriptor.context.bindTexture(r, _a.TEXTURE_UNIT_FOR_UPDATES);
  }
  clearMipmap() {
    const e2 = this._descriptor;
    if (e2.hasMipmap) {
      if (this._wasImmutablyAllocated) throw new s("texture:immutable-change", "Cannot delete mipmaps to immutable texture after allocation");
      e2.hasMipmap = false, this._samplingModeDirty = true, s4(e2);
    }
    9985 === e2.samplingMode ? (this._samplingModeDirty = true, e2.samplingMode = 9729) : 9984 === e2.samplingMode && (this._samplingModeDirty = true, e2.samplingMode = 9728);
  }
  setSamplingMode(t2) {
    t2 !== this._descriptor.samplingMode && (this._descriptor.samplingMode = t2, this._samplingModeDirty = true);
  }
  setWrapMode(t2) {
    t2 !== this._descriptor.wrapMode && (this._descriptor.wrapMode = t2, s4(this._descriptor), this._wrapModeDirty = true);
  }
  setShadowFiltering(t2) {
    t2 !== this._descriptor.linearFilterDepth && (this._descriptor.linearFilterDepth = this._descriptor.compareEnabled = t2, this.setSamplingMode(t2 ? 9729 : 9728), s4(this._descriptor), this._shadowFilterDirty = true);
  }
  applyChanges() {
    this._samplingModeDirty && (this._applySamplingMode(), this._samplingModeDirty = false), this._wrapModeDirty && (this._applyWrapMode(), this._wrapModeDirty = false), this._shadowFilterDirty && (this._applyShadowMode(), this._shadowFilterDirty = false);
  }
  abortCompression() {
    this._compressionAbortController = e(this._compressionAbortController);
  }
  _setData(i2, r) {
    const s5 = this._descriptor, o = s5.context?.gl;
    if (!o) return;
    u(o), this.hasWebGLTextureObject || (this._glName = o.createTexture(), s5.context.instanceCounter.increment(O.Texture, this)), s4(s5);
    const p2 = s5.context.bindTexture(this, _a.TEXTURE_UNIT_FOR_UPDATES);
    s5.context.setActiveTexture(_a.TEXTURE_UNIT_FOR_UPDATES), this._configurePixelStorage(), u(o);
    const l2 = r ?? s5.target, g2 = x(l2);
    if (w(i2)) this._setDataFromTexImageSource(i2, l2);
    else {
      const { width: e2, height: r2, depth: n } = s5;
      if (null == e2 || null == r2) throw new s("texture:missing-size", "Width and height must be specified!");
      if (g2 && null == n) throw new s("texture:missing-depth", "Depth must be specified!");
      if (s5.internalFormat = E2(s5), s5.isImmutable && !this._wasImmutablyAllocated && this._texStorage(l2, s5.internalFormat, s5.hasMipmap, e2, r2, n), d2(i2)) {
        if (!f(s5.internalFormat)) throw new s("texture:format-mismatch", "Attempting to use compressed data with an uncompressed format!");
        this._setDataFromCompressedSource(i2, s5.internalFormat, l2);
      } else this._texImage(l2, 0, s5.internalFormat, e2, r2, n, i2), u(o), s5.hasMipmap && this.generateMipmap();
    }
    this._applySamplingMode(), this._applyWrapMode(), this._applyAnisotropicFilteringParameters(), this._applyShadowMode(), u(o), s5.context.bindTexture(p2, _a.TEXTURE_UNIT_FOR_UPDATES);
  }
  _setDataCubeMap(t2 = null) {
    for (let e2 = 34069; e2 <= 34074; e2++) this._setData(t2, e2);
  }
  _configurePixelStorage() {
    const t2 = this._descriptor.context.gl, { unpackAlignment: e2, flipped: i2, preMultiplyAlpha: r } = this._descriptor;
    t2.pixelStorei(t2.UNPACK_ALIGNMENT, e2), t2.pixelStorei(t2.UNPACK_FLIP_Y_WEBGL, i2 ? 1 : 0), t2.pixelStorei(t2.UNPACK_PREMULTIPLY_ALPHA_WEBGL, r ? 1 : 0);
  }
  _setDataFromTexImageSource(t2, e2) {
    const { gl: i2 } = this._descriptor.context, r = this._descriptor;
    r.internalFormat = E2(r);
    const s5 = x(e2), { width: o, height: n, depth: p2 } = T2(t2);
    r.width && r.height, r.width || (r.width = o), r.height || (r.height = n), s5 && r.depth, s5 && (r.depth = p2), r.isImmutable && !this._wasImmutablyAllocated && this._texStorage(e2, r.internalFormat, r.hasMipmap, o, n, p2), this._texImage(e2, 0, r.internalFormat, o, n, p2, t2), u(i2), r.hasMipmap && (this.generateMipmap(), u(i2));
  }
  _setDataFromCompressedSource(t2, e2, i2) {
    const r = this._descriptor, { width: s5, height: o, depth: a } = r, n = t2.levels, p2 = F(i2, s5, o, a), l2 = Math.min(p2, n.length) - 1;
    this._descriptor.context.gl.texParameteri(r.target, 33085, l2), this._forEachMipmapLevel((t3, r2, s6, o2) => {
      const a2 = n[Math.min(t3, n.length - 1)];
      this._compressedTexImage(i2, t3, e2, r2, s6, o2, a2);
    }, l2);
  }
  _texStorage(e2, i2, r, s5, o, a) {
    const { gl: n } = this._descriptor.context;
    if (!h2(i2) && !m(i2) && !p(i2)) throw new s("texture:missing-format", "Immutable textures must have a sized internal format");
    if (!this._descriptor.isImmutable) return;
    const p2 = r ? F(e2, s5, o, a) : 1;
    if (x(e2)) {
      if (null == a) throw new s("texture:missing-depth", "Missing depth dimension for 3D texture upload");
      n.texStorage3D(e2, p2, i2, s5, o, a);
    } else n.texStorage2D(e2, p2, i2, s5, o);
    this._wasImmutablyAllocated = true;
  }
  _texImage(e2, i2, r, s5, o, a, n) {
    const p2 = this._descriptor.context.gl, l2 = x(e2), { isImmutable: m2, pixelFormat: h3, dataType: d3 } = this._descriptor;
    if (m2) {
      if (null != n) {
        const r2 = n;
        if (l2) {
          if (null == a) throw new s("texture:missing-depth", "Missing depth dimension for 3D texture upload");
          p2.texSubImage3D(e2, i2, 0, 0, 0, s5, o, a, h3, d3, r2);
        } else p2.texSubImage2D(e2, i2, 0, 0, s5, o, h3, d3, r2);
      }
    } else {
      const m3 = n;
      if (l2) {
        if (null == a) throw new s("texture:missing-depth", "Missing depth dimension for 3D texture upload");
        p2.texImage3D(e2, i2, r, s5, o, a, 0, h3, d3, m3);
      } else p2.texImage2D(e2, i2, r, s5, o, 0, h3, d3, m3);
    }
  }
  _compressedTexImage(e2, i2, r, s5, o, a, n) {
    const p2 = this._descriptor.context.gl, l2 = x(e2);
    if (this._descriptor.isImmutable) {
      if (null != n) if (l2) {
        if (null == a) throw new s("texture:missing-depth", "Missing depth dimension for 3D texture upload");
        p2.compressedTexSubImage3D(e2, i2, 0, 0, 0, s5, o, a, r, n);
      } else p2.compressedTexSubImage2D(e2, i2, 0, 0, s5, o, r, n);
    } else if (l2) {
      if (null == a) throw new s("texture:missing-depth", "Missing depth dimension for 3D texture upload");
      p2.compressedTexImage3D(e2, i2, r, s5, o, a, 0, n);
    } else p2.compressedTexImage2D(e2, i2, r, s5, o, 0, n);
  }
  async _compressOnWorker(t2) {
    const { width: i2, height: r, context: a, flipped: n, preMultiplyAlpha: p2, hasMipmap: l2 } = this._descriptor, m2 = this._descriptor.compress?.compressionTracker, h3 = this._descriptor.compress?.compressionCallback, { compressedTextureETC: d3, compressedTextureS3TC: c2 } = a.capabilities;
    if (!_a.compressionWorkerHandle?.isCompressible(t2, this._descriptor) || !d3 && !c2) return;
    this.abortCompression();
    const _2 = new AbortController();
    let u2;
    this._compressionAbortController = _2, m2?.increment();
    try {
      t2 instanceof Uint8Array ? u2 = t2.buffer : (u2 = await createImageBitmap(t2, { imageOrientation: n ? "flipY" : "none" }), s2(_2));
      const o = { data: u2, width: i2, height: r, needsFlip: t2 instanceof Uint8Array && this.descriptor.flipped, components: 6408 === this._descriptor.pixelFormat ? 4 : 3, preMultiplyAlpha: p2, hasMipmap: l2, hasETC: !!d3, hasS3TC: !!c2 }, a2 = await _a.compressionWorkerHandle.invoke(o, _2.signal, "low");
      if (s2(_2), a2.compressedTexture && this.hasWebGLTextureObject) {
        const t3 = this.usedMemory;
        this._descriptor.internalFormat = a2.internalFormat, this._setData(a2.compressedTexture), h3?.(t3 - this.usedMemory);
      }
    } catch (g2) {
      d(g2) || E3().error("Texture compression failed!");
    } finally {
      m2?.decrement(), this._compressionAbortController?.signal.aborted && (this._compressionAbortController = null), u2 instanceof ImageBitmap && u2.close();
    }
  }
  _forEachMipmapLevel(e2, i2 = 1 / 0) {
    let { width: r, height: s5, depth: o, hasMipmap: a, target: n } = this._descriptor;
    const p2 = 32879 === n;
    if (null == r || null == s5 || p2 && null == o) throw new s("texture:missing-size", "Missing texture dimensions for mipmap calculation");
    for (let t2 = 0; e2(t2, r, s5, o), a && (1 !== r || 1 !== s5 || p2 && 1 !== o) && !(t2 >= i2); ++t2) r = Math.max(1, r >> 1), s5 = Math.max(1, s5 >> 1), p2 && (o = Math.max(1, o >> 1));
  }
  _applySamplingMode() {
    const t2 = this._descriptor, e2 = t2.context?.gl;
    let i2 = t2.samplingMode, r = t2.samplingMode;
    9985 === i2 || 9987 === i2 ? (i2 = 9729, t2.hasMipmap || (r = 9729)) : 9984 !== i2 && 9986 !== i2 || (i2 = 9728, t2.hasMipmap || (r = 9728)), e2.texParameteri(t2.target, e2.TEXTURE_MAG_FILTER, i2), e2.texParameteri(t2.target, e2.TEXTURE_MIN_FILTER, r);
  }
  _applyWrapMode() {
    const t2 = this._descriptor, e2 = t2.context?.gl;
    "number" == typeof t2.wrapMode ? (e2.texParameteri(t2.target, e2.TEXTURE_WRAP_S, t2.wrapMode), e2.texParameteri(t2.target, e2.TEXTURE_WRAP_T, t2.wrapMode)) : (e2.texParameteri(t2.target, e2.TEXTURE_WRAP_S, t2.wrapMode.s), e2.texParameteri(t2.target, e2.TEXTURE_WRAP_T, t2.wrapMode.t));
  }
  _applyShadowMode() {
    const t2 = this._descriptor, e2 = t2.context?.gl, i2 = t2.compareEnabled ? e2.COMPARE_REF_TO_TEXTURE : e2.NONE;
    e2.texParameteri(t2.target, e2.TEXTURE_COMPARE_MODE, i2), t2.compareEnabled && e2.texParameteri(t2.target, e2.TEXTURE_COMPARE_FUNC, e2.GREATER), u(e2);
  }
  _applyAnisotropicFilteringParameters() {
    const t2 = this._descriptor, e2 = t2.context.capabilities.textureFilterAnisotropic;
    if (!e2) return;
    t2.context.gl.texParameterf(t2.target, e2.TEXTURE_MAX_ANISOTROPY, t2.maxAnisotropy ?? 1);
  }
}, _a.TEXTURE_UNIT_FOR_UPDATES = 0, _a.compressionWorkerHandle = null, _a);

export {
  h,
  m,
  p,
  f,
  t,
  A
};
//# sourceMappingURL=chunk-YNGW52OC.js.map
